# 実験仕様書（Spec） - EXP-OBS000001（PEAD: 決算サプライズ後ドリフト）

> 担当: S戦略チーム（chief-strategist）が A設計官を兼務（PJ000002 §2.1）
> 鉄則: **成功基準は「回す前」に数値で確定する**（HARKing防止）。結果を見てから基準を緩めない。
> 前提文書: `research/EXP-OBS000001/00-prescreen.md`（条件付きGO・必須条件①〜⑤・条件⓪・引き継ぎ論点S-1〜S-8）
> 起票日: 2026-09-13

## 対応OBS番号

OBS000001（`research/EXP-OBS000001/`）

---

## 0. 本 spec の位置づけと prescreen 必須条件の反映表

prescreen の必須条件・引き継ぎ論点が本 spec のどこに落ちているかの対応表。C品質チームはこの表を使って落とし漏れを検査すること。

| prescreen | 内容 | 本 spec の反映先 |
|---|---|---|
| 条件⓪ | R-1/R-1c/R-1e 解消・R-1d 部分解消 | §1.3（未解消項目の扱い）・§9（B実装が先に潰す項目） |
| 必須条件① | ②テクニカルシグナルと合成しない | §5.1 シグナル定義／§10 禁止事項 |
| 必須条件② | 入力は会計サプライズのみ・価格由来の代理変数を使わない | §3 SUE定義／§10 禁止事項（許可される価格利用の範囲も明記） |
| 必須条件③ | 連続値IC主指標＋デシル単調性副指標＋日付ブロックpermutation | §6.1〜§6.3 |
| 必須条件④ | G2はSharpe有意性で判定しない。約定不能率・スロット競合率等を生データ出力 | §6.4・§6.5 |
| 必須条件⑤ | ベースラインはロングオンリー／G3・G4は判定不能 | §5.2・§6.6・§6.7 |
| S-1 | SUE定義の数値確定・多重検定の扱い | §3 |
| S-2 | 約定モデルの固定・**窓長の算術的整合性** | §4（特に **§4.0 の値幅制限による約定可否判定**・§4.5 の算術整合性表） |
| S-3 | 決算跨ぎルールの読み替え | §7 チェックリスト D項 |
| S-4 | リスク集中上限の数値固定 | §5.3 |
| S-5 | G3/G4判定不能の申し送り | §6.6・§6.7 |
| S-6 | 選定/確認分割はA案主・B案補助 | §2 |
| S-7 | 日本株固有模擬条件チェックリスト | §7 |
| S-8 | 損益分岐勝率の再計算 | §8.3 |

---

## 1. 仮説（1文）

**決算短信の開示によって更新される「会社自身の通期経常利益見通し」の変化量（＝会計サプライズ SUE）は、開示翌営業日始値から起算した3〜10営業日の株価リターンを、日本株 TOPIX500 相当ユニバースにおいて、往復コスト0.35%控除後も正の期待値が残る程度に予測する。**

### 1.1 エッジ族の宣言

- 予測変数の生成過程: **会計データのみ**（会社予想・実績の経常利益と予想売上高）。価格・出来高・ボラティリティを**一切入力にしない**。
- したがって PJ000001 §4 C-3 が低事前確率とした「価格由来の特徴量から方向を予測する」族には属さない。
- ただし出力形（イベント後の一方向継続を取る）は TSMOM と同型である。この区別が本質的でなかった場合、本 EXP も同じ天井に当たる（prescreen §弱点4）。**この可能性は spec 段階で排除できない。G1 の結果で判定する。**

### 1.2 事前確率の見立て

**中**。ただし根拠は弱い。prescreen 要確認 R-5（日本株 PEAD の学術的エビデンス）は **spec 作成時点でも一次文献を直接確認できていない**。STRATEGY-BRIEF の「学術的に日本株でも報告のあるアノマリー」という記述の一次出典は未検証である。**本 spec は「日本株 PEAD が文献で確認されている」ことを前提にしない。** 文献の存在を根拠に基準を緩めることはしない。

### 1.3 条件⓪の残件と本 spec での扱い

| 項目 | 状態 | 本 spec での扱い |
|---|---|---|
| R-1（会社予想フィールド） | 解消 | `FOdP` / `NxFOdP` / `FSales` / `NxFSales` を §3 で使用 |
| R-1b（開示時刻） | 実質解消（`DiscTime` がスキーマに存在） | **記録するがエントリー判定には使わない**（§4.1 で保守側に固定） |
| R-1c（TOPIX500識別） | 解消（`ScaleCat`） | §5.4 ユニバース定義で使用。**ただし取りうる値の対応付けは未確認 → §9-1 で B が先に実測し `params.json` に明記。推測で埋めることを禁止** |
| R-1d（日足の全期間連続性） | 部分解消（契約開始〜2024-10-01 の5ヶ月のみ確認） | **§9-2 を B実装の最初のタスクとし、欠損営業日率 > 5% なら S に差し戻す**（実験を回さない） |
| R-1e（契約有効性） | 解消（Freeプラン契約中） | 対応不要 |
| R-2（呼値単位） | 未確認 | コスト前提は仮置きのまま。§8.2 の感度分析でカバー |
| R-3（手数料・金利の実額） | 未確認（司令塔確認待ち） | 同上。仮置き値を §8.1 に固定 |
| R-4（2024年8月急落の配置） | 本 spec で確定 | §2.3 に記載。**確認期間にテール局面が入らない** |
| R-6（決算集中度） | 未確認 | §9-3 で B が実測し生データ出力 |

---

## 2. 選定 / 確認プロトコル（過学習の炙り出し）

### 2.1 主方式（A案・時期分割）

| 区分 | 期間（開示日 `DiscDate` ベース） | 役割 |
|---|---|---|
| **選定（参照）** | 2024-06-21 〜 2025-06-30 | 事前固定した設計が選定期間でどう見えるかを**記録するだけ**。ここで何かを選ぶことはしない |
| **確認（未見）** | 2025-07-01 〜 2026-06-21 | **G1・G2 の合否はこの期間のみで判定する** |

### 2.2 本 EXP における「選定」の自由度はゼロである（最重要）

本 spec は SUE 定義・予測ホライズン・トレール4パラメータ・閾値・ユニバース条件・コスト前提の**すべてを回す前に数値で固定している**。したがって**選定期間で選ぶパラメータは1つも存在しない**。

- 選定期間は「事前固定設計の参照測定」であり、チューニングの場ではない。
- B実装チームは、選定期間の結果を見て spec のいかなる値も変更してはならない。変更が必要と判断した場合は**実装を止めて S に差し戻す**（§10）。
- 選定/確認の符号一致（§6.2 G1-2、§6.4 G2-3）は、この前提の下での再現性確認として要求する。

### 2.3 測定範囲の空白（明示）

- **確認期間（2025-07-01〜2026-06-21）にはテール局面が含まれない。** 2024年8月上旬の急落は**選定期間側**に配置される。prescreen §レジーム依存性が「部分的に改善」としたのは全期間についての記述であり、A案の時期分割では確認期間側のテール窓はゼロになる。**この空白は STEP3 フォワード較正でのみ補完できる。**
- 確認期間に含まれる決算シーズンは約4回。**実質的に4シーズン分の共通要因しか観測していない。** 季節性・特定シーズンのマクロ環境に依存した結果である可能性を排除できない。
- 信用規制（日々公表銘柄・増担保規制）は現行契約でデータが取得できず（C-5・HTTP 403）、**模擬不能**。バックテストは規制発動がない前提＝**楽観側に偏る**（§7 F項）。

### 2.4 補助方式（B案・銘柄分割）

**確認期間で G1 に合格した場合にのみ**、追加確認として実施する。

- 分割方法: ユニバース銘柄を `Code` の昇順に並べ、**奇数番目群 / 偶数番目群**の2群に分ける（乱数を使わない決定的分割）。
- 全期間（2024-06-21〜2026-06-21）で両群それぞれの `IC_bar`（§6.1）を算出し、**符号が一致するか**を確認する。
- **B案の結果は合否判定に使わない。** 追加情報としてのみ `prediction-unit.json` に出力する。

---

## 3. シグナル定義: SUE（S-1の確定）

### 3.1 単一の事前登録定義（これ以外を合否判定に使わない）

各イベント（＝ある銘柄 j のある決算短信開示 t）について、以下を定義する。使用フィールドはすべて `/fins/summary`。

| 記号 | 定義 | 取得元フィールド |
|---|---|---|
| `A(t)` | **本開示時点における当期通期経常利益の会社見通し** | 1Q/2Q/3Q 開示 → `FOdP`（当期通期予想）<br>通期（FY）開示 → `OdP`（当期通期**実績**。予想が実績に確定した情報更新として扱う） |
| `B(t)` | **直前開示時点で示されていた「同一会計年度」の通期経常利益の会社予想** | 同一銘柄・同一会計年度に対する通期経常利益予想を含む**直近の開示**（`DiscDate`, `DiscTime` の順で最大）の値。<br>直前が1Q/2Q/3Q開示または業績修正開示 → その開示の `FOdP`<br>直前が前期のFY開示 → その開示の `NxFOdP`（＝当期の通期予想） |
| `Scale(t)` | **直前開示時点の同一会計年度の通期予想売上高**（スケールフリー化の分母） | 直前が1Q/2Q/3Q開示または修正開示 → `FSales`<br>直前が前期のFY開示 → `NxFSales` |

**RawSUE(j, t) = ( A(t) − B(t) ) / Scale(t)**

単位は「通期予想売上高比の経常利益ポイント（会計マージンのポイント）」。

### 3.2 会計年度の一致判定（曖昧さを残さない）

- `A(t)` の対象会計年度は本開示の `CurFYSt` / `CurFYEn` で特定する。
- `B(t)` / `Scale(t)` を探すとき、直前開示が 1Q/2Q/3Q/修正開示なら、その開示の `CurFYSt` / `CurFYEn` が本開示の `CurFYSt` / `CurFYEn` と**完全一致**することを要求する。
- 直前開示が FY開示なら、その開示の `NxtFYSt` / `NxtFYEn` が本開示の `CurFYSt` / `CurFYEn` と**完全一致**することを要求する。
- 一致する直前開示が存在しない（＝データ窓の先頭でその銘柄の最初の開示）場合、そのイベントは**除外**し、除外件数を `prediction-unit.json` に記録する。

### 3.3 除外規則（すべて事前登録・件数を必ず記録）

以下はいずれも**リターンを見る前に**適用する。

1. `A(t)`、`B(t)`、`Scale(t)` のいずれかが null / 空文字 / 非数値 → 除外（「予想非開示」「未定」を含む）
2. `Scale(t)` ≤ 0 → 除外
3. 予測対象イベントは **四半期決算短信および通期決算短信の開示のみ**（`DocType` による）。業績修正開示・配当予想修正等は `B(t)` の探索対象にはなるが、**イベント（予測対象）にはしない**
4. `DocType` / `CurPerType` / `ScaleCat` / `MrgnNm` の取りうる値は未確認である。**B実装チームは実データから全ユニーク値と件数を先に出力し、本 spec の分類への対応付けを `params.json` に明記すること。対応付けが一意に決まらない場合は推測で埋めず S に差し戻す**（trading-app-v2 EXP-OBS000037 の教訓）
5. 同一銘柄・同一 `DiscDate` に同種の開示が複数ある場合（訂正等）、`DiscTime` が最も遅い1件のみを採用する。`DiscTime` が同一なら `DiscNo` が最大の1件

### 3.4 標準化

- **主指標（IC）には追加の標準化を行わない。** §6.1 の主検定は Spearman 順位相関であり、単調変換に不変であるため `RawSUE` をそのまま使う。
- **副指標（デシル）には、先読みのないトレーリング分位を使う**:
  - `SUEpct(j,t)` = ユニバース全銘柄の、**`DiscDate` が t より厳密に前**である全イベントの `RawSUE` の経験分布における、`RawSUE(j,t)` のパーセンタイル（拡大窓）。
  - 拡大窓の参照イベント数が **200件未満**の場合、そのイベントは**デシル分析からのみ除外**する（IC分析からは除外しない）。除外件数を記録する。
  - 同値（タイ）は平均順位で処理する。`RawSUE` がちょうど 0（＝予想改訂なし）のイベントが大量に発生することが想定されるため、**`RawSUE` の厳密なゼロ件数の比率を必ず出力すること**。

### 3.5 winsorize / 外れ値処理

- `RawSUE` の winsorize: **行わない**（主指標が順位相関のため不要）。
- リターンの winsorize: **行わない**（実際に発生する損益を歪めるため）。

### 3.6 事前登録した感度分析（合否判定には使えない）

以下は**生データとして出力を必須**とするが、**G1・G2 の合否判定に使ってはならない**。これらのいずれかが良好であることを理由に本 EXP を合格にすることは禁止する。使いたい場合は**新規 EXP として起票**し、改めて確認期間を切り直す。

| # | 振る軸 | 水準 |
|---|---|---|
| SA-1 | 利益項目 | `OdP`（経常利益・**主**） / `OP`（営業利益） / `NP`（純利益） |
| SA-2 | 予測ホライズン H | 3営業日 / **5営業日（主）** / 10営業日 |
| SA-3 | シグナル閾値 | 上位5% / **上位10%（主）** / 上位20% / 上位30% |
| SA-4 | 往復コスト | 0.25% / **0.35%（主）** / 0.50% |

**多重検定の扱い**: 合否判定に用いる検定は **§6.1 の主検定1本のみ**である（主定義 `OdP` × H=5 × 上位10% × コスト0.35%）。したがって多重性の補正は不要。SA-1〜SA-4 は記述統計であり、検定として解釈してはならない。

---

## 4. 約定モデルの固定（S-2の確定）

### 4.0 値幅制限フラグ `UL` / `LL` の解釈と約定可否判定関数（2026-09-13 改訂で確定）

**本節は §4.1（エントリー）・§4.4（決済）・§7 B項が参照する唯一の正本である。約定可否に関する値幅制限の判定は、本節の2式以外の方法で行ってはならない。**

#### 4.0.1 実測により確定したフィールドの意味

- `/equities/bars/daily` の日足スキーマは `Date,Code,O,H,L,C,UL,LL,Vo,Va,AdjFactor,Adj*,MktCap,ExRT` であり、**その日の値幅制限の価格水準そのものを表すフィールドは存在しない。**
- `UL` / `LL` は**価格水準ではなく 0/1 のフラグ**である（文字列型で `"1"` / `"0"` が返る）。
- 意味の確定: **`UL(t)=1` ⇔ その日の高値 `H(t)` がその日の制限値幅上限の価格に達した。`LL(t)=1` ⇔ その日の安値 `L(t)` がその日の制限値幅下限の価格に達した。**
  - 根拠1（実測 `130A0` / 2024-08-05、`O=677, H=720, L=637, C=637, LL="1"`）: `L=637` を下限価格とすると、直前終値 `787`（基準値段 700円以上1000円未満 → 制限値幅 ±150円）からの下限 `787−150=637` と厳密に一致する。JPX 制限値幅表の下で `P−w(P)=637` を満たす基準値段は `P=787` のみであり、一意に整合する。
  - 根拠2（実測 `UL="1"` サンプル、`O=738, H=748, C=741`）: `H=748` を上限価格とすると、直前終値 `648`（基準値段 500円以上700円未満 → 制限値幅 ±100円）からの上限 `648+100=748` と厳密に一致する。`P+w(P)=748` を満たす基準値段も `P=648` のみで一意。
  - 根拠2は **`C≠H`** である。したがって「**終値**が制限に達したか」という解釈ではこのサンプルを説明できない。「**日中の高値／安値**が制限に達したか」と読むのが唯一整合する解釈である。
- **系（重要）**: `UL(t)=1` のとき、その日の制限値幅上限の価格は `H(t)` に等しい。`LL(t)=1` のとき、制限値幅下限の価格は `L(t)` に等しい。フラグが 0 の日は制限価格を復元できないが、**制限に達していない日は約定可否判定に一切影響しないため、復元する必要がない。**

#### 4.0.2 旧定義との論理的同値性（設計判断ではなく恒等式であること）

改訂前の spec は `O(t1) ≥ UL(t1)`（`UL` を上限価格とみなす数値比較）を条件としていた。§4.0.1 の意味の下で、次が**恒等的に成立する**（近似ではない）。

- `O(t) ≥ 上限価格(t)` ⟺ `UL(t)=1 かつ O(t) = H(t)`
  - （⇐）`UL(t)=1` なら `H(t)` が上限価格。`O(t)=H(t)` より `O(t)` は上限価格に等しく、`≥` を満たす。
  - （⇒）`O(t)` は上限価格を超えられないので `O(t) = 上限価格`。また `O(t) ≤ H(t) ≤ 上限価格` であるから `O(t)=H(t)=上限価格`。よって `UL(t)=1` かつ `O(t)=H(t)`。
- 同様に `O(t) ≤ 下限価格(t)` ⟺ `LL(t)=1 かつ O(t) = L(t)`

**したがって本改訂は約定モデルの厳しさを変更しない。** 改訂前の意図を、実在するフィールドだけで**厳密に**再構成したものである。B実装チームは「近似だから安全側に倒す」といった追加の解釈を加えてはならない（N-6）。

#### 4.0.3 パース規則（一意に確定。推測禁止）

- `UL` / `LL` の生の値を文字列化し前後空白を除去した `s` について: **`s == "1"` → 真。`s == "0"` または `s == ""` または値が null → 偽。それ以外の値が1件でも出現した場合、推測で埋めずに S へ差し戻す**（N-6）。
- B実装チームは §9-1b として **`UL` / `LL` の全ユニーク値と件数**を出力すること。
- `UL` / `LL` が null / 欠損である行の件数を出力する。**ユニバース×全期間の日足行数に対する欠損率が 1% を超える場合は S へ差し戻す**（模擬不能な範囲が無視できない大きさになるため）。
- 価格の同値比較（`O==H` / `O==L`）は、**同一日・同一スケールの値どうし**で行う（生値どうし、または調整値どうし。生値と `Adj*` を混在させない。同一日の中では調整係数が共通のため、どちらを使っても判定結果は変わらない）。
- 数値は `Decimal`（API 応答文字列から直接生成）で厳密比較する。やむを得ず `float` を使う場合の同値判定は `abs(x − y) ≤ 1e-6 × max(1.0, abs(y))` とする。

#### 4.0.4 約定可否の判定関数（この2式をそのままコードに落とすこと）

```
BUY_BLOCKED(t)  :=  (Vo(t) == 0) or (O(t) is null) or ( UL(t)==1 and O(t) == H(t) )
SELL_BLOCKED(t) :=  (Vo(t) == 0) or (O(t) is null) or ( LL(t)==1 and O(t) == L(t) )
```

- `UL(t)==1 and O(t)==H(t)` は「**寄り付き値がその日の制限値幅上限の価格に一致する**」ことを厳密に意味する。本 spec はこれを「ストップ高で寄り付き、寄り付きの買い注文が約定しない」とみなす。**寄らずのストップ高（比例配分）は `O=H=L=C=上限価格` となるため、同一条件で捕捉される。**
- **`UL(t)=1` だが `O(t) < H(t)` の日は約定不能にしない。** 寄り付きは上限未満であり、寄り付きの買いは約定する。日中に上限へ達した事実は、日足完結モデルでは約定に影響しない（根拠2の実測サンプルがこのケースに該当する）。売り側も対称に扱う。
- **方向の非対称性は意図的である。** 買い注文が約定しないのは需要超過（ストップ高）のときであり、売り注文が約定しないのは供給超過（ストップ安）のときである。**買いでストップ安、売りでストップ高は約定制約にしない**（そちらの方向では約定が容易になるため）。
- **JPX の制限値幅表を直前終値から自前で再構成して上限/下限価格を計算してはならない。** 値幅制限の拡大措置・新規上場銘柄・基準値段の特例など例外規定を再現できず、独自解釈の温床になる（§4.0.1 の根拠提示で用いた制限値幅表は、フラグの**意味を同定するための検証**にのみ使ったものであり、**実装には使わない**）。判定は §4.0.4 の2式のみで行う。
- 本節の `UL` / `LL` / `O` / `H` / `L` / `Vo` の利用は**約定可否の制約および損益計算**であり、予測変数 `SUE` の生成には一切入らない。必須条件②・N-2 に抵触しない（§10.3）。

### 4.1 エントリー

- 開示日を `t0`（= `DiscDate`）とする。
- **エントリーは `t0` の翌営業日 `t1` の始値 `O(t1)`** に固定する。`DiscTime` が場前・場中・場後のいずれであっても同じ扱いとする（最も保守的な統一。場前開示の機会を捨てる方向）。
- **`BUY_BLOCKED(t1)`（§4.0.4）が真の場合は約定不能**とし、**そのイベントは見送る（翌日以降の再エントリーを行わない）**。約定不能として件数を記録する。
  - 内訳を必ず3分類で記録する: (i) `Vo(t1) = 0`、(ii) `O(t1)` が null、(iii) `UL(t1)=1` かつ `O(t1)=H(t1)`（ストップ高寄り付き）。
  - 参考値として、**`UL(t1)=1` だが `O(t1) < H(t1)` であったため約定したとみなした件数**も併せて記録する（判定には使わない。C品質チームが約定可否の切り分けを検証するため）。
- 約定価格 = `O(t1) × (1 + 0.075%)`（買い片道スリッページ）。

### 4.2 予測単位（G1）の前方リターン

- **ホライズン H = 5営業日（主）**。
- **R_H(j) = C(t_H) / O(t_1) − 1**（グロス。コスト・スリッページを一切引かない）。
- `t_1` から `t_H` までの営業日が確保できないイベント（データ末尾近傍）は G1 から**除外**し、除外件数を記録する。
- `t_1` が取れないイベント（§4.1 の約定不能）も G1 から除外する。**ただし G1 は「取引可能性」ではなく「予測力」の検定であるため、約定不能イベントを除外した場合と、`O(t1)` を用いて仮想的に含めた場合の両方の `IC_bar` を出力すること**（主判定は除外版）。

### 4.3 決済ルール（トレーリングストップ。PJ000001 C-1 決着に準拠）

4パラメータをすべて数値固定する。

| # | パラメータ | 固定値 |
|---|---|---|
| 1 | 初期損切幅 | エントリー約定価格の **−3.0%** |
| 2 | トレール発動水準 | 終値ベースの含み益が **+4.0%** 以上に到達した日の**翌営業日から**有効化 |
| 3 | 追随幅 | 発動後、`t_1` 以降の**日次終値**の最高値から **−2.0%** |
| 4 | 更新足と約定価格 | **日足終値のみ**でストップ水準を更新し、**日足終値のみ**で抵触判定する。抵触した日の**翌営業日始値**で約定したとみなす |

- 日中高値・日中安値（`H` / `L`）は**トレールの更新にも抵触判定にも使わない**。日中の高値でトレール判定して当日中に約定できたとみなす楽観を構造的に排除するため（PJ000001 C-1 決着の警告への対応）。
- 初期損切も同じ扱い: `C(t) ≤` エントリー約定価格 × 0.97 なら、**翌営業日始値**で手仕舞い。
- 決済時の約定価格 = `O(t_exit) × (1 − 0.075%)`（売り片道スリッページ）。

### 4.4 決済できない場合（値幅制限の模擬）

- 手仕舞い日 `t_exit` について **`SELL_BLOCKED(t_exit)`（§4.0.4）が真の場合、約定できず持ち越し**とする。
- 持ち越し中は、**その後の各営業日 `t` について `SELL_BLOCKED(t)` を同じ式で再評価**し、偽となった最初の営業日の始値で約定する（約定価格は §4.3 の売り片道スリッページを適用）。その間の下落はすべて損失として計上する。
- **最大5営業日**持ち越しても約定できない場合、5営業日目の終値で強制的に評価損益を確定し、「強制評価」としてフラグを立てて件数を記録する。
- 本ルールは、§4.3 の初期損切・トレール抵触による決済、および §4.5 の最大保有期間超過による強制手仕舞い（`t_11` 始値）、§7 D項の未発表決算跨ぎ回避による手仕舞い、§5.3-10 のサーキットブレーカー清算を含む、**すべての売り約定に一律に適用する**。
- 内訳を必ず3分類で記録する: (i) `Vo(t_exit) = 0`、(ii) `O(t_exit)` が null、(iii) `LL(t_exit)=1` かつ `O(t_exit)=L(t_exit)`（ストップ安寄り付き）。
- 参考値として、**`LL(t_exit)=1` だが `O(t_exit) > L(t_exit)` であったため約定したとみなした件数**も併せて記録する（判定には使わない）。
- **`UL(t_exit)=1`（ストップ高）は売り約定を妨げない**（§4.0.4 の非対称性）。ストップ高の日の始値での売り約定は通常どおり成立させる。

### 4.5 保有期間の上限と、窓長の算術的整合性（trading-app-v2 EXP-OBS000037 の教訓）

**最大保有期間 = 10営業日**（PJ000001 §1 の「3〜10営業日保有」の上限）。`t_1` から数えて10本目の日足 `t_10` の終値時点で未決済なら、**`t_11` の始値**で強制手仕舞いする。

以下の対応関係を、実装前に必ず確認すること。**曖昧な箇所を独自解釈で埋めてはならない。**

| 用語 | 日足の本数 | 使う価格の点数 | 日付レンジ | 日次リターンの本数 |
|---|---|---|---|---|
| **予測単位 H=5営業日** | `t_1`〜`t_5` の **5本** | 始値1個 `O(t_1)` ＋ 終値5個 `C(t_1)`〜`C(t_5)` = **6点** | `t_1` 〜 `t_5` | `O(t_1)→C(t_1)`, `C(t_1)→C(t_2)`, …, `C(t_4)→C(t_5)` の **5本**（6点の隣接差分 = 6−1 = 5本 ✓） |
| **予測単位 H=3営業日**（SA-2） | `t_1`〜`t_3` の **3本** | 始値1個 ＋ 終値3個 = **4点** | `t_1` 〜 `t_3` | **3本**（4−1=3 ✓） |
| **予測単位 H=10営業日**（SA-2） | `t_1`〜`t_10` の **10本** | 始値1個 ＋ 終値10個 = **11点** | `t_1` 〜 `t_10` | **10本**（11−1=10 ✓） |
| **パイプライン最大保有** | `t_1`〜`t_10` の **10本**（保有判定に使う日足）＋ 強制決済日 `t_11` の始値 | 始値1個 `O(t_1)` ＋ 終値10個 `C(t_1)`〜`C(t_10)` ＋ 始値1個 `O(t_11)` = **12点** | エントリー `t_1`、最終決済 `t_11` | `O(t_1)→C(t_1)` から `C(t_9)→C(t_10)` まで **10本**、加えて `C(t_10)→O(t_11)` の決済ギャップ **1本** = 合計 **11本**（12−1=11 ✓） |
| **トレール最高値の参照範囲** | `t_1` 〜 抵触判定日 `t_k` | 終値のみ **k個**（`C(t_1)`〜`C(t_k)`） | `t_1` 〜 `t_k` | — |

- **「N営業日保有」は、`t_1` から数えて N本の日足を保有判定に使うことを意味する。** 決済約定はその翌営業日 `t_{N+1}` の始値である（＝価格点は N+2 個になる）。
- `t_0`（開示日）の日足は**一切使わない**。`C(t_0)` も `O(t_0)` も損益計算・シグナル計算に入れない。
- 営業日の定義: **日足データ（`/equities/bars/daily`）にその銘柄の行が存在する日**とする。売買停止で行が存在しない日は営業日として数えない（数えない場合の件数を記録）。

---

## 5. パイプライン統合の設計

### 5.1 シグナル（必須条件①・②の実装）

- ロング候補 = `SUEpct(j,t0)` が**上位10%（第10デシル）**のイベント。
- **テクニカル指標を一切参照しない。** MACD / RSI / ボリンジャーバンド / CCI / 移動平均 / ADX / ATR / Stochastic / モメンタム / 出来高指標のいずれも、エントリー・エグジット・フィルタ・サイジングの判断に用いない（`②` との合成は本 EXP のスコープ外。G1 合格後に別 EXP として起票する）。
- 決済は §4.3 のトレーリングストップのみ。

### 5.2 方向（必須条件⑤）

- **ベースラインはロングオンリー。** ショート（信用売り）のパイプラインは本 EXP では**組まない**（逆日歩が事前算定不能でコストモデルが成立しないため）。
- **SUE 下位10%（第1デシル）の予測単位のみ**（グロス `IC`・デシル平均リターン）は生データとして出力する。**この下位デシルの結果を根拠に G1 を合格にしてはならない。**

### 5.3 建玉・リスク集中上限（S-4の確定）

| # | 項目 | 固定値 | 根拠 |
|---|---|---|---|
| 1 | 運用資本 | 1,000,000円 | PJ000001 司令塔判断1 |
| 2 | 1銘柄あたり目標建玉 | 250,000円 | prescreen 前提 |
| 3 | 単元数の決定 | `u = max(1, round(250,000 / (終値 × 100)))`（四捨五入・半数切り上げ）。`u × 終値 × 100 > 375,000` なら `u` を1減らす。それでも 375,000 を超えるなら**見送り**（件数記録）。基準となる終値は `C(t_0)` | 単元100株の離散性 |
| 4 | 1銘柄あたり建玉上限 | 375,000円 | 目標の1.5倍 |
| 5 | **同時保有銘柄数上限** | **5銘柄** | prescreen §Kelly・破産サニティ |
| 6 | **建玉総額上限** | **1,500,000円**（実効レバレッジ1.5倍） | ギャップ −10% を同時被弾しても損失 15万円 = 資本の15.0% に収まる（DD上限15%）。建玉枠200万円をそのまま使うと資本の20%となり §8.1 の DD上限を単発突破する |
| 7 | **同一 `S33` 業種の同時建玉上限** | **2銘柄** | セクター共通要因の同時被弾抑制 |
| 8 | **同一開示日 `t0` に由来する新規建て上限** | **3銘柄** | 同日クラスタの共通要因被弾抑制 |
| 9 | 候補が上限を超える場合の優先順位 | `SUEpct` 降順 → 同値なら `Code` 昇順（決定的） | 再現性 |
| 10 | サーキットブレーカー | 日次エクイティの直近ピークからの下落率が **10%** 到達で新規建て停止（DDが10%未満に回復するまで）。**15%** 到達日に全ポジションを翌営業日始値で清算し、**その日でバックテストを打ち切る**（自動再開しない。PJ000001 §8.1 は再開に司令塔GOを要するため） | PJ000001 §8.1 |

- 見送った候補（上限に阻まれたもの）はすべて**スロット競合**として件数・内訳を記録する（必須条件④(c)）。
- 各トレードについて、**離散化後の実効建玉金額／目標建玉金額**と、**実効 f（＝建玉金額 ÷ 資本）** を毎回記録する（必須条件④(e)、PJ000001 §8.2、trading-app-v2 EXP-OBS000041 の教訓）。

### 5.4 ユニバース定義（`/equities/master` の実測フィールドの使い方）

選定期間開始日（2024-06-21）時点と確認期間開始日（2025-07-01）時点で**それぞれ1回だけ確定し、期間中は固定する**（期中リバランスをしない＝先読み防止・単純化）。

| 手順 | 条件 | 使用フィールド |
|---|---|---|
| U-1 | 規模区分が **TOPIX500 相当（Core30 / Large70 / Mid400 に相当する区分）** であること | `ScaleCat`（`/equities/master`） |
| U-2 | 信用区分が **制度信用で買建て可能**であること（「信用取引不可」に相当する区分を除外。貸借銘柄は含める） | `Mrgn` / `MrgnNm` |
| U-3 | 業種区分を記録する（セクター上限 §5.3-7 とセクター中立リターン §6.3 に使用） | `S33` / `S33Nm` |
| U-4 | 流動性: 期間開始日の**直前60営業日の日次売買代金の中央値 ≥ 5億円** | 日足 `Va` |
| U-5 | 株価帯: 期間開始日時点の終値が **1,000円以上 3,500円以下**（1単元10〜35万円。目標建玉25万円に対する離散化誤差を許容範囲に収める） | 日足 `C` |
| U-6 | U-1〜U-5 を満たす銘柄が **175を超える**場合、売買代金中央値の降順で**上位175銘柄**を採る | — |
| U-7 | U-1〜U-5 を満たす銘柄が **150未満**の場合、株価帯条件（U-5）を **700円以上 8,000円以下**に**一度だけ**緩和する。それでも150未満なら**ユニバース不成立として S に差し戻す** | — |

- **`ScaleCat` / `MrgnNm` の実際の値（コード体系・名称）は未確認である。** B実装チームは最初に全ユニーク値と件数を出力し、U-1・U-2 への対応付けを `params.json` に明記すること。**一意に決まらない場合は推測で埋めず S に差し戻す。**
- **ユニバースを TOPIX500 の外へ広げて有効 n を稼ぐことを禁止する**（trading-app-v2 OBS000030）。U-7 の緩和は株価帯のみであり、`ScaleCat` 条件の緩和は一切認めない。
- 生存バイアス防止: 期間中に上場廃止・売買停止となった銘柄は、**発生日以降のイベントのみ除外**し、遡及的にユニバースから外さない。

---

## 6. 成功 / 不採用の数値基準（回す前に確定）

### 6.0 データ十分性ゲート（条件⓪-2。リターンを見る前に評価する）

以下を**すべて**満たさない場合、G1 は「**判定不能**」とし、**Kill ではなく保留**とする。データ拡張（J-Quants 有料プランへの切替）の是非を司令塔判断に上げる。

| # | 条件 | 閾値 |
|---|---|---|
| DS-1 | 確認期間の有効イベント数（§3.3 の除外後） | **≥ 300件** |
| DS-2 | 確認期間の「1日あたり5件以上のイベントを持つ開示日」の数 | **≥ 30日** |
| DS-3 | 確認期間のユニバース銘柄数 | **≥ 150銘柄** |
| DS-4 | ユニバース×全期間の日足の欠損営業日率 | **≤ 5%** |

**この判定はイベント件数・銘柄数・欠損率のみを入力とし、リターンを一切参照せずに行う。** B実装チームは §9 の先行タスクとしてこれらのカウントを先に出力すること。

### 6.1 G1 主検定（必須条件③）

**主指標: 開示日内クロスセクション Spearman 順位相関の等加重平均（`IC_bar`）**

1. 確認期間の各開示日 `d` について、その日にイベントを持つ銘柄群の `RawSUE` と `R_5`（§4.2）の **Spearman 順位相関 `IC_d`** を算出する。タイは平均順位で処理する。
2. **1日あたりのイベント数が5件未満の開示日は `IC_bar` の算出から除外**する（除外日数・除外イベント数を記録）。
3. **`IC_bar` = 対象日の `IC_d` の単純平均**（日を等加重。イベント数の多い日に引っ張られないようにする）。
4. **有意性検定 = 開示日ブロック内 permutation**:
   - 帰無仮説: 「同一開示日の銘柄間で、`RawSUE` の順位はリターンの順位を説明しない」
   - 手続き: **同一開示日ブロックの内部でのみ `RawSUE` をシャッフル**し（ブロック間の入れ替えは行わない）、`IC_bar` を再計算する。これを **10,000回**繰り返す。
   - 乱数シード: **20260913**（固定。変更禁止）
   - 片側 p 値 = ( 1 + #{ `IC_bar_perm` ≥ `IC_bar_obs` } ) / ( 1 + 10,000 )
   - **同日ブロック内でのみ並べ替えることにより、同日発表銘柄が共有するマクロ／セクター共通要因が p 値を過小評価させる問題を構造的に排除する。**

**検出力の事前開示（MDE）**

- 1日あたり平均イベント数を `n_d`、`IC_d` の標準偏差を概ね `1/√(n_d − 1)` とみなすと、`n_d ≈ 12` で `SD(IC_d) ≈ 0.30`。
- 確認期間の対象日数 `K ≈ 60` とすると `SE(IC_bar) ≈ 0.30 / √60 ≈ 0.039`。
- 片側5%の検出可能最小効果 **MDE(IC_bar) ≈ 1.645 × 0.039 ≈ 0.064**。
- **したがって本設計は `IC_bar` が概ね 0.065 以上でなければ検出できない。これより小さい真のエッジは検出できず G1 未達（＝Kill）となる。この限界を事前に受け入れる。**
- B実装チームは**実測の `K`・平均ブロックサイズ・permutation 帰無分布の標準偏差**を出力すること（C品質チームが実測 MDE を再計算できるようにするため）。

### 6.2 G1 合格条件（すべて満たすこと）

| # | 条件 | 閾値 |
|---|---|---|
| **G1-1** | 確認期間の `IC_bar` | **> 0 かつ 開示日ブロック内 permutation の片側 p < 0.05** |
| **G1-2** | 選定期間の `IC_bar` | **> 0**（符号一致。有意性は問わない） |
| **G1-3** | 確認期間の SUE 上位デシル（第10デシル）の `R_5` **グロス平均** | **≥ +0.35%**（往復コスト前提と同額。これを下回るグロスエッジは構造上 G2 を通過し得ないため、ここで打ち切る） |
| **G1-4** | 確認期間の SUE 上位3デシル（第8〜10デシル）の **市場超過** `R_5` 平均（§6.3 の定義） | **> 0**（市場全体の上昇だけで合格することを防ぐ） |
| **G1-5** | デシル単調性（副指標。§6.3） | **デシル番号とデシル平均 `R_5` の Spearman 順位相関 `ρ_dec` ≥ +0.30** |

**G1-1 〜 G1-5 のいずれか1つでも未達なら G1 不合格とし、G2 以降は測定しない**（PJ000001 §5.3「G1 未達は即打ち切り」）。

### 6.3 デシル分析（必須条件③(b)・OBS000018 / OBS000038 の教訓）

- `SUEpct`（§3.4）で確認期間のイベントを10デシルに分け、各デシルの `R_5` グロス平均・中央値・件数・標準偏差を出力する。
- **市場超過リターンの定義**: TOPIX 日足が現行契約で取得不可（HTTP 403）のため、**ユニバース全銘柄の等加重平均リターン**（同一カレンダー窓 `t_1`〜`t_H`）を市場プロキシとする。`R_5^excess(j) = R_5(j) − (ユニバース等加重平均 R_5 over 同一窓)`。
- **セクター中立リターン**も併せて出力する: `R_5(j) − (同一 `S33` 業種のユニバース構成銘柄の等加重平均 R_5 over 同一窓)`。これは生データであり、合否判定には使わない。
- 閾値非単調性チェック（SA-3）: 上位5% / 10% / 20% / 30% の各閾値での平均リターンを出力する。**合否は上位10%（事前固定）のみで判定し、他の閾値の結果を合格根拠にしてはならない。**
- レジーム別分解（**要**）: 確認期間を四半期ごとに分けたリターン、および決算シーズン別のリターンを出力する。**部分集合の好成績を全体の合格根拠にしてはならない**（trading-app-v2 OBS000038）。

### 6.4 G2 合格条件（必須条件④: **Sharpe の統計的有意性を基準にしない**）

| # | 条件 | 閾値 |
|---|---|---|
| **G2-1** | 確認期間の**コスト控除後**平均トレードリターン（建玉金額比） | **> 0.00%** |
| **G2-2** | 確認期間の**コスト控除後**累積純リターン（資本100万円比） | **> 0.0%** |
| **G2-3** | 選定期間と確認期間で G2-1 の符号が一致 | **両期間とも > 0.00%** |
| **G2-4** | 確認期間の最大ドローダウン（資本比） | **≤ 15.0%**（PJ000001 §8.1 の運用停止水準。到達した時点で §5.3-10 によりバックテストは打ち切られる） |
| **G2-5** | 確認期間の実約定トレード数 | **≥ 30件**（これ未満ではパイプライン統計が意味をなさない） |
| **G2-6** | 確認期間の約定不能率（シグナル発生に対し §4.1 で建てられなかった割合） | **≤ 50%**（これを超える場合、シグナルの過半が制度的に取れず実装不能とみなす） |

**Sharpe の扱い**: 年率 Sharpe は**算出・報告するが、G2 の合否判定には一切用いない**。報告時は必ず 95% 信頼区間を併記すること（2年観測での年率 Sharpe 推定の標準誤差は概ね `√(1/2) ≈ 0.71` であり、信頼区間の幅は ±1.4 程度になる）。**Sharpe の再現性判定は STEP3 フォワード較正へ送る。**

> **PJ000001 §5.2 との差異（申し送り）**: PJ000001 §5.2 の STEP2 KPI は「確認期間でコスト控除後 Sharpe ≥ 0.5」としているが、本 EXP ではこれを**合否ゲートに使わない**。2年・年約50トレードの観測から Sharpe を有意に識別することが原理的に不可能であるため（prescreen §(c)）。代償として **G1 側を PJ000001 §5.3 より厳しくしている**（G1-3 のグロス下限、G1-4 の市場超過、G1-5 の単調性を追加）。この差異は C品質チームの検査対象とし、司令塔の了解事項として扱う。

### 6.5 G2 で必ず出力する生データ（必須条件④(a)〜(e)）

判定に使うか否かに関わらず、以下は**すべて `pipeline.json` に出力を必須**とする。欠けている場合、C品質チームは自動的に「差し戻し」とする。

| # | 項目 |
|---|---|
| P-1 | コスト控除後の平均トレードリターン・中央値・標準偏差・勝率・トレード数（選定/確認それぞれ） |
| P-2 | **約定不能率**（分母=上位10%シグナル件数、分子=§4.1 で `BUY_BLOCKED(t1)` が真となり建てられなかった件数）と、その内訳（`UL(t1)=1 かつ O(t1)=H(t1)` / `Vo(t1)=0` / `O(t1)` が null）。加えて参考値として `UL(t1)=1 かつ O(t1)<H(t1)` で約定したとみなした件数 |
| P-2b | **決済側の約定不能**（§4.4）: `SELL_BLOCKED` が真となった件数と内訳（`LL=1 かつ O=L` / `Vo=0` / `O` が null）、持ち越し営業日数の分布、5営業日超過による「強制評価」件数。参考値として `LL=1 かつ O>L` で約定したとみなした件数 |
| P-2c | `UL` / `LL` の全ユニーク値と件数、および null / 欠損行の件数と欠損率（§4.0.3） |
| P-3 | **スロット競合率**（分母=上位10%シグナル件数、分子=§5.3-5〜8 の上限で見送った件数）と、上限別の内訳（同時保有数 / 建玉総額 / 業種 / 同一開示日 / 単元離散化で見送り） |
| P-4 | **約定不能サンプルを除いた場合と、仮に建てられたとした場合の期待値の差**（pp表示） |
| P-5 | **離散化後の実効 f** の全トレード分布（平均・中央値・最小・最大）と、目標建玉金額比の乖離 |
| P-6 | **決済理由の内訳**（初期損切 / トレール / 最大保有10営業日の時間切れ / ストップ安持ち越し / 強制評価 / 未発表決算による強制手仕舞い / サーキットブレーカー清算） |
| P-7 | **トレール発動率**と、発動後の平均到達最高含み益 |
| P-8 | 平均保有営業日数・平均保有暦日数（金利計算の検証用） |
| P-9 | 日次エクイティ曲線、最大DD、DD10%・15%への到達有無と到達日 |
| P-10 | **日次のスリーブPnL系列**（将来 G3/G4 を再評価するために必須。§6.6） |
| P-11 | 年率 Sharpe と 95%信頼区間（**参考値。判定には使わない**） |
| P-12 | コスト感度（SA-4: 0.25% / 0.35% / 0.50%）ごとの P-1 |
| P-13 | 日次の委託保証金率（§7 E項）と、30% を下回った日数 |

### 6.6 G3（低相関）

**本 EXP では判定不能**（必須条件⑤・S-5）。スリーブ在庫がゼロであり、比較対象が存在しないため。

- 数値基準そのものは PJ000001 §5.3 のまま据え置く（既存スリーブとの |ρ| < 0.3）。
- **本 EXP では測定しない。** ただし将来の測定のため、**確認期間の日次スリーブPnL系列を `pipeline.json` に保存すること**（P-10）。
- **再評価条件**: 2本目のスリーブが C品質チームにより「採用可」と宣告された時点で、PEAD を含む合成で G3・G4 を再評価する。

### 6.7 G4（限界寄与）

**本 EXP では判定不能**（同上）。数値基準は PJ000001 §5.3 のまま据え置く（合成 Sharpe が既存構成 +0.15 以上、**または** 最大DD が既存構成 −5pt 以下）。再評価条件は §6.6 と同じ。

**G3・G4 が判定不能であるため、本 EXP は G1・G2 のみで暫定判定する。G1・G2 の合格は「暫定採用候補」であり、PJ000001 §5.3 の採否基準の半分（G3・G4）が未充足のままであることを、C品質チームの判定文および `portfolio-ledger.md` に必ず明記すること。**

### 6.8 Kill 基準（明示）

| # | 該当したら | 扱い |
|---|---|---|
| K-1 | §6.2 の G1-1〜G1-5 のいずれかが未達 | **Kill**。G2 以降は測定しない。`portfolio-ledger.md` の「不採用」に記録 |
| K-2 | 選定期間で `IC_bar` > 0 だが確認期間で消失（G1-1 未達） | **Kill**（過学習）。同上 |
| K-3 | G1 合格だが §6.4 の G2-1〜G2-6 のいずれかが未達 | **Kill**（trading-app-v2 OBS000025 型）。同上 |
| K-4 | 本 spec の基準を事後に緩めないと通らない | **Kill**（HARKing）。同上 |
| K-5 | §6.0 のデータ十分性ゲート未達 | **Kill ではなく保留**。データ拡張の是非を司令塔判断に上げる |
| K-6 | §3.3-4 / §5.4 の値の対応付けが確定できない、または §9-2 の欠損率が5%超 | **実験を回さず S に差し戻し**（結果を作らない） |

---

## 7. 日本株固有の模擬条件（S-7・バックテストに必ず含める）

| # | 項目 | 実装ルール（数値で固定） | 状態 |
|---|---|---|---|
| **A** | **単元100株の離散性** | §5.3-3 の単元数決定ルール。**実効ポジションサイズと目標の乖離、実効 f を毎トレード記録**（P-5） | [ ] 実装必須 |
| **B** | **値幅制限（ストップ高/安）** | 日足の `UL` / `LL` は**0/1 のフラグ**であり価格水準ではない（§4.0.1）。判定は **§4.0.4 の2式のみ**で行う。<br>エントリー: `BUY_BLOCKED(t1) = (Vo(t1)==0) or (O(t1) is null) or (UL(t1)==1 and O(t1)==H(t1))` → 約定不能・イベント見送り（§4.1）。<br>決済: `SELL_BLOCKED(t_exit) = (Vo(t_exit)==0) or (O(t_exit) is null) or (LL(t_exit)==1 and O(t_exit)==L(t_exit))` → 持ち越し、最大5営業日（§4.4）。<br>**損切注文が約定しないケースを必ず模擬する**（§4.4 はトレール・初期損切・最大保有超過・決算跨ぎ・サーキットブレーカーの全売り約定に適用）。<br>JPX 制限値幅表からの上限/下限価格の自前再構成は**禁止**（§4.0.4） | [ ] 実装必須 |
| **C** | **取引時間と約定タイミングの固定** | 前場 9:00–11:30 / 後場 12:30–15:30（2024-11-05 以降。それ以前の大引けは 15:00）。**本 EXP は日足完結であり、`O` = 寄り付き、`C` = 大引けとのみ対応づける。** `DiscTime` は `params.json` に記録するが、エントリー判定には使わない（§4.1） | [ ] 実装必須 |
| **D** | **決算跨ぎ制約の読み替え（S-3）** | PJ000001 §8.1 / H-3 の「決算発表予定日を跨ぐポジションを持たない」を、本 EXP では **「未発表の決算発表予定日を跨ぐポジションを持たない」と読み替える**（字義通りでは発表当日のポジションも持てず PEAD が成立しないため）。<br>実装: `/equities/earnings-calendar` から保有銘柄の**次回**決算発表予定日を取得し、**予定日の前営業日の引け時点で手仕舞いを決定し、予定日当日の始値で約定**とする。PEAD の保有は最大10営業日、次の四半期決算は約3ヶ月先であるため通常は抵触しないが、**抵触件数を必ず記録する**。<br>保有期間中の**業績修正・その他適時開示**は本 EXP のスコープ外とし、**ルール上の手仕舞いは行わない。発生件数のみ記録する** | [ ] 実装必須 |
| **E** | **委託保証金率・最低委託保証金30万円** | 制度信用・委託保証金率30%。建玉総額上限150万円に対し必要保証金45万円（資本100万円で充足）。**日次で保証金率を計算し、30%を下回った日数を記録**（P-13）。最低委託保証金30万円を下回る日があれば記録 | [ ] 実装必須 |
| **F** | **信用規制（日々公表銘柄・増担保規制）** | **模擬不能**。該当データが現行契約で取得できない（C-5・HTTP 403）。**「模擬していない」ことを `params.json` に明記し、測定範囲の空白として記録する。** 増担保規制がかかると委託保証金率が引き上げられ実効レバレッジが低下するため、**本バックテストは楽観側に偏っている**ことを申し送る | [ ] 明記必須 |
| **G** | **制度信用 買方金利** | 年率 **2.8%** × **実保有暦日数** ÷ 365（固定値0.08%ではなく、保有日数に比例させる） | [ ] 実装必須 |
| **H** | **貸株料・逆日歩・空売り規制** | **ロングオンリーのため適用なし**（§5.2）。ショートアームは本 EXP のスコープ外 | [x] 該当なし |

---

## 8. コスト前提

### 8.1 適用するコストモデル（`cost-model.json` に出力）

| 項目 | 片道 | 往復 | 出典 |
|---|---|---|---|
| スプレッド（呼値1ティック相当） | 0.025% | **0.05%** | **仮置き**（R-2 未確認） |
| スリッページ | 0.075% | **0.15%** | **仮置き**（決算翌日の寄り付きを想定） |
| 委託手数料 | 0.025% | **0.05%** | **仮置き**（R-3 未確認・司令塔確認待ち） |
| 制度信用 買方金利 | — | 年率2.8% × 実保有暦日数 ÷ 365（平均10暦日で **約0.08%**） | **仮置き**（R-3 未確認） |
| 貸株料・逆日歩 | — | 適用なし（ロングオンリー） | — |
| **往復コスト合計（平均10暦日保有時）** | — | **0.35%** | 上記の合計 |

- スリッページはエントリー・決済の約定価格に直接織り込む（§4.1・§4.3）。スプレッド・手数料・金利はトレード損益から控除する。二重計上しないこと。

### 8.2 感度分析（必須出力・合否判定には使わない）

往復コスト **0.25% / 0.35%（主） / 0.50%** の3水準で §6.5 P-1 を出力する（SA-4）。**0.25% の結果が良いことを理由に合格にしてはならない。**

### 8.3 損益分岐勝率の再計算（S-8）

§4.3 で固定したトレール4パラメータ（初期損切 −3.0% / 発動 +4.0% / 追随 −2.0% / 日足終値判定・翌営業日始値約定）と往復コスト0.35%に基づく再計算。**翌営業日始値約定による約定ギャップを保守側に 0.30% 見込む。**

| ケース | 名目 | コスト後 |
|---|---|---|
| 損失側（トレール未発動で初期損切） | −3.0% | −3.0 − 0.35 − 0.30 = **−3.65%** |
| 利益側の**最小**（発動直後に追随幅まで戻す） | +4.0% − 2.0% = +2.0% | +2.0 − 0.35 − 0.30 = **+1.35%** |

2値近似での損益分岐勝率（利益側の平均値を振る）:

| 利益側の名目平均 | 利益側コスト後 | 損失側コスト後 | 損益分岐勝率 |
|---|---|---|---|
| +2.0%（最小・発動直後戻し） | +1.35% | −3.65% | **73.0%** |
| +4.0% | +3.35% | −3.65% | **52.1%** |
| +6.0% | +5.35% | −3.65% | **40.6%** |
| +8.0% | +7.35% | −3.65% | **33.2%** |

> **警告（設計上の厳しさを正直に記す）**: 本パラメータでは、**トレール発動後すぐに戻された場合の利益が +1.35%(コスト後) しかなく、損失側の −3.65% に対して RR が 0.37 しかない。** この設計が成立するには、トレール発動後に平均して **+4%以上**まで伸びる必要がある（分岐勝率 52.1% 相当）か、勝率が大きく50%を超える必要がある。PEAD のドリフトは3〜10営業日で数%オーダーであり、これは**厳しい**。
> **この厳しさを理由にパラメータを事後に変更することを禁止する。** B実装チームは §6.5 P-6（決済理由の内訳）・P-7（トレール発動率と発動後の平均到達最高含み益）を必ず出力し、C品質チームはこれらを使って「パラメータが構造的に不適合だったのか、エッジが不在だったのか」を切り分けること。
> なお時間切れ決済（最大保有10営業日）は上記2値近似に含まれない第3の帰結であり、実際の分岐勝率は上表とずれる。上表は**目安**であり、実測は P-1・P-6 で確認する。

---

## 9. B実装チームが「回す前」に先に潰すタスク

以下は**検証の実行より前**に実施し、結果を `10-result/` に出力すること。いずれかが不成立なら、**実験を回さずに S へ差し戻す**。

| # | タスク | 合格条件 | 不成立時 |
|---|---|---|---|
| **9-1** | `/equities/master` の `ScaleCat` / `Mrgn` / `MrgnNm` / `S33` / `S33Nm`、`/fins/summary` の `DocType` / `CurPerType` の**全ユニーク値と件数**を出力し、§3.3-3・§5.4 U-1・U-2 への対応付けを `params.json` に明記する | 対応付けが**一意に決まる** | **S に差し戻す**（推測で埋めない） |
| **9-1b** | `/equities/bars/daily` の **`UL` / `LL` の全ユニーク値と件数**、および null / 欠損行の件数・欠損率を出力する（§4.0.3） | ユニーク値が `"1"` / `"0"` / `""` / null のみに収まる **かつ** 欠損率 **≤ 1%** | **S に差し戻す**（`"1"`/`"0"`/空/null 以外の値を推測で解釈しない） |
| **9-2** | **R-1d の完全解消**: ユニバース候補全銘柄 × 契約全期間（2024-06-21〜2026-06-21）の日足を取得し、**欠損営業日率**を算出・出力する（HTTP 429 に対しては指数バックオフでリトライし、429 起因の欠測を真の欠測と混同しないこと） | 欠損営業日率 **≤ 5%** | **S に差し戻す** |
| **9-3** | **R-6 の実測**: 決算発表の集中度（決算シーズン数、シーズンあたりの開示営業日数、1日あたりイベント数の分布、3月決算企業の比率）を出力する | — （記録のみ） | — |
| **9-4** | **§6.0 データ十分性ゲート（DS-1〜DS-4）の評価**。**リターンを一切参照せずに**カウントのみで評価する | DS-1〜DS-4 をすべて充足 | **保留として S / 司令塔に上げる**（K-5） |
| **9-5** | `RawSUE` の記述統計（件数、厳密ゼロの比率、分位点、除外理由別の除外件数）を出力する | — （記録のみ） | — |

---

## 10. B実装チームへの指示（禁止事項）

### 10.1 共通の禁止事項

- 判定語（「良好」「有望」「改善」「期待できる」等）を書かない。**生データのみ**を出力する
- パラメータ・閾値・分割・seed をチューニングしない
- 成功基準を事後に緩めない
- 成果物の実在を確認してから完了報告する

### 10.2 本 EXP 固有の禁止事項

| # | 禁止事項 | 出典 |
|---|---|---|
| **N-1** | **②テクニカルシグナルとの合成を行わない。** MACD / RSI / ボリンジャーバンド / CCI / 移動平均 / ADX / ATR / Stochastic 等をエントリー・エグジット・フィルタ・サイジングに一切使わない | 必須条件① |
| **N-2** | **SUE の計算・正規化に価格由来の変数を使わない。** 発表日の異常リターン、出来高サプライズ、株価ボラティリティ、モメンタム、時価総額（＝価格×株数）を SUE の分子・分母・標準化のいずれにも使わない | 必須条件② |
| **N-3** | **決算短信テキストの LLM スコア化を行わない**（③LLM単独スリーブとして別 EXP） | 必須条件② |
| **N-4** | **ユニバースを TOPIX500 相当（`ScaleCat` 条件）の外へ広げない。** 有効 n が不足しても銘柄数で救おうとしない | trading-app-v2 OBS000030 |
| **N-5** | **部分集合（特定デシル・特定業種・特定シーズン・特定コスト水準）の好結果を全体の合格根拠にしない。** SA-1〜SA-4 は記述統計であり、検定として扱わない | trading-app-v2 OBS000018 / OBS000038 |
| **N-6** | **spec の定義に矛盾・欠落・曖昧さを見つけたら、自分で「安全側だろう」と解釈して埋めず、即座に S に差し戻す。** 特に窓長・ラグ・オフセット（§4.5）、値の対応付け（§9-1）、**API フィールドの型・意味（§4.0.3 の `UL`/`LL`、§9-1b）**について。<br>**本 spec は 2026-09-13 にこの経路で実際に1回差し戻され改訂されている**（`UL`/`LL` が価格水準ではなくフラグであった件。§4.0）。差し戻しは正しい動作であり、ペナルティではない | trading-app-v2 EXP-OBS000037 / 本 EXP の §4.0 改訂 |
| **N-7** | **選定期間の結果を見てから spec の値を変更しない**（§2.2） |  |
| **N-8** | **G1 が未達の場合、G2 を測定しない**（§6.8 K-1） | PJ000001 §5.3 |

### 10.3 許可される価格データの利用範囲（N-2 との混同を避けるための明示）

以下は**シグナルではなく取引可能性の制約・損益計算**であるため、価格データの使用を**明示的に許可**する。

- ユニバースの流動性フィルタ（売買代金 `Va` の中央値。§5.4 U-4）
- ユニバースの株価帯フィルタと単元数計算（`C`。§5.4 U-5、§5.3-3）
- 値幅制限による**約定可否**の判定（`UL` / `LL` フラグ、および同一日の `O` / `H` / `L` / `Vo`。§4.0.4・§7 B項）。**これは方向予測の入力ではなく取引可能性の制約であり、N-2 に抵触しない**
- 前方リターン・損益の計算（`O` / `C`。§4.2・§4.3）
- 市場プロキシ・セクター中立リターンの算出（§6.3）

**これらは予測変数（SUE）の生成には一切入らない。** 逆に、これら以外の用途で価格データを使うことは N-2 違反である。

---

## 11. 使用スクリプト / 再現方法（想定）

| スクリプト | 役割 | 出力先 |
|---|---|---|
| `scripts/pead_fetch_data.py` | J-Quants からユニバース候補の日足・財務・決算発表予定・銘柄マスタを取得しキャッシュ（429 は指数バックオフでリトライ） | `data/raw/pead/` |
| `scripts/pead_build_universe.py` | §5.4 のユニバース確定＋§9-1 の値ユニーク一覧出力 | `10-result/universe.json` / `10-result/params.json` |
| `scripts/pead_feasibility.py` | §9-2〜§9-5（欠損率・集中度・データ十分性ゲート・SUE記述統計） | `10-result/feasibility.json` |
| `scripts/pead_prediction_unit.py` | §6.1〜§6.3（`IC_bar`・permutation・デシル・感度） | `10-result/prediction-unit.json` |
| `scripts/pead_pipeline.py` | §4〜§5・§6.4〜§6.5（トレール・約定模擬・生データ全項目） | `10-result/pipeline.json` / `cost-model.json` |

実行コマンド想定:

```bash
python scripts/pead_fetch_data.py      --config research/EXP-OBS000001/01-spec.md --out data/raw/pead/
python scripts/pead_build_universe.py  --out research/EXP-OBS000001/10-result/
python scripts/pead_feasibility.py     --out research/EXP-OBS000001/10-result/
python scripts/pead_prediction_unit.py --seed 20260913 --out research/EXP-OBS000001/10-result/
python scripts/pead_pipeline.py        --seed 20260913 --out research/EXP-OBS000001/10-result/
```

- **決定性**: 同じコマンドで同じ結果が出ること。乱数を使うのは permutation のみであり、シードは **20260913** に固定する。
- `run.log` に**再現用の実行コマンドを必ず含める**こと（`_templates/result.readme.md` 準拠）。
- 成果物: `prediction-unit.json` / `pipeline.json` / `params.json` / `cost-model.json` / `run.log`（＋本 spec 固有の `universe.json` / `feasibility.json`）。**`prediction-unit.json` と `pipeline.json` の両方が揃っていない場合、C品質チームは自動的に「差し戻し」とする**（PJ000002 §6）。

---

## 12. 本 spec が測定しないこと（申し送り）

| # | 未測定項目 | 理由 | 送り先 |
|---|---|---|---|
| 1 | G3（低相関）・G4（限界寄与） | スリーブ在庫がゼロで比較対象が存在しない | 2本目のスリーブ採用時に再評価（§6.6） |
| 2 | 確認期間のテール局面での挙動 | A案分割では2024年8月急落が選定側に落ちる（§2.3） | STEP3 フォワード較正 |
| 3 | Sharpe の再現性 | 2年ではSharpeを有意に識別できない（SE≈0.71） | STEP3 フォワード較正（§6.4） |
| 4 | 信用規制（日々公表・増担保）の影響 | 現行契約でデータ取得不可（C-5・403） | 有料化判断時に再検討（§7 F項） |
| 5 | ショート（SUE下位デシル）のパイプライン | 逆日歩が事前算定不能でコストモデルが成立しない | 別アーム／別 EXP（§5.2） |
| 6 | ②テクニカルシグナルとの合成 | 本 EXP のスコープ外（必須条件①） | G1 合格後に別 EXP として起票 |
| 7 | 決算短信テキストの LLM スコア化 | 本 EXP のスコープ外（必須条件②） | ③LLM単独スリーブとして別 EXP |
| 8 | 日本株 PEAD の学術的エビデンス（R-5） | 一次文献を直接確認できていない | 事前確率の根拠としては使わない（§1.2） |

---

## 変更履歴

- 2026-09-13（第2版）: **B実装チーム（quant-researcher）からの差し戻しを受けた改訂（N-6 の正常動作）。** 初版は値幅制限による約定不能を `O(t1) ≥ UL(t1)` / `O(t_exit) ≤ LL(t_exit)` という**価格水準としての数値比較**で定義していたが、B実装チームが `/equities/bars/daily` を実測した結果、**`UL` / `LL` は価格水準ではなく 0/1 の文字列フラグ**であり、日足スキーマ（`Date,Code,O,H,L,C,UL,LL,Vo,Va,AdjFactor,Adj*,MktCap,ExRT`）に**その日の値幅制限の価格水準を表すフィールドが存在しない**ことが判明した。初版の定義は実在フィールドでは**計算不能**であった。
  - 実測2サンプル（`130A0` / 2024-08-05 の `L=637, LL="1"`、および `O=738, H=748, C=741, UL="1"`）を JPX 制限値幅表と突き合わせた結果、基準値段がそれぞれ 787円・648円と**一意に定まり整合**したため、フラグの意味を「**`UL=1` ⇔ 日中高値が上限価格に達した / `LL=1` ⇔ 日中安値が下限価格に達した**」と確定した（§4.0.1）。`UL="1"` サンプルで `C≠H` であることから、「終値が制限に張り付いた」という解釈は棄却した。
  - 判定条件を `BUY_BLOCKED(t) = (Vo==0) or (O is null) or (UL==1 and O==H)` / `SELL_BLOCKED(t) = (Vo==0) or (O is null) or (LL==1 and O==L)` に置き換えた（§4.0.4）。これは初版の意図との**論理的同値変形であり、約定モデルの厳しさを変更していない**（§4.0.2 に証明を明記）。
  - 併せて、パース規則（`"1"`/`"0"`/空/null 以外の値は推測で埋めず差し戻し）・欠損率上限1%・同値比較の数値規約を一意に確定し（§4.0.3）、先行タスク §9-1b、生データ P-2 / P-2b / P-2c、§7 B項、§10.3、N-6 を更新した。
  - この改訂は**約定可否の制約**に関するものであり、予測変数 `SUE` の生成には一切入らない。必須条件② / N-2 に抵触しない（§10.3 で許可された用途の範囲内）。
  - 改訂範囲: §0 反映表（S-2 行）／**§4.0 新設**／§4.1／§4.4／§6.5（P-2 改訂・P-2b・P-2c 追加）／§7 B項／§9-1b 追加／§10.3／§10.2 N-6／本変更履歴。**成功/不採用の数値基準（§6.2 G1-1〜G1-5、§6.4 G2-1〜G2-6）は一切変更していない。**
- 2026-09-13: 初版作成。S戦略チーム（chief-strategist・A設計官兼務）が `00-prescreen.md` の条件付きGO（必須条件①〜⑤・条件⓪・引き継ぎ論点S-1〜S-8）を受けて起票。SUE定義・約定モデル・トレール4パラメータ・ユニバース・リスク集中上限・G1/G2 の数値基準を**回す前に固定**した。G3/G4 は判定不能として申し送り。

## 関連ドキュメント

- [00-prescreen.md](./00-prescreen.md) — 事前目利き（条件付きGO）
- [PJ000001](../../obs/smart_wave_rider/00プロジェクト方針/PJ000001-日本株スイングトレードAI自動売買エンジン.md) — §4 C-1〜C-5 / §5.3 採否基準 / §8 リスク管理
- [PJ000002](../../obs/smart_wave_rider/00プロジェクト方針/PJ000002-マルチエージェント運営計画書.md) — 役割境界・差し戻し条件
- [portfolio-ledger.md](../portfolio-ledger.md) — 姉妹PJから輸入した負の資産・実装プロセス上の教訓
- [STRATEGY-BRIEF.md](../STRATEGY-BRIEF.md) — 戦略的現在地
- [_templates/result.readme.md](../_templates/result.readme.md) — B実装チームの出力様式
