# 事前目利き（Prescreen） - EXP-OBS000006（非決算オーバーナイト・ギャップ反転／10年データ版）

> 担当: S戦略チーム（chief-strategist）
> 位置づけ: **EXP-OBS000003（第3版・frozen）の後継として、10年分データにもとづき選定/確認分割を最初から設計し直す新規EXP。**
> **frozen spec の延伸ではない**（N-9）。旧EXPの基準日・区切り方は一切引き継がない。

## 変更履歴

- 2026-09-15: **§A（事前登録）のみを先に作成し、この時点でコミットした。** 実測の前に分割規則・閾値・判定規則を凍結するため（HARKing 防止・PJ000002 §6）。
- 2026-09-15: §B（実測）・§C（判定）を追記。

---

# §A 事前登録（**実測の前に凍結する。この節はコミット後に変更しない**）

> **この節は、`data/raw/` の10年分データに対して件数の測定を1回も行っていない時点で書き、単独でコミットした。**
> §B 以降の実測結果を見てから §A の数値・規則を変更することを**禁止**する。変更が必要と判断した場合は、本EXPを破棄して別のEXP番号で最初から起票し直す。

## A-0. なぜ新規EXPなのか、そして**なぜ今回は再挑戦する価値があるのか**

EXP-OBS000003 は §6.0 のデータ十分性ゲートで停止した（DS-2a / DS-5 / DS-6 / DS-7 の4つが不合格・K-5 保留）。S は第3版 §6.0.1(g) で「**本 spec の枠内で §6.0 を充足させる再設計は存在しない**」と結論している。その決定的根拠は §6.0.1(a):

> DS-6 の計数対象はイベント集合 `E` ではなく**プール `P`**（`z ≤ −1.5`）であり、`z*` は `P` の定義に一切入らない。**したがって `z*` の較正をどう引き直しても実測 62日は動かない。**

**この論証は「契約データ期間を固定したまま」という条件下でのみ成立する。** DS-6 は「プールの日次ブロックのうち5件以上を持つ日数」であり、**期間の長さに概ね比例して増える量**である。10年データ（約2,440営業日）は2年データ（487営業日）の約5倍であり、**`z*` を一切動かさずとも DS-6 の母数が5倍になる。** これが本EXPを再挑戦に値するものにしている唯一かつ十分な理由である。

> **重要**: 本EXPは「基準を緩めて通す」のではない。**閾値は1つも動かさず、母数だけを増やす。** 旧EXPが停止した4ゲート（DS-2a / DS-5 / DS-6 / DS-7）のうち、DS-5・DS-6・DS-7 は期間の長さで直接改善しうる量であり、DS-2a は較正グリッドの範囲不足という**選定期間のみの情報から診断済みの設計不備**である（旧 spec §6.0.1(b)）。

### A-0.1 N-9 の適用（frozen spec を延伸しない）

1. 旧 spec の確認期間の区切り（2025-07-01）は、PEAD（EXP-OBS000001）が契約2年から逆算して決めた分割点をそのまま流用したものであり、10年データに対する合理的な分割ではない。
2. 旧 spec は DS ゲートで停止しており、**その実測値（DS-5=25日・DS-6=62日・DS-7=52.83%）を知った後で期間の区切りを動かすことは K-4 と外形上区別できない**。
3. したがって**新規EXP番号で、分割規則を先に凍結してから**測定する。

**本EXPは EXP-OBS000003 の仮説・シグナル定義・約定モデル・成功基準をそのまま継承し、変更するのは以下の3点のみである。**

| # | 変更点 | 性質 |
|---|---|---|
| 1 | 参照データ範囲（2年 → 10年） | データ拡張（司令塔決裁 D-9・`research/ACTIVE.md` 0-23） |
| 2 | 選定/確認の分割規則 | 新規EXPとして先に凍結（A-2） |
| 3 | `z*` 較正グリッドの**範囲**の拡張（`−4.00` 止まり → `−8.00` まで） | **閾値の緩和ではない。**「グリッドが実測のテールの厚さに届いていなかった」という**選定期間のみの情報から診断済みの設計不備**（旧 spec §6.0.1(b)）の修復。較正の目標値 215件/年・選択規則・DS-2a の帯 `[150, 300]`・DS-2b の帯 `[100, 400]` は**すべて不変** |

## A-1. 参照営業日カレンダー `T`（本EXPで唯一の正本）

- `T` := 候補銘柄の `/equities/bars/daily` の**観測日付の和集合**を昇順に並べた列。**1始まりの添字**。
- 範囲: **2016-09-15 〜 2026-09-14**（J-Quants Standard プランの契約可能期間）。
- `N := |T|`。
- **`T` は日付のリストであり、価格を一切参照しない。**
- 定義は `research/EXP-OBS000001/01-spec.md` §2.1.0 / `research/EXP-OBS000003/01-spec.md` §2.0 と同一。

## A-2. 分割規則（**測定の前に凍結。候補を複数試して選ぶことはしない**）

**登録する分割規則は以下の1つだけであり、EXP-OBS000005（PEAD・10年版）と<u>同一</u>である。**

```
m := floor(N / 2)
```

| 区分 | ユニバース確定日 `D_u` | U-4 窓（`D_u` を含まない直前60営業日） | イベント日 `D` の範囲 |
|---|---|---|---|
| **選定（参照）** | `D_sel = T[61]` | `T[1]` 〜 `T[60]`（**ちょうど60本・`Va` は銘柄あたり60個**） | **`T[68]` 〜 `T[m − 11]`** |
| **確認（未見）** | `D_conf = T[m + 1]` | `T[m − 59]` 〜 `T[m]`（**ちょうど60本・`Va` は銘柄あたり60個**） | **`T[m + 1]` 〜 `T[N − 11]`** |

### A-2.1 なぜ `floor(N/2)`（時系列50/50）なのか（**実測を一切見ずに言える理由のみ**）

| # | 理由 |
|---|---|
| 1 | **自由度がゼロである。** 分割点は `N` のみから機械的に決まる従属変数であり、こちらが選んだ値ではない |
| 2 | **確認期間を最大化する方向に働く。** 本EXPが座礁した DS-5（相異なるイベント日数）・DS-6（プール日次ブロック数）は**いずれも確認期間の長さに概ね比例する量**であり、確認期間を短くする分割は死因を温存する |
| 3 | **選定期間の自由度は `z*` の1個だけ**（旧 spec §2.3）であり、それも**カウントのみを入力とする決定的手続き**（§3.6）である。選定期間に5年あれば較正には十分すぎる |
| 4 | **時系列順（古い側を選定・新しい側を確認）にする。** 確認期間は本番運用に時間的に最も近い区間であるべき |
| 5 | **PEAD（EXP-OBS000005）と同一の分割規則を使う。** 2本のEXPで異なる分割を使うと、どちらかが「都合の良い分割を選んだ」という疑いを構造的に免れない。同一規則にすることでその疑いを消す |

### A-2.2 イベント日の開始添字が `68` である算術的根拠（**T-8・trading-app-v2 EXP-OBS000037 の教訓**）

旧 spec §2.1 の論証をそのまま引き継ぐ。

- `σ_gap(j, D)`（`D = T[i]`）は、**`T[i−66]` 〜 `T[i−1]` の範囲にある有効ギャップのうち新しい順にちょうど60本**を採る（旧 spec §3.2）。
- その範囲で最も古いギャップ `g(j, T[i−66]) = ln( O(j,T[i−66]) / C(j,T[i−67]) )` は**終値 `C(j, T[i−67])` を要する**。
- ゆえに `i − 67 ≥ 1`、すなわち **`i ≥ 68`**。
- 同時に `D` はユニバース確定日 `T[61]` より後でなければならない（`68 > 61` で充足）。
- **⇒ 両条件を同時に満たす最小の添字が 68。**

### A-2.3 前方窓の算術的整合性（**本数と対象範囲の対応を明記する**）

イベント日を `D = T[i]` とする。

| 用語 | 営業日の本数 | 対象となる `T` の添字 | 必要な価格の個数 |
|---|---|---|---|
| ギャップ `g(j,D)` | 1本のオーバーナイト窓 | `T[i−1]` の終値 と `T[i]` の始値 | **価格2個**（`C(T[i−1])` と `O(T[i])`） |
| `σ_gap` の窓 | **有効ギャップちょうど60本** | `T[i−66]` 〜 `T[i−1]` の範囲（**遡り上限66営業日**） | ギャップ60本 ⇒ 参照する終値/始値は最大で `T[i−67]` まで |
| エントリー日 `t_1` | 1本 | `T[i+1]` | 始値 `O(T[i+1])` 1個 |
| 予測単位 `R_5`（H=5営業日） | **5本のホライズン** | `t_1 = T[i+1]` 〜 `t_5 = T[i+5]` | **`R_5 = C(T[i+5]) / O(T[i+1]) − 1`。始値1個＋終値1個＝価格2個**（隣接差分の連鎖ではないため「H+1個」ではない） |
| 最大保有（5営業日）＋時間切れ決済 | 5本の保有＋決済1本 | `t_1 = T[i+1]` 〜 `t_5 = T[i+5]`、決済は `t_6 = T[i+6]` の**始値** | 添字は `i+6` まで |
| `SELL_BLOCKED` による持ち越し上限 | **＋5本** | 最悪で `T[i+11]` | 添字は `i+11` まで |
| **⇒ イベントに必要な添字の範囲** | — | **`i − 67` 〜 `i + 11`** | — |

- したがってイベント日の添字 `i` は **`68 ≤ i ≤ N − 11`**（確認期間は `m+1 ≤ i ≤ N − 11`、選定期間は `68 ≤ i ≤ m − 11`）を満たす。
- **選定期間側にも `−11` の余白を置く理由**: 余白が無いと選定期間末尾のイベントの前方窓が確認期間の先頭に食い込み、「選定期間の測定」が確認期間の価格を観測してしまう。旧 spec（選定末 2025-06-30／確認始 2025-07-01）はこの重なりを持っていた。**本EXPは構造的に排除する。基準の緩和ではなく分離の強化である。**
- **イベント集合は G1・G2 で共通に定義する。** 上記の添字範囲は G1（`i+5` まで）と G2（`i+11` まで）の**両方**を満たすよう `i ≤ N−11` で統一しており、「測定ごとに異なるイベント集合を使う」ことを構造的に排除する。
- `m`・`N`・各実日付は `T` から機械的に求め、`params.json` に出力する。**推定日付をハードコードしてはならない。**

## A-3. 継承する定義（**1つも変更しない**）

以下は `research/EXP-OBS000003/01-spec.md`（第3版）から**そのまま引き継ぐ**。

| 項目 | 継承元 | 値 |
|---|---|---|
| 仮説・賭けの対象 | §1 / §1.3 | 市場ストレス時の売り需給の偏りが数営業日かけて解消する。ロングオンリー・日足 |
| 価格の調整規約・権利落ち/配当落ち識別 | §3.1 / §3.1.1 | そのまま（V-1〜V-5 の検証ゲートを含む） |
| シグナル定義 | §3.2 | `g = ln(O(T[i])/C(T[i−1]))`、`σ_gap` = 有効ギャップちょうど60本（遡り上限66）、`z = g/σ_gap`、`S = −z` |
| ホライズン・保有 | §3.3 | H = 5営業日（主）、最大保有5営業日 |
| イベント候補・除外規則 | §3.4 | C-1〜C-4、E-1 / E-2A / E-2B / E-3（`DiscDate ∈ [T[i−2], T[i+2]]`）/ E-4 / E-5 / E-6 |
| PEAD との排反性 | §3.5 | そのまま |
| `z*` 較正の**目標値・選択規則** | §3.6 | 目標 **215件/年**、`|N_ann(z*) − 215|` を最小化、同値はより極端側、年換算定数 **245**。**グリッドの範囲のみ A-4 で拡張する** |
| 約定モデル | §4.0〜§4.5 | `t_1` 始値エントリー、`BUY_BLOCKED`/`SELL_BLOCKED`、E-7 / E-8、トレール4パラメータ、持ち越し上限5営業日 |
| ユニバース | §5.4（= PEAD §5.4） | U-1〜U-7（窓60営業日・5億円・1,000〜3,500円・上限175・下限150・緩和700〜8,000円） |
| G1 合格条件 | §6.2 | G1-1〜G1-7（**G1-7 の単日依存性除去を含む**） |
| G2 合格条件 | §6.4 | G2-1〜G2-7（**G2-7 の往復0.60%ケースを含む**） |
| コスト前提 | §8 | 往復 0.45%（主）、感度 0.35% / 0.60%。買方金利 年2.8% |
| 日本株固有の模擬条件 | §7 A〜H | そのまま |
| Kill 基準 | §6.8 | K-1〜K-6 |

## A-4. `z*` 較正グリッドの範囲拡張（**閾値の緩和ではない**）

| 項目 | 旧 EXP-OBS000003 | 本EXP | 性質 |
|---|---|---|---|
| グリッド | `{−1.50, −1.75, …, −4.00}`（0.25刻み・**11点**） | **`{−1.50, −1.75, …, −8.00}`（0.25刻み・27点）** | **範囲の拡張のみ。刻み幅 0.25 は不変** |
| 目標年換算件数 | 215件/年 | **215件/年（不変）** | — |
| 選択規則 | `|N_ann(z*) − 215|` 最小、同値はより極端側 | **不変** | — |
| 年換算定数 | 245営業日/年 | **不変** | — |
| DS-2a の帯 | `[150, 300]` | **不変** | — |
| DS-2b の帯 | `[100, 400]` | **不変** | — |

**拡張の根拠（確認期間を見ずに言える）**: 旧 spec §6.0.1(b) が**選定期間のみの実測**として記録したとおり、`z` の p01 は **−6.235**（選定期間）であり、標準正規を下半分に切断した理論分位点（p01 ≈ −2.58）から大きく外れている。**グリッド上限を `−4.00` に置いた設計は、テールが概ね正規に近いという暗黙の前提に立っており、その前提は選定期間の測定だけで既に否定されている。** 本EXPは新しい選定期間を持つが、日本株のギャップ分布がファットテールであるという性質は期間に依らない。**したがって範囲の拡張は、確認期間の情報を用いずに正当化できる。**

> **なぜこれが HARKing でないか**: (i) 拡張の根拠は**確認期間の件数分布ではなく、frozen 済みの旧 spec に記録された選定期間の分位点**である。(ii) 選択規則が `|N_ann − 215|` の最小化である以上、グリッドを細かく/広くすることは**目標 215 により近づける方向にしか働かず、「良い結果が出る点を選ぶ」余地を作らない**（`N_ann` は `z*` について単調）。(iii) **DS-2a / DS-2b の帯そのものは1ミリも動かしていない。**

### A-4.1 `N_ann` の分母の一意な定義（**旧 spec §3.6 の曖昧さを修復する**）

旧 spec は `N_ann = 件数 × 245 ÷ (選定期間のイベント日数)` と書いたが、「選定期間のイベント日数」が3通りに読めた（旧 spec §3.6 第3版注記）。**本EXPでは `T` の添字から一意に再計算できる式として定義する。**

```
n_sel_eff := #{ i | 68 ≤ i ≤ m − 11 かつ T[i] が E-2B の一括除外日でない }
N_ann(z*) := (選定期間の候補イベント件数) × 245 ÷ n_sel_eff
```

- **E-2B** = 各暦月の最終営業日の1営業日前 `X(m)` を全銘柄共通で除外する規則（旧 spec §3.1.1）。`X(m)` は `T` と暦のみから決まり、価格を参照しない。
- 確認期間側の `N_ann` も同様に `n_conf_eff := #{ i | m+1 ≤ i ≤ N − 11 かつ T[i] が E-2B の除外日でない }` を分母とする。
- **B実装チームは `n_sel_eff` / `n_conf_eff` を必ず `params.json` に出力すること。**

## A-5. データ十分性ゲート（**閾値は既存値をそのまま再利用する。緩めも厳しくもしない**）

**リターンを一切参照せず、件数・銘柄数・日数・欠損率のみを入力として評価する。**

| # | 条件 | 閾値 | 出典（**新設した閾値は C7-3 を除いて無い**） |
|---|---|---|---|
| **DS-1** | 確認期間の**プール**観測数（`z ≤ −1.5`・全除外後） | **≥ 1,500件** | EXP-OBS000003 §6.0 DS-1 と同一 |
| **DS-2a** | 選定期間で `N_ann(z*) ∈ [150, 300]` を満たすグリッド点が**存在する** | 存在すること | 同 DS-2a と同一 |
| **DS-2b** | 確認期間の候補イベント年換算件数 `N_ann` | **100 ≤ N_ann ≤ 400** | 同 DS-2b と同一 |
| **DS-3** | 確認期間のユニバース銘柄数 | **≥ 150銘柄** | 同 DS-3 と同一 |
| **DS-4** | ユニバース × 全期間の日足の欠損営業日率 | **≤ 5%** | 同 DS-4 と同一 |
| **DS-5** | 確認期間の候補イベントが属する**相異なるイベント日数** `K_event` | **≥ 80日** | 同 DS-5 と同一 |
| **DS-6** | 確認期間の**プール**日次ブロックのうち **1日5件以上**を持つ日数 `K` | **≥ 100日** | 同 DS-6 と同一 |
| **DS-7** | 確認期間のイベント総件数に対する**最大の1日**が占めるシェア | **≤ 25%** | 同 DS-7 と同一 |

### A-5.1 重大論点 C-7 の3点チェック

D-10 が求める3点を本 prescreen に直接組み込む。本EXPでは3点のうち2点が既存の DS ゲートと**同一量**である。

| # | 測る量 | 閾値 | 対応する既存ゲート |
|---|---|---|---|
| **C7-1** | 確認期間の相異なるイベント日数 | **≥ 80日** | **DS-5 と同一**（新設ではない） |
| **C7-2** | 確認期間の最大単日イベントシェア | **≤ 25%** | **DS-7 と同一**（新設ではない） |
| **C7-3** | **選定期間と確認期間のイベント発生率の比** = max(率) ÷ min(率)。率 := イベント件数 ÷ 有効イベント対象営業日数（`n_sel_eff` / `n_conf_eff`） | **≤ 2.0** | **新設。EXP-OBS000003 §6.0 DS-2b の帯 `[150→]` 中心 215 と上下限 `[100, 400]` から導出**（`400/215 = 1.86`、`215/100 = 2.15`）。旧EXPの実測比は **3.2倍**であり、この水準なら prescreen で条件付きGOを出していない（STRATEGY-BRIEF「D-10」） |

> **C7-3 は本PJで初めて数値化するゲートであるが、「新しく決めた自由な値」ではなく既存の DS-2b の帯から算術的に導いた値である。** また、これは**追加**のゲートであって既存のどの閾値も緩めていない。

## A-6. 判定規則（**この規則も測定前に凍結する**）

1. **DS-1〜DS-7 および C7-3 を<u>すべて</u>満たす** ⇒ **条件付きGO**。spec（`01-spec.md`）を起票する。
2. **1つでも満たさない** ⇒ **保留（K-5・判定不能）**。Kill ではない。**そして、分割規則 `m = floor(N/2)` および `z*` 較正グリッドを変えて再測定することを禁止する。** それを行った瞬間に K-4（HARKing）である。司令塔判断に上げる。
3. 測定は**件数・銘柄数・日数・欠損率のみ**を入力とする。**前方リターンを一切参照しない。**

> **`g` / `σ_gap` / `z` の算出は「リターンを見る」ことに当たるか**: 当たらない。`z` は**シグナルそのもの**であり、合否を決める前方リターン `R_5`（`t_1` 始値 → `t_5` 終値）とは別の量である。旧EXPでも B実装チームは §9-6 で `z` の分布と件数を出力したうえで DS ゲートを評価し、前方リターンには一度も触れていない（旧 spec §6.0.1 柱書）。本EXPも同じ境界を守る。

## A-7. 本 prescreen の測定が持つ既知の限界（**先に宣言する**）

| # | 限界 | 内容 | spec での扱い |
|---|---|---|---|
| **L-1** | **候補銘柄集合のサバイバーシップ・バイアス** | 取得済みの日足511銘柄は 2024-06-21 / 2025-07-01 の `/equities/master` で U-1・U-2 を通過した銘柄の和集合であり、2016〜2021年の選定期間に対しては**その後に上場廃止・規模区分から脱落した銘柄が構造的に欠落**する | **spec §9 の先行タスクとして `D_sel` / `D_conf` 時点の `/equities/master` を追加取得し、候補集合を再構成したうえで DS ゲートを再評価することを B実装チームに要求する。** 1つでも外したら G1 へ進まず S に差し戻す |
| **L-2** | **U-1 / U-2 の代理適用** | 本 prescreen では `D_sel` 時点の master が手元に無いため、U-1・U-2 は「511候補集合に属すること」で代理する。U-4・U-5・U-6 は日足から正しく適用する | 同上 |
| **L-3** | **E-1 / E-2A（銘柄別・厳密な権利落ち/配当落ち識別）の未適用** | 旧 spec §3.1.1 の5段階手続きと検証ゲート V-1〜V-5 は実装コストが大きく、prescreen の件数見積りでは**E-2B（暦ベースの一括除外）のみを適用**する。E-1 / E-2A は `σ_gap` の窓にも適用されるため、未適用だと `σ_gap` が膨張し `z` が圧縮される（＝**イベント件数を過小**に見積もる方向）。一方でイベント候補側の除外漏れは件数を過大にする。**正味の向きは断定しない**（旧 spec §3.7 の教訓: 曖昧さを「安全側だろう」と推測しない） | spec §9 で B実装チームが**正規の E-1 / E-2A / E-2B を適用して DS ゲートを再評価**する。prescreen の件数は**桁の確認**であって合否の確定ではない |
| **L-4** | **市場水準のリターン情報の部分的既知性** | `research/STRATEGY-BRIEF.md` および旧 spec §2.5 B-1 に、確認期間に含まれる一部日付の**ユニバース中央値ギャップ**（例 `2025-04-07: −7.258%`、`2026-03-09: −4.681%`）が既に記載されている。**個別銘柄の前方リターンは一度も観測されていない**が、完全な未見ではない | spec §2 の「測定範囲の空白」に明記して申し送る |

---
