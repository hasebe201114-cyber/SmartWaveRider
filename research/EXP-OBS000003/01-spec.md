# 実験仕様書（Spec） - EXP-OBS000003（非決算オーバーナイト・ギャップ反転）

> 担当: S戦略チーム（chief-strategist）が A設計官を兼務（PJ000002 §2.1）
> 鉄則: **成功基準は「回す前」に数値で確定する**（HARKing防止）。結果を見てから基準を緩めない。
> 前提文書: `research/EXP-OBS000003/00-prescreen.md`（**条件付きGO**・必須条件①〜⑥・申し送り T-1〜T-10）
> 起票日: 2026-09-14 ／ **現行版: 第3版**（§9-6 DS ゲート未達＝K-5 差し戻しを受けた**記録専用の改訂**。**前方リターンは誰も未観測であり、G1・G2・DS の合否基準は一切変更していない**）
> 司令塔判断（2026-09-14）: EXP-OBS000001（PEAD）を保留（凍結）とし、本EXPに着手する

> **現在の状態: §6.0 データ十分性ゲート DS-2a / DS-5 / DS-6 / DS-7 不合格（2026-09-14）により §6.8 K-5「Kill ではなく保留（判定不能）」。**
> **S戦略チームの結論: 本 spec の枠内では DS ゲートを満たす再設計は存在しない**（決定的根拠は §6.0.1 の DS-6。**DS-6 は `z*` に依存しない量であり、`z*` の較正グリッドをどう引き直しても 62日は動かない**）。
> **確認期間（2025-07-01〜）のリターンは一度も参照されておらず、未見のまま温存されている。** G1（§6.1）以降の実行は N-11 により引き続き禁止。
> **司令塔判断待ち: D-9（契約データ期間の構造的不足＝データ階層の投資判断）／ D-10（市場共通因子駆動イベントの prescreen 事前スクリーニング化）。**

## 対応OBS番号

OBS000003（`research/EXP-OBS000003/`）

---

## 0. 本 spec の位置づけと prescreen 必須条件の反映表

prescreen の必須条件・申し送りが本 spec のどこに落ちているかの対応表。**C品質チームはこの表を使って落とし漏れを検査すること。**

| prescreen | 内容 | 本 spec の反映先 |
|---|---|---|
| 必須条件① | 他シグナルとの合成・ゲート・足し算を一切行わない | §5.1／§10.2 N-1・N-2 |
| 必須条件② | 1日保有（寄付→引け）版を含めない。保有は 3/5/10 営業日に限る | §4.5（主=5営業日）／§10.2 N-3 |
| 必須条件③ | ショート脚を含めない。下方ギャップのロングのみ | §5.2／§10.2 N-3 |
| 必須条件④ | 予測単位（連続値IC＋z単調性＋permutation p値）とパイプライン統合（コスト控除後・最大DD・約定不能率）の**両方**を測定 | §6.1〜§6.3（予測単位）／§6.4〜§6.5（パイプライン） |
| 必須条件⑤ | 必要グロス 0.45%（損益分岐）／0.85%（Sharpe 1.0）を回す前に固定。統計的有意でも 0.45% 未満なら不採用 | §6.2 **G1-3**（0.45% を合否ゲート化）／§8.4（0.85% は報告基準） |
| 必須条件⑥ | EXP-OBS000001（PEAD）と事象集合として排反 | §3.4 E-3（`DiscDate` 前後2営業日の全除外）／§3.5（排反性の証明） |
| T-1 | イベント件数の上限を事前固定（常時フル投資への退化防止） | §3.6（z\* のカウントのみによる較正）／§6.0 **DS-2a / DS-2b** |
| T-2 | 閾値の非単調性チェック（OBS000018 対策） | §6.1 G1-1（主指標＝連続値IC）／§6.2 G1-5（デシル単調性）／§6.3-4（閾値スイープは記述統計） |
| T-3 | PEAD との事象集合の排反性 | §3.4 E-3（N=2営業日で数値固定）／§3.5 |
| T-4 | リスク集中の数値固定 | §5.3（同日2件・同時5銘柄・同一S33 2銘柄）／§4.3（初期損切・トレール4パラメータ） |
| T-5 | 寄付の約定モデルを保守側に固定 | §4.0（UL/LL 規約の流用）／§4.1（片道0.10%・ストップ安寄り除外）／§4.6（日足では検証不能である旨の明記） |
| T-6 | 「非決算」サブグループを主検定にしない（OBS000038 対策） | §3.4（決算起因を**除外した後の全サンプル**が主検定の母集団）／§6.3-5 |
| T-7 | 「非決算」定義の限界の明記 | §3.7（混入の推定が構造的に不可能であることと、バイアスの向きを断定しないこと） |
| T-8 | 窓長・ラグの算術的整合性（EXP-OBS000037 の教訓） | §4.5 の算術整合性表（**本数・価格点数・日付レンジを全行で明記**）。第2版で `σ_gap` 窓の改訂に伴い書き直し、「60本のギャップ ⇔ 終値61個」が隣接の場合のみ成立する特殊ケースであったことを明記 |
| （第2版で追加） | 配当落ち（E-2）の識別手段の確立 | §3.1.1（決定的な構成手続き）／§9-2b（検証ゲート V-1〜V-5）／§6.8 K-6 |
| （第2版で追加） | 単日集中の検知と合格の単日非依存性 | §6.0 **DS-7**（最大単日シェア ≤ 25%）／§6.2 **G1-7**（最大イベント日を除いても符号が保たれること） |
| T-9 | 選定／確認の分割 | §2.1（**分割する**。判断根拠はイベント件数のみ・§2.2） |
| T-10 | コスト感度の必須3点 | §8.2（0.35/0.45/0.60）／§6.4 **G2-7**（0.60% で損益分岐を割るなら不合格） |

---

## 1. 仮説（1文）

**日本株 TOPIX500 相当ユニバースにおいて、寄付の板寄せで前日終値から自銘柄の平常ギャップ変動の数倍にあたる下方乖離をもって始まった銘柄のうち、決算短信等の開示に起因しないものは、その乖離の一部を続く5営業日で回復し、往復コスト0.45%を控除した後も正の期待値を残す。**

### 1.1 エッジ族の宣言（prescreen §4.2 の確定を引き継ぐ）

- 予測変数 `g = ln(O(D)/C(D−1))` は**価格のみから算術的に導かれる量**であり、**PJ000001 §4 C-3 が事前確率「低」と宣告した「価格由来の特徴量から方向を予測する」族に属する。**
- **本EXPは本プロジェクトの賭け（非価格情報に直交エッジがあるか）を検証するものではない。** 非価格なのは特徴量ではなく条件分岐（決算開示の有無）のみである。
- 「夜間取引＝非価格情報」という司令塔提示の前提は prescreen §4.2 で**誤りと判定済み**であり、本 spec はこの訂正を前提に書かれている。

### 1.2 事前確率の見立て

**低〜中（低寄り）**。prescreen §4.2 の判定をそのまま引き継ぐ。**結果を見てからこの評価を引き上げることはしない。**

日本株固有の構造（17.5時間の取引停止・寄付の板寄せ単一価格オークション・値幅制限・昼休みによる分断）は 8連敗の測定対象である24時間連続取引の暗号資産には**概念として存在しない**（prescreen §4.3）。ただしこれは「測る価値がある」ことの根拠であって、「有望である」ことの根拠ではない。

### 1.3 本EXPが賭けているものの正体（prescreen §7.6 の再掲・忘れないこと）

- 文献が報告するオーバーナイト反転は**日中セッションで完結する**ものであり、本プロジェクトのコスト構造はその保有期間を支払えない（prescreen §7.5: 1日保有版は必要グロス0.45%に対し効果量0.07%で**6.4倍不足**＝回す前に却下済み）。
- したがって本EXPは「**その反転の一部が数営業日にわたって残存している**」という文献の外挿に賭けている。
- **文献の存在は本EXPの根拠を補強しない。** 文献が「日中で完結する」と報告していることは、数営業日の残存に対してはむしろ弱い反証である。

---

## 2. 選定 / 確認プロトコル（過学習の炙り出し）

### 2.0 参照営業日カレンダー `T` の定義（本 spec 全体で唯一の正本）

- `T` := ユニバース候補全銘柄の `/equities/bars/daily` の**観測日付の和集合**を昇順に並べた列。**1始まりの添字**を用いる。
- これは `research/EXP-OBS000001/01-spec.md` §2.1.0 と**完全に同一の定義**であり、新しい定義を導入するものではない。
- 既知の実測値（`research/EXP-OBS000001/10-result/feasibility.json` の `9-2_missing_rate`）: `T[1] = 2024-06-21`、`|T| = 487`、`T[487] = 2026-06-22`、欠損営業日率 **0.0%**、候補銘柄数 511。
- **`T` は日付のリストであり、価格を一切参照しない。**
- B実装チームは本EXPのデータ取得結果から `T` を機械的に再構成し、上記の既知値と一致することを確認する（§9-1）。**推定日付をハードコードしてはならない。**

### 2.1 期間区分（**分割する**）

| 区分 | ユニバース確定日 `D_u` | U-4 の売買代金中央値を計算する窓 | イベント日 `D` の範囲 | 役割 |
|---|---|---|---|---|
| **選定（参照）** | **`T[61]`**（推定 2024-09-18） | **`T[1]` 〜 `T[60]`（ちょうど60営業日）** | **`T[68]` 〜 2025-06-30** | 事前固定した設計が選定期間でどう見えるかを**記録する**。加えて §3.6 の `z*` 較正（**カウントのみ**）を行う唯一の場所 |
| **確認（未見）** | **2025-07-01** | 2025-07-01 の直前60営業日（`D_u` を含まない） | **2025-07-01 〜 §2.4 で定める末尾** | **G1・G2 の合否はこの期間のみで判定する** |

- **ユニバース確定日・U-1〜U-7 の規則は `research/EXP-OBS000001/01-spec.md` §5.4 を一字一句そのまま流用する**（§5.4 参照）。窓長60営業日・閾値5億円・上限175銘柄・下限150銘柄を**一切変更しない**。
- **イベント日の開始が `T[68]` である理由（算術的根拠。第2版で `T[62]` から改訂）**: §3.2 の `σ_gap` は `T[i−66]` 〜 `T[i−1]` の範囲から有効ギャップ60本を採る。その範囲の最も古いギャップ `g(j, T[i−66])` は終値 `C(j, T[i−67])` を要する（§4.5 の算術整合性表）。したがって `i − 67 ≥ 1`、すなわち **`i ≥ 68`**。同時に、`D` はユニバース確定日 `T[61]` より後でなければならない（`68 > 61` で充足）。両条件を同時に満たす最小の添字が **68** である。
- **`T[61]` / `T[68]` の実日付はハードコードせず、`T` から機械的に求めて `params.json` に出力する**（§9-1）。

### 2.2 「分割する」という判断の根拠（**リターンを一切見ずに、件数のみで行った**）

prescreen 申し送り T-9 は「確認期間だけで T-1 の件数条件を満たすならば分割を行う」と定めている。本 spec は以下の**件数のみの算術**により分割を選択した。

| 量 | 値 | 算出 |
|---|---|---|
| 参照営業日の総数 | 487本 | 実測（`feasibility.json`） |
| 年あたり営業日（本 spec で固定する年換算定数） | **245本/年** | 487 ÷ 2.0年 ≈ 243.5 を保守的に 245 と置く。**以降すべての年換算にこの定数のみを使う** |
| 選定期間のイベント日数 | **174本**（第3版で訂正。第2版は 173本） | `T[68]` 〜 2025-06-30 の営業日 **183本**（`T[68..250]`、`250−68+1`）− E-2B の一括除外 **9本**（第2版は 10本と誤記） |
| 確認期間のイベント日数 | **220本**（第3版で訂正。第2版は 221本） | 2025-07-01 〜 `T[481]` の営業日 **231本**（`T[251..481]`、`481−251+1`。第2版は 232本と誤記）− E-2B の一括除外 **11本** |
| ユニバース × 確認期間の銘柄日 | 約 **38,500 銘柄日** | 175銘柄 × 220本 |
| `z*` 較正の目標イベント件数（§3.6） | **215件/年** | prescreen §4.6 の想定値 |
| 確認期間の期待イベント件数 | 約 **193件** | 215 × 220 ÷ 245 |
| 選定期間の期待イベント件数 | 約 **153件** | 215 × 174 ÷ 245 |

- 確認期間単独で期待193件（T-1 の帯 150〜300 の内側）、選定期間単独でも期待153件（同帯の内側）であり、**両期間とも単独で件数条件を満たす。** したがって分割する。

> **【第3版で訂正】§2.2 の営業日算術に2件の誤りがあった（B実装チームの独立再計算により発覚）。**
> ① 確認期間の営業日を **232本**と書いたが、正しくは **231本**である（`T[251] = 2025-07-01`、`T[481] = 2026-06-12` ⇒ `481 − 251 + 1 = 231`）。
> ② 選定期間の E-2B 一括除外日を **10本**と書いたが、`T[68] = 2024-09-30` 以降 2025-06-30 以前に入る `X(m)` は `2024-10-30 / 11-28 / 12-27 / 2025-01-30 / 02-27 / 03-28 / 04-28 / 05-29 / 06-27` の **9本**である（`2024-09-27` は `T[68]` より前）。
> **いずれも `183 − 9 = 174` / `231 − 11 = 220` へ訂正した。分割の判断（両期間とも期待件数が T-1 の帯 150〜300 の内側）は変わらない。**
> **この誤りは、T-8（trading-app-v2 EXP-OBS000037 の教訓）が禁じた「導出済みの数値を散文の表に書き、実装者が再計算できる式を書かない」という設計側の過失である。** 派生した実害は §3.6 の分母の曖昧化であり、その扱いは §3.6 の第3版注記に記す。
- **第2版での改訂**: 第1版はイベント日数を 181本／245本と**推定**していたが、`T` の実測と、`σ_gap` 窓の改訂（開始 `T[62]` → `T[68]`）および E-2B の一括除外を反映して **173本／221本**に改めた。**選定期間の期待件数は 159件 → 152件へ減るが、T-1 の下限 150 を上回るため分割の判断は変わらない。** なお §3.6 の `z*` 較正は**年換算率** `N_ann = 件数 × 245 ÷ イベント日数` を目標215に合わせる手続きであるため、イベント日数の変化は較正によって自動的に吸収される（目標件数そのものは動かしていない）。
- **分割点 2025-07-01 は本EXPのデータを見て選んだ日付ではない。** EXP-OBS000001（PEAD）が 2026-09-13 時点で既に確定・凍結している分割点をそのまま流用したものであり、本EXPに固有の選択の自由度は存在しない。
- 上記の算術には**リターンが一切入っていない**（銘柄日数と目標抽出率のみ）。

### 2.3 本 EXP における「選定」の自由度は `z*` の1個だけであり、それもカウントのみで決まる

本 spec は、シグナル定義・正規化窓・ホライズン・トレール4パラメータ・ユニバース条件・コスト前提・リスク上限の**すべてを回す前に数値で固定している**。選定期間で決めるものは **`z*`（イベント閾値）ただ1つ**であり、その決め方は §3.6 の通り**候補イベント件数のみを入力とする決定的手続き**である。**リターンは一切参照しない。**

- B実装チームは、選定期間の**リターン**を見て spec のいかなる値も変更してはならない。変更が必要と判断した場合は**実装を止めて S に差し戻す**（N-7）。
- 選定/確認の符号一致（§6.2 G1-2・G1-6、§6.4 G2-3）は、この前提の下での再現性確認として要求する。

### 2.4 確認期間の末尾（前方リターン窓との整合）

`D = T[i]` に対し、予測単位（H=5）は `t_5 = T[i+5]` を、パイプライン（最大保有5営業日＋翌営業日始値決済）は `t_6 = T[i+6]` を要する。

| 用途 | 必要な最大添字 | 許容される最大のイベント日 | 実日付 |
|---|---|---|---|
| 予測単位 G1（H=5） | `i+5 ≤ 487` | **`T[482]`** | `params.json` に出力（推定 2026-06-15 近傍） |
| 予測単位 SA（H=10） | `i+10 ≤ 487` | `T[477]` | 同上 |
| パイプライン G2（最大保有5＋決済1） | `i+6 ≤ 487` | **`T[481]`** | 同上 |

- **イベント集合は G1・G2 で共通に定義し、前方窓が確保できないイベントは各測定から除外して件数を記録する**（PEAD §4.2 と同じ扱い）。イベント定義側を測定ごとに変えてはならない。
- 実日付は `T` から機械的に求めて `params.json` に出力する。

### 2.5 測定範囲の空白（**必ず申し送ること**）

| # | 空白 | 内容 |
|---|---|---|
| **B-1** | **テール局面の観測が少数日に限られる**（**第2版で事実誤認を訂正**） | **第1版の記述は誤りであった。** 第1版は「契約データ2年間で唯一のテール局面である2024年8月上旬の急落が測定窓の外にある」と書いたが、S が候補511銘柄の**日次のユニバース中央値ギャップ**を実測したところ、以下のとおり**測定窓の内側にも 2024-08-05 と同等以上のストレス日が存在する**。<br><br>`2025-04-07: −7.258%`【選定】／`2024-08-05: −5.072%`【窓外】／`2026-03-09: −4.681%`【確認】／`2026-03-30: −4.556%`【確認・E-2B で除外】／`2025-04-11: −3.694%`【選定】／`2025-04-03: −3.598%`【選定】<br><br>すなわち (a) **データ期間中で最大のストレス日 2025-04-07（−7.26%）は選定期間の内側**にあり、2024-08-05 より大きい。(b) **確認期間にも 2026-03-09（−4.68%）というストレス日が存在する。**<br>**残る本当の空白**は「2024年8月上旬のエピソードが窓外であること」そのものではなく、**独立なストレス・エピソードの数が確認期間で実質1回（2026年3月）しかないこと**である。テール局面での挙動は**1エピソードからしか観測されず、再現性を確認する術がない**。C品質チームは判定文にこの訂正後の事実を明記すること。補完は STEP3 フォワード較正でのみ可能 |
| **B-7** | **確認期間の第2のストレス日が配当落ちと同居し、除外される**（第2版で新設） | **2026-03-30 は確認期間で2番目に大きいストレス日（中央値ギャップ −4.556%）であると同時に、3月期決算会社の配当落ち日でもある。** E-2B により**除外される**。<br>この日の中央値ギャップ −4.556% のうち約 **1.39pp** は機械的な配当落ち（実測平均配当利回り 1.3894%）であり、残りが市場要因である。両者はこの日の横断回帰で分離できない（当日の日内クロスセクション回帰の傾き `β = −0.485` は、他の落ち日の −0.80〜−0.90 と異なり、市場全体の −4.2% の動きと銘柄ベータの交絡により減衰している）。<br>**除外の代償**: 確認期間のストレス日が2日から1日（2026-03-09）へ減る。**除外しない場合の代償**: 全ユニバースの `z` がその日に約 1.2σ 分だけ一律に押し下げられ、イベントが1日へ集中する（＝OBS000029 の時間方向再発そのもの）。**S は前者を選んだ**（判断事項 D-8） |
| **B-8** | **イベント過程が市場共通因子に支配される群発過程であり、契約データ2年では日次ブロック統計が成立しない**（**第3版で新設。実測により確定**） | 設計時は「イベントは日ごとにばらついて発生し、確認期間で `K ≈ 180日` の日次ブロックが得られる」と想定していた（§6.1.4(a)）。実測は **`K = 62日`**、イベント集合は **106件が25日**に集中し、単日 `2026-03-09` が **52.8%** を占める。<br>**これは `z*` の較正の失敗ではなく、日本株のオーバーナイト・ギャップが市場共通因子（米国市場の夜間の動き・為替）に支配されているという市場構造そのものの反映である。** 詳細と、再設計が不可能である論証は **§6.0.1**。<br>**本EXPが測定できなかったのは「ギャップ反転のエッジの有無」であり、代わりに測定できたのは「イベント過程の時間構造」である。** 後者は後継の**あらゆる**イベント駆動EXPに転用可能な制約として `portfolio-ledger.md` と `STRATEGY-BRIEF.md` に記録する |
| **B-2** | **寄付の約定前提が日足では検証できない** | §4.6。本EXPは「板寄せの始値で買える」という前提に全面的に依拠するが、自分の成行注文が清算価格の形成に参加する効果・特別気配による寄付遅延は日足データでは原理的に測定できない |
| **B-3** | **未識別ニュース起因ギャップの混入** | §3.7。`/fins/summary` に載らない適時開示（業績修正・自己株買い・M&A 等）は TDnetアドオン未契約のため識別できず、**混入率の条件付き推定は構造的に不可能**（§3.7 で理由を数値で示す） |
| **B-4** | **信用規制（日々公表銘柄・増担保規制）** | 現行契約でデータ取得不可（C-5・HTTP 403）。**模擬不能**。バックテストは規制発動がない前提＝**楽観側に偏る** |
| **B-5** | **取引時間の構造変化** | 東証の大引けは **2024-11-05 に 15:00 → 15:30 へ延長**された。オーバーナイト窓長は 18.0時間 → 17.5時間に変化しており、**本EXPのシグナルの定義そのものに期中の構造変化がある**。確認期間（2025-07-01〜）は**全期間が延長後**であり合否判定には影響しないが、選定期間の先頭約33営業日は延長前である。§6.3-6 で前後別の記述統計を出力する |
| **B-6** | **EXP-OBS000001（PEAD）確認期間との窓の重複** | 本EXPの確認期間（2025-07-01〜）は、凍結中の PEAD が「未見の確認期間」として温存している窓と**同一カレンダー**である。本EXPを実行すると、当該窓の**株価リターンは観測済み**となる。<br>**S の判断**: 契約データが2年しかない以上、2本目以降の実験が既存の確認窓を避けることは物理的に不可能であり、窓を避けて分割点を動かすことは本EXP側に新たな自由度を持ち込む（＝より悪い）。また PEAD は再開時に §N-9 により**新規EXPとして分割を事前登録し直す**ことが既に定められており、実質的な損失は限定的である。**ただしこれは S の判断であって自明な結論ではないため、司令塔判断事項として申し送る（判断事項 D-6）** |

---

## 3. シグナル定義（回す前に数値で確定）

### 3.1 使用する価格（調整の規約）

- ギャップ・`σ_gap`・前方リターン・損益のすべてにおいて、**同一の調整規約で揃えた価格系列を使う。** 生値と調整値を混在させてはならない。
- 株式分割・併合を跨ぐ日の生値ギャップは**株価水準の人為的な段差**であり、これを「下方ギャップ」と誤認すると、**シグナルの最極端側が分割イベントで埋まる**。これは本EXPを構造的に破壊する。
- **B実装チームは §9-2 として、`/equities/bars/daily` の `AdjFactor` / `ExRT` / `Adj*` 系フィールドの正式名称・全ユニーク値・件数・調整の累積規約を実測して出力し、`params.json` に明記すること。** 意味が**一意に決まらない場合は推測で埋めず S に差し戻す**（N-8）。
- 調整規約が確定した上で、以下を**必ず実装する**（二重の安全網）:
  - **E-1（権利落ちの除外）**: `AdjFactor(D) ≠ 1`、または `ExRT` が `D` における権利落ち（分割・併合・権利付最終日翌日等）を示す場合、**その銘柄日 `D` をイベント候補およびプールから除外する**。除外件数を記録する。
  - **E-2（配当落ちの除外）**: `AdjFactor` は配当を調整しない（§9-2a で実測確定）。したがって**配当落ち日の下方段差は本EXPのシグナルに直接混入する。** 日本株の配当落ちは3月末・9月末に極端に集中するため、これは **OBS000029（実質1本張り）の時間方向再発**を直接引き起こす。本 spec は §3.1.1 の**決定的な構成手続き**により配当落ち日を識別し、**E-2A（銘柄別・厳密）と E-2B（暦ベース・一括）の和集合**を除外する

> **この扱いを「記録して先へ進む」にしなかった理由**: 配当落ちは(a)方向が常に下向き、(b)反転しない（理論価格の段差）、(c)暦上2日に極端に集中する、という3条件をすべて満たす。混入した場合の影響は「効果の希釈」ではなく「特定2日への集中と、そこでの確実な負け」であり、バイアスの向きが安全側ではない。EXP-OBS000037 の教訓（曖昧さを安全側と推測しない）を、ここで適用する。

> **「配当分だけ価格を補正して使う」（除外せず調整する）を採らなかった理由**: 理論上の落ち幅は配当全額だが、**実際の落ち幅は配当額の 100% ではない**（源泉徴収 20.315% を含む税制クライアンテレ効果により、実証的な落ち率は 0.7〜1.0 倍の範囲に入る）。補正を行えば「どの落ち率を使うか」という**新しい自由パラメータ**が生じ、かつ残差（≈ 0.15 × 平均配当利回り 1.39% ≈ **0.21%**）は**配当利回りと横断的に相関して残る**。これは `σ_gap` の典型値（約1.2%）の **0.17σ** に相当し、「高配当銘柄が下方ギャップを出して反転しない」という**本EXPが検出したいものと正反対の系統成分**を残す。**除外は規約であり自由度を持たないが、補正は自由度を持つ。** よって除外を採る。

#### 3.1.1 配当落ち日の決定的な構成手続き（§9-2b で検証する）

**識別の原理**: 日本株の配当の権利確定日（基準日）は**会計期間の期末日**であり、これは `/fins/summary` の `CurFYSt` / `CurFYEn` / `CurPerType` / `CurPerEn` から取得できる。実際に配当が支払われたか否かと金額は**実績配当**フィールド `Div1Q` / `Div2Q` / `Div3Q` / `DivFY` から取得できる。除権日そのもののフィールドは存在しないが、**基準日から権利落ち日への写像は 2019-07-16 以降の T+2 決済のもとで一意に決まる**（下記 step 3）。したがって「除権日フィールドが無い」ことは「配当落ちが識別できない」ことを意味しない。

| step | 手続き（**すべて決定的。推定・近似・当てはめを含まない**） |
|---|---|
| **1** | `/fins/summary` の各行について、`CurFYSt`（会計年度開始日）と `CurFYEn`（会計年度末日）を読む。いずれかが空の行は使わない（件数を記録） |
| **2** | 会計四半期末日を**暦月加算**で構成する: `Q1末 = CurFYSt + 3ヶ月 − 1日`、`Q2末 = CurFYSt + 6ヶ月 − 1日`、`Q3末 = CurFYSt + 9ヶ月 − 1日`、`FY末 = CurFYEn`。**暦月加算は日を保存し、月末超過のみクランプする**（例: `CurFYSt = 2023-08-21` ⇒ `Q2末 = 2024-02-21 − 1日 = 2024-02-20`）。基準日 `R` は**暦日**であって営業日ではない |
| **3** | 基準日 `R` を権利落ち日 `X` へ写す: `b(R) :=` **`T` の中で `R` 以下である最大の営業日**、`X :=` **`T` 上で `b(R)` の1営業日前**（＝ `T` の添字を1減じる。暦日で減じてはならない）。`T` に `R` 以下の営業日が無い場合、または `b(R) = T[1]` の場合は構成しない（件数を記録）<br>**根拠（T+2 決済の算術）**: 基準日 `R` に株主名簿に載るには受渡が `b(R)` までに完了する必要があり、`T+2` 決済のもとで権利付最終日 `= b(R) の2営業日前`、その翌営業日である `b(R) の1営業日前` が権利落ち日である |
| **4** | 各四半期の実績配当 `Div1Q` / `Div2Q` / `Div3Q` / `DivFY` を step 2 の対応する期末日に**フィールド名の四半期番号で**結びつける。**開示回の四半期ではなく、会計年度内の四半期に対応する**（例: 3Q短信が `Div2Q = 5.0` を報告する場合、それは中間期末を基準日とする配当である） |
| **5** | **`DPS > 0` のもののみ**を配当落ちとする。`0.0` の明示申告は「配当なし」として除外対象にしない（件数を記録）。同一 `(銘柄, 基準日)` に複数の申告値が現れた場合は**最大値**を採り、その発生件数を記録する |

- **E-2A（銘柄別・厳密）**: step 1〜5 で構成された銘柄日 `(j, X)` を、**イベント候補・プール `P`・`σ_gap` の窓のすべてから除外する。**
- **E-2B（暦ベース・一括）**: 各暦月 `m` について、`T` 内で月 `m` に属する最後の営業日を `L(m)`、その1営業日前を `X(m) := T[idx(L(m)) − 1]` とする。**`X(m)` を全銘柄共通で、イベント候補・プール `P`・`σ_gap` の窓のすべてから除外する。**

> **E-2B を E-2A に加えて置く理由（E-2A だけでは足りない）**: (a) **株主優待・新株予約権無償割当等、配当以外の権利落ち**が同一基準日に同居するが `Div*` には現れない、(b) **配当落ち日には銘柄横断の需給歪み**（配当再投資に伴う先物・現物のフロー、指数の配当落ち調整）が生じ、**配当を出さない銘柄にも及ぶ**ため銘柄別除外では取りこぼす、(c) `/fins/summary` のカバレッジが欠ける銘柄（実測 511 中 1 銘柄）を E-2A は救えない。**E-2B は「配当落ち日という日そのものを測定対象から外す」規約であり、E-2A は「非月末決算期の銘柄」を救う補完である**（実測: E-2A の 1,859 銘柄日のうち **12 件**が E-2B に覆われない＝ `2024-05-15` 期末・`2024-08-20` 期末などの非月末決算会社）。
> **E-2B の副作用（記録必須）**: `T` の末尾が暦月の途中で終わるため、最終月の `X(m)` は真の月末落ち日ではない。ただしこれは**イベント日範囲の上限 `T[481]` より後**にあり実害がない。B実装チームは該当件数を記録すること。

### 3.2 ギャップと正規化（**単一の事前登録定義。これ以外を合否判定に使わない**）

`D = T[i]` を**イベント日**（ギャップが観測される日）とする。

| 記号 | 定義 |
|---|---|
| `g(j,D)` | `ln( O(j, T[i]) / C(j, T[i−1]) )` — **銘柄 `j` の `D` におけるオーバーナイト・ギャップ（対数）** |
| `σ_gap(j,D)` | **有効ギャップちょうど60本**の**標本標準偏差（ddof=1）**（下記「`σ_gap` の窓」を参照） |
| **`z(j,D)`** | **`g(j,D) / σ_gap(j,D)`** — 下方ギャップでは負値 |
| **`S(j,D)`** | **`−z(j,D)`** — 下方ギャップの深さ。**大きいほど深い。** 予測単位の説明変数はこの `S` である |

**`σ_gap` の窓（第2版で改訂。改訂理由は §9-2b の実測にもとづく）**

- **有効ギャップ**とは、`g(j, T[m])` が算出可能（`O(j,T[m])` と `C(j,T[m−1])` がともに存在し正）であり、**かつ `T[m]` が `j` にとって E-1 / E-2A / E-2B の除外日でない**ギャップをいう。
- `σ_gap(j, D)`（`D = T[i]`）は、**`T[i−66]` 〜 `T[i−1]` の範囲にある有効ギャップのうち、新しい順にちょうど60本**を採り、その標本標準偏差（ddof=1）とする。
- **60本に満たない場合はイベント候補から除外する**（§3.4 C-2）。窓長の延長・短縮・「取得できた分での代用」を**禁止する**（N-8）。
- **遡り上限 66 を選んだ根拠（推測ではなく実測）**: 候補511銘柄・全銘柄日について、60本の有効ギャップを集めるのに必要な遡り営業日数 `K` の分布を実測したところ **最大 `K` = 65、`K ≤ 66` でカバー率 100.0000%（取りこぼし 0 件）** であった。66 は「余裕を見た値」ではなく、**実測された最大値 65 に 1 を足した上限**である。

> **なぜ第1版の「直前60本（隣接）」を改めたか（**測定にもとづく改訂であり、結果を見た緩和ではない**）**: 第1版は権利落ちギャップが `σ_gap` の窓に混入することを考慮していなかった。実測すると、**全銘柄日の 49.7% が窓内に権利落ちギャップを含み**、汚染ありの `σ` を除去後の `σ` で割った比の分布は **中央値 1.0033・p90 1.0838・p99 1.2725・最大 2.7873**（2% 超の膨張が 33.0%）であった。中央値では無視できるが、**裾では `σ` が最大 27%（p99）膨張し、`z` を同率だけ圧縮する。** 膨張量は配当利回りと相関するため、これは**高配当銘柄を系統的にイベント集合から押し出す**横断的な選択歪みであり、方向が安全側だと断定できない。**「中央値が小さいから無視してよい」と推測せず、実測して対処する**（N-6 を自分自身に適用）。

**仮説の符号**: `S` が大きいほど前方リターン `R_5` が大きい ⇒ **IC は正**。

**なぜ生の % ではなく `z` を主定義にするか**（C品質チームの検査用・回す前に確定した設計判断）:

1. ユニバースの株価帯は1,000〜3,500円（3.5倍幅）であり、銘柄ごとの日次ボラティリティも大きく異なる。**固定の % 閾値を使うと、イベント集合が系統的に高ボラ銘柄で埋まる。** それは「下方ギャップを取る戦略」ではなく「高ボラ銘柄のロング」であり、**OBS000029（実質1本張り）・OBS000030（ブレッド拡張はエッジ族の枯渇を救わない）と同型化する**。
2. `z` は「その銘柄の平常のギャップ変動に対して何倍か」という、銘柄間で比較可能な尺度を与える。
3. **`σ_gap` による正規化は、単一シグナル（ギャップ）のスケール変換であって、別のシグナルとの合成ではない。** したがって必須条件①／N-1 に抵触しない。ATR・ボリンジャーバンド等の**テクニカル指標は一切使わない**（N-1）。
4. 生の % 版（`g` をそのまま用いる版）は §6.3-7 で**記述統計としてのみ出力**する。**合否判定に使うことを禁止する**（N-10）。

### 3.3 ホライズンと保有期間

| 用途 | 値 | 位置づけ |
|---|---|---|
| 予測単位の主ホライズン | **H = 5営業日** | **合否判定はこれのみ**（§6.2） |
| 予測単位の事前登録 SA | H = 3営業日 / H = 10営業日 | **記述統計。合否判定に使うことを禁止**（N-10） |
| パイプラインの最大保有 | **5営業日**（`t_5` の終値時点で未決済なら `t_6` 始値で強制手仕舞い） | prescreen §4.6 の枠容量算術（8枠×245÷5）および §7.1 の金利計算（7暦日）が 5営業日を前提に組まれているため |
| **1営業日（寄付→引け）版** | **禁止**（必須条件②・prescreen §7.5） | N-3 |

### 3.4 イベント候補の定義と除外規則（**すべて事前登録・件数を必ず記録**）

銘柄日 `(j, D)` が**イベント候補**であるとは、以下をすべて満たすことをいう。

| # | 条件 | 備考 |
|---|---|---|
| **C-1** | `j` が当該期間のユニバース（§5.4）に属する | — |
| **C-2** | `D`、`T[i−1]`、および `σ_gap` の窓 `T[i−61] … T[i−1]` のすべてに `j` の日足行が存在する | 欠測があれば除外・件数記録 |
| **C-3** | `σ_gap(j,D) > 0` かつ有限 | ゼロ分散・NaN は除外・件数記録 |
| **C-4** | **`z(j,D) ≤ z*`**（`z*` は §3.6 で較正し凍結） | イベント（トレード対象）の定義 |

**除外規則**（イベント候補・プールの双方に適用。**すべて件数と内訳を記録する**）:

| # | 除外 | 数値固定 |
|---|---|---|
| **E-1** | 権利落ち日 | §3.1 |
| **E-2A** | **配当落ち日（銘柄別・厳密）**。§3.1.1 step 1〜5 で構成した `(j, X)` | §3.1 / §3.1.1 |
| **E-2B** | **配当落ち日（暦ベース・一括）**。各暦月の最終営業日の1営業日前 `X(m)` を**全銘柄共通**で除外 | §3.1 / §3.1.1 |
| **E-3** | **決算開示の近傍**（必須条件⑥・T-3） | `/fins/summary` の `DiscDate` が **`[T[i−2], T[i+2]]`（前後2営業日・計5営業日）**に1件でも存在する銘柄日 `D` を**全除外**する |
| **E-4** | 前日終値が値幅制限に張り付いている | `UL(T[i−1]) = 1` または `LL(T[i−1]) = 1` の場合に除外（前日終値が市場清算価格でないため `g` が意味を持たない） |
| **E-5** | イベント日の始値が値幅制限下限に一致 | `LL(D) = 1 かつ O(D) = L(D)`（ギャップが制度により打ち切られており `z` が銘柄間で比較不能）。§4.0 の規約で判定する |
| **E-6** | 上方ギャップ | `g(j,D) ≥ 0` は本EXPの対象外（ショート脚の禁止・必須条件③） |

- **表記について（第2版）**: 本 spec で以降「**E-1〜E-6**」と書いたときは、**E-1 / E-2A / E-2B / E-3 / E-4 / E-5 / E-6 の7規則すべて**を指す。E-2 は第2版で E-2A と E-2B に分かれたが、範囲表記の意味は変えていない。**E-1 / E-2A / E-2B の3規則のみ、イベント候補・プール `P` に加えて `σ_gap` の窓にも適用される**（§3.2）。E-3〜E-6 は `σ_gap` の窓には適用しない（決算近傍・値幅制限・上方ギャップは `g` の機械的段差ではなく、平常のボラティリティの一部だからである）。
- **E-3 の N=2 を選んだ根拠**: (a) 決算短信は当日15:00前後の開示が多く、翌営業日の寄付に直撃するため `D−1` の除外は必須。(b) 場前開示・訂正開示・翌日以降の連鎖開示を捉えるため前後に1営業日ずつ余裕を持たせた。(c) 窓をこれ以上広げるとイベント件数が減り検出力を落とす。**N=2 は回す前に固定し、結果を見て動かさない**（N-9）。
- **E-3 は「非決算」サブグループを作る操作ではない。** E-3 適用**後の全サンプル**が本EXPの主検定の母集団である（T-6）。「決算起因のギャップ」と「非決算のギャップ」を比較して後者の優位を示す設計は**採らない**（OBS000038 の教訓：条件付きサブグループの合格を全体の改善と読まない）。

### 3.5 PEAD（EXP-OBS000001）との事象排反性の証明（必須条件⑥）

- PEAD のイベントは `(j, DiscDate)` であり、そのエントリー日は `DiscDate` の翌営業日である。
- 本EXPのイベント日 `D` は、E-3 により `DiscDate ∈ [D−2, D+2]` なる銘柄日をすべて除外している。
- 仮に本EXPの `D` が PEAD のエントリー日と一致したとすると `DiscDate = D−1` であり、これは `[D−2, D+2]` に含まれるから E-3 で除外されている。**矛盾。**
- 同様に、PEAD のイベント日そのもの（`D = DiscDate`）も `[D−2, D+2]` に含まれるため除外されている。
- **∴ 本EXPのイベント集合と PEAD のイベント集合・エントリー日集合は排反である。** B実装チームは、E-3 による除外件数を**銘柄日ベースと `DiscDate` ベースの両方**で出力し、この排反性を実データ上で確認すること（§9-7）。

### 3.6 イベント閾値 `z*` の較正（T-1。**カウントのみを入力とする決定的手続き**）

1. 探索グリッドを**事前に固定する**: `z* ∈ { −1.50, −1.75, −2.00, −2.25, −2.50, −2.75, −3.00, −3.25, −3.50, −3.75, −4.00 }`（0.25刻み・11点）。**グリッドの追加・細分化を禁止する**（N-9）。
2. **選定期間のみ**について、各グリッド点の**候補イベント件数**（§3.4 の C-1〜C-4 と E-1〜E-6 をすべて適用した後の件数）を数える。
3. 年換算件数 `N_ann(z*) = 件数 × 245 ÷ (選定期間のイベント日数)`。**年換算定数は §2.2 で固定した 245 のみを使う。**
4. **`z*` = `|N_ann(z*) − 215|` を最小にするグリッド点。** 同値の場合はより極端側（より負）を採る。
5. `z*` を**凍結**する。確認期間・パイプライン・感度分析のすべてで同じ `z*` を使う。
6. **この手続きに前方リターンは一切入らない。** 入力は銘柄日の件数のみである。

> **【第3版で記録】step 3 の分母「選定期間のイベント日数」が一意に定義されておらず、B実装チームは §2.2 の値ではなく期間レンジの営業日数を用いた。**
> B は選定期間を `T[68..250]` の **183本**、確認期間を `T[251..487]` の **237本**として `N_ann` を算出した（`feasibility.json` `9-6` / `9-5_gap_sigma_z_descriptive_stats.period_ranges`）。一方 §2.2 が定義していたのは E-2B を差し引いた **173本 / 221本**（第3版の訂正後は **174本 / 220本**）であり、さらに確認期間の上限は `T[487]` ではなく `T[481]`（§2.4）である。**3通りの読みが成立する曖昧な定義であった。**
> **B の実装は N-8 違反ではない。** spec が式を与えず導出済みの数値だけを書いていたため、B が期間レンジから機械的に再計算したのは合理的な解釈である。**責は設計側（S）にある。**
> **3通りの読みすべてでゲートの合否は変わらない**（下表）。したがって本件は DS ゲートの結論に影響しない。**この確認をもって、分母の定義を第3版で「正しい値」に書き換えることはしない**——結果を見た後に較正式の分母を選び直すことは、たとえ結論が同じでも K-4（HARKing）と外形上区別できないためである。後継EXPを起票する場合は、`N_ann` の分母を**`T` の添字から一意に再計算できる式**として書くこと。

| 分母の読み | 選定 `N_ann(z*=−4.00)` | DS-2a（`[150,300]` の点が存在するか） | 確認 `N_ann` | DS-2b（`[100,400]`） |
|---|---|---|---|---|
| B が採用: 183 / 237（期間レンジの営業日数） | **356.12** | **不合格**（11点すべて 300 超） | **109.58** | 合格 |
| 第2版 §2.2 の記載: 173 / 221 | **376.71** | **不合格**（同上） | **117.51** | 合格 |
| 第3版の訂正値: 174 / 220 | **374.54** | **不合格**（同上） | **118.05** | 合格 |

- 分母を小さく取るほど `N_ann` は**大きく**なる（＝ DS-2a の帯からさらに遠ざかる）。すなわち **B の読みが3通りの中で最も本EXPに有利**であり、それでも DS-2a は不合格である。

### 3.7 「非決算」の定義の限界（T-7。**安全側だと扱わない**）

- 本EXPが除外できるのは **`/fins/summary` に載る開示（決算短信等）のみ**である。業績予想の修正・自己株式取得・M&A・上方/下方修正を伴わない適時開示は、**TDnetアドオン（¥11,000/月）未契約のため識別できない**（prescreen §3.2）。
- **混入率の条件付き推定は構造的に不可能である。** 理由を数値で示す:
  - 無償の TDnet インデックスの保持期間は約38日であり、自前スナップショットは **2026-08-06〜** しか存在しない。
  - J-Quants Free の日足は 12週遅延により **`T[487] = 2026-06-22`** で終わる。
  - **両者のカレンダーは1日も重複しない。** したがって「極端下方ギャップだった銘柄日に適時開示があったか」を結合して数えることは、本EXPの検証期間については**原理的にできない**。
- **B実装チームが代わりに出力すること**（§9-8。いずれも記述統計であり合否判定に使わない）:
  - (a) TDnet スナップショット（2026-08-06〜、9,361件）を用いた、**確認期間ユニバース175銘柄における非決算適時開示の無条件発生率**（銘柄日あたり）。これは混入率の**下限の目安**にはなるが、極端ギャップ日に条件付けた率ではない。
  - (b) E-3 によりイベント候補から除外された銘柄日の**割合**（＝極端下方ギャップのうち、決算短信で説明がつく割合）。これは「極端ギャップがどの程度ニュース駆動か」の**識別可能な部分**である。
- **バイアスの向きを断定しない。** 未識別ニュース起因のギャップは「反転しないので効果を希釈する（＝安全側）」と考えるのが自然だが、**ニュースへの過剰反応がむしろ強く反転する可能性を排除できない。** trading-app-v2 EXP-OBS000037 の教訓（曖昧さを「安全側だろう」と推測した結果、実際には安全側でなかった）に従い、**本 spec はバイアスの符号を決めない。** C品質チームはこの未確定性を判定文に明記すること。

---

## 4. 約定モデルの固定

### 4.0 値幅制限フラグ `UL` / `LL` の解釈と約定可否判定関数（**PEAD spec の規約をそのまま流用する**）

**本 spec は `UL` / `LL` を独自に再解釈しない。** `research/EXP-OBS000001/01-spec.md` **§4.0（§4.0.1 意味の確定／§4.0.2 論理的同値性／§4.0.3 パース規則／§4.0.4 判定関数）を本 spec の一部として参照により取り込む。** 要点のみ再掲する（差異があった場合は PEAD spec §4.0 が正）:

```
BUY_BLOCKED(t)  :=  (Vo(t) == 0) or (O(t) is null) or ( UL(t)==1 and O(t) == H(t) )
SELL_BLOCKED(t) :=  (Vo(t) == 0) or (O(t) is null) or ( LL(t)==1 and O(t) == L(t) )
```

- `UL` / `LL` は**価格水準ではなく 0/1 の文字列フラグ**である（`"1"` / `"0"`）。`UL(t)=1` ⇔ その日の高値が制限値幅上限に達した。`LL(t)=1` ⇔ その日の安値が制限値幅下限に達した。
- パース規則: `"1"` → 真、`"0"` / 空文字 / null → 偽。**それ以外の値が1件でも出現したら推測で埋めず S に差し戻す**（N-8）。欠損率が1%を超える場合も差し戻す。
- **JPX 制限値幅表から上限/下限価格を自前で再構成することを禁止する**（PEAD §4.0.4）。
- **方向の非対称性は意図的である**: 買いを阻むのは `UL`、売りを阻むのは `LL` である。**この非対称性を本EXPで反転・変更してはならない。**
- 既知の実測（`feasibility.json` の `9-1b`）: 全245,273行のうち `UL=1` が 185行（0.075%）、`LL=1` が 111行（0.045%）、欠損 0.0%。

> **T-5② との関係（重要・矛盾ではない）**: prescreen T-5② は「`LL` に始値が接している銘柄は約定不能として除外」と要求している。一方 PEAD §4.0.4 の規約では、ストップ安寄り付きは**買い注文を阻まない**（売り手過多のため買いは約定する）。本 spec は両者を次のように整理し、**どちらも歪めない**:
> - **約定可否そのもの**は §4.0.4 の2式のみで判定する（規約を変更しない）。
> - **その上で、本EXP固有の事前登録された追加除外**として、`LL(t_1)=1 かつ O(t_1)=L(t_1)` のイベントを**見送る**（§4.1 の E-8）。理由は約定可否ではなく、**(a) 始値が自由な市場清算価格ではない、(b) 特別気配による寄付遅延で「始値で買えた」という前提が検証不能**という測定上の理由である。
> - 両者を**別の項目として件数を分けて記録する**（§6.5 P-2 / P-2d）。

### 4.1 エントリー（**先読みを構造的に排除する**）

- **エントリーは `D` の翌営業日 `t_1 = T[i+1]` の始値 `O(t_1)` に固定する。**
- **`O(D)` でのエントリーを禁止する（N-4）。** `g(j,D)` は `O(D)` そのものから計算される量であり、`O(D)` で約定できると仮定することは「板寄せの清算価格を、その板寄せに注文を出す前に知っている」ことを意味する。**これは近似ではなく明確な先読みである。**
- 約定価格 = `O(t_1) × (1 + s)`、`s` = 買い片道スリッページ（中心ケース **0.100%**。§8.1）。
- 以下のいずれかに該当する場合は**約定不能として見送る**（翌日以降の再エントリーを行わない）。**件数と内訳を必ず分けて記録する**:
  - **E-7**: `BUY_BLOCKED(t_1)` が真（内訳3分類: `Vo(t_1)=0` / `O(t_1)` が null / `UL(t_1)=1 かつ O(t_1)=H(t_1)`）
  - **E-8**: `LL(t_1)=1 かつ O(t_1)=L(t_1)`（ストップ安寄り付き。§4.0 の但し書き）
  - **E-9**: §5.3 のリスク集中上限・単元離散化により建てられなかった（スロット競合。E-7/E-8 とは別カウント）
- 参考値として `UL(t_1)=1 だが O(t_1)<H(t_1)` で約定したとみなした件数、`LL(t_1)=1 だが O(t_1)>L(t_1)` で約定したとみなした件数も記録する（判定には使わない）。

**エントリーを `t_1` に置くことの代償（正直に記す）**: 文献が報告する反転の大部分は `D` の日中セッションで起きるとされる（prescreen §7.5）。`t_1` 始値エントリーは**その部分を構造的に捨てる**。すなわち本 spec の設計は文献の効果に対して**厳しい側に倒れている**。これは意図的であり、**この厳しさを理由にエントリー日を `D` に変更することを禁止する**（N-4。物理的に不可能であるため、そもそも代替設計ではない）。

### 4.2 予測単位（G1）の前方リターン

- **`R_5(j,D) = C(j, t_5) / O(j, t_1) − 1`**（**グロス**。コスト・スリッページを一切引かない）。`t_k = T[i+k]`。
- 前方窓が確保できないイベント（`i > 482`）は G1 から**除外**し、件数を記録する（§2.4）。
- `t_1` が取れないイベント（E-7 / E-8）は G1 から除外する。**ただし G1 は「取引可能性」ではなく「予測力」の検定であるため、除外した場合と `O(t_1)` を用いて仮想的に含めた場合の両方の `IC_bar` を出力すること**（主判定は除外版）。PEAD §4.2 と同じ扱い。
- **市場超過リターン**: `R_5^excess(j,D) = R_5(j,D) − (同一カレンダー窓 `t_1`〜`t_5` におけるユニバース全銘柄の等加重平均 `R_5`)`。TOPIX 日足が現行契約で取得不可（HTTP 403）であるため、ユニバース等加重平均を市場プロキシとする（PEAD §6.3 と同一）。
- **セクター中立リターン**: `R_5(j,D) − (同一 `S33` 業種のユニバース構成銘柄の等加重平均 `R_5` over 同一窓)`。**生データであり合否判定に使わない。**

> **注（回す前に確定しておく数学的事実）**: 同一イベント日 `D` に属する観測はすべて同一のカレンダー窓 `t_1`〜`t_5` を持つ（欠損営業日率 0.0% の実測による）。したがって市場プロキシの減算は**日内で定数**であり、**日内 Spearman 順位相関を変化させない。** §6.1 の `IC_bar` は `R_5` でも `R_5^excess` でも同値である。B実装チームは `R_5^excess` を用い、同値性を数値で確認して `prediction-unit.json` に出力すること（食い違ったら S に差し戻す）。

### 4.3 決済ルール（トレーリングストップ。PJ000001 C-1 決着／司令塔判断3 に準拠）

4パラメータをすべて数値固定する。

| # | パラメータ | 固定値 | 根拠 |
|---|---|---|---|
| 1 | **初期損切幅** | エントリー約定価格の **−4.0%（固定値）** | prescreen §7.2。**ATR連動にしない**（ATR はテクニカル指標であり必須条件①／N-1 に抵触する。加えて新しい乗数パラメータと第2のルックバック窓を導入し EXP-OBS000037 型の曖昧さを増やす）。prescreen §7.2 が「下方ギャップ銘柄は −2.0% では即座に刈られる」として −4.0% を要求した水準をそのまま採る |
| 2 | **トレール発動水準** | 終値ベースの含み益が **+4.0%** 以上に到達した日の**翌営業日から**有効化 | PEAD §4.3 と同一構造 |
| 3 | **追随幅** | 発動後、`t_1` 以降の**日次終値**の最高値から **−2.0%** | 同上 |
| 4 | **更新足と約定価格** | **日足終値のみ**でストップ水準を更新し、**日足終値のみ**で抵触判定する。抵触した日の**翌営業日始値**で約定したとみなす | 日中高値でトレール判定して当日中に約定できたとみなす楽観を構造的に排除する（PJ000001 C-1 決着の警告への対応） |

- 日中高値・日中安値（`H` / `L`）は**トレールの更新にも抵触判定にも使わない。**
- 初期損切も同じ扱い: `C(t) ≤ エントリー約定価格 × 0.96` なら**翌営業日始値**で手仕舞い。
- 決済時の約定価格 = `O(t_exit) × (1 − s)`。
- **最大保有5営業日**: `t_5` の終値時点で未決済なら `t_6` の始値で強制手仕舞い（§4.5）。
- **予想される挙動について（回す前の正直な見立て）**: 本EXPが狙う効果量はトレードあたり数十bp〜数%であり、**トレール発動水準 +4.0% に到達するトレードは少数にとどまる可能性が高い。** その場合、実質的な決済は「初期損切 −4.0%」と「5営業日の時間切れ」の2種類に収斂する。**これは設計の欠陥ではなく、5営業日という短い保有期間と司令塔判断3（固定利確幅を置かない）の当然の帰結である。** §6.5 P-6（決済理由の内訳）・P-7（トレール発動率）で実測する。**この見立てを理由にパラメータを事後に変更することを禁止する**（N-9）。

### 4.4 決済できない場合（値幅制限の模擬）

- 手仕舞い日 `t_exit` について **`SELL_BLOCKED(t_exit)` が真の場合、約定できず持ち越し**とする。
- 持ち越し中は、その後の各営業日 `t` について `SELL_BLOCKED(t)` を同じ式で再評価し、偽となった最初の営業日の始値で約定する。その間の下落はすべて損失として計上する。
- **最大5営業日**持ち越しても約定できない場合、5営業日目の終値で強制的に評価損益を確定し、「強制評価」としてフラグを立てて件数を記録する。
- 本ルールは、初期損切・トレール抵触・最大保有超過（`t_6` 始値）・§7 D項の決算跨ぎ回避・§5.3-10 のサーキットブレーカー清算を含む、**すべての売り約定に一律に適用する。**
- 内訳を3分類で記録する（`Vo=0` / `O` が null / `LL=1 かつ O=L`）。参考値として `LL=1 だが O>L` で約定したとみなした件数も記録する。
- **`UL(t_exit)=1`（ストップ高）は売り約定を妨げない。**

### 4.5 窓長・ラグ・オフセットの算術的整合性（**T-8。trading-app-v2 EXP-OBS000037 の教訓**）

**以下の対応関係を、実装前に必ず確認すること。曖昧な箇所を独自解釈で埋めてはならない（N-8）。** 「本数」「価格点数」「日付レンジ」が**すべて算術的に整合していること**を、B実装チームが自ら検算し `params.json` に出力する。

| 用語 | 日足の本数 | 使う価格の点数 | 日付レンジ（`D = T[i]`） | 系列の本数 |
|---|---|---|---|---|
| **ギャップ `g(j,D)`** | 2本（`T[i−1]`, `T[i]`） | 終値1個 `C(T[i−1])` ＋ 始値1個 `O(T[i])` = **2点** | `T[i−1]` 〜 `T[i]` | ギャップ **1本** |
| **`σ_gap` の窓**（第2版で改訂） | 探索範囲は **66本**（`T[i−66]` 〜 `T[i−1]`）。そのうち**採用するのはちょうど60本**（新しい順・有効ギャップのみ） | 始値 **60個** ＋ 終値 **60個** = **120点**。**採用した60本のギャップ `g(j,T[m])` それぞれが、始値 `O(j,T[m])` 1個と終値 `C(j,T[m−1])` 1個の計2点から成る**（∴ 60本 ⇔ 60始値＋60終値。**隣接とは限らないため「終値61個」ではない**） | 採用日は `T[i−66]` 〜 `T[i−1]` の部分集合。使う終値の日付は各採用日の**直前営業日**であり、最も古い場合で `T[i−67]` | ギャップ **60本** |
| **`σ_gap` が要求する最小添字** | — | — | 最古の採用候補 `T[i−66]` のギャップが終値 `C(T[i−67])` を要する ⇒ `i − 67 ≥ 1` ⇒ **`i ≥ 68`** | — |
| **`σ_gap` の遡り上限 66 の根拠** | — | — | 実測（候補511銘柄・全銘柄日）: 60本の有効ギャップ収集に必要な遡り営業日数 `K` は **最大 65**、`K ≤ 66` でカバー率 **100.0000%**（取りこぼし 0 件） | — |
| **予測単位 H=5（主）** | `t_1`〜`t_5` の **5本** | 始値1個 `O(t_1)` ＋ 終値5個 `C(t_1)`〜`C(t_5)` = **6点** | `T[i+1]` 〜 `T[i+5]` | 日次リターン **5本**（6点の隣接差分 = 6−1 = 5 ✓） |
| **予測単位 H=3（SA）** | 3本 | 始値1個 ＋ 終値3個 = **4点** | `T[i+1]` 〜 `T[i+3]` | **3本**（4−1=3 ✓） |
| **予測単位 H=10（SA）** | 10本 | 始値1個 ＋ 終値10個 = **11点** | `T[i+1]` 〜 `T[i+10]` | **10本**（11−1=10 ✓） |
| **パイプライン最大保有** | `t_1`〜`t_5` の **5本**（保有判定に使う日足）＋ 強制決済日 `t_6` の始値 | 始値1個 `O(t_1)` ＋ 終値5個 `C(t_1)`〜`C(t_5)` ＋ 始値1個 `O(t_6)` = **7点** | エントリー `T[i+1]`、最終決済 `T[i+6]` | `O(t_1)→C(t_1)` から `C(t_4)→C(t_5)` まで **5本**、加えて `C(t_5)→O(t_6)` の決済ギャップ **1本** = 合計 **6本**（7−1=6 ✓） |
| **トレール最高値の参照範囲** | `t_1` 〜 抵触判定日 `t_k` | 終値のみ **k個**（`C(t_1)`〜`C(t_k)`） | `T[i+1]` 〜 `T[i+k]` | — |
| **U-4 流動性窓** | **60本** | 銘柄あたり `Va` **60個** | 確定日 `D_u` を**含まない**直前60本 | — |

- **「N営業日保有」は、`t_1` から数えて N本の日足を保有判定に使うことを意味する。** 決済約定はその翌営業日 `t_{N+1}` の始値である（＝価格点は N+2 個になる）。
- **`D` の日足は損益計算に一切入らない。** `D` の `O` は `g` の計算に、`D` の終値 `C(D)` は §5.3-3 の単元数決定にのみ使う。
- 営業日の定義: **日足データにその銘柄の行が存在する日**とする（売買停止で行が存在しない日は営業日として数えない。件数を記録）。
- **`σ_gap` の窓について「取得できた分で代用する」「窓長を短縮する」「遡り上限 66 を延ばす」ことを禁止する。** 有効ギャップ60本ちょうどが `T[i−66]`〜`T[i−1]` の範囲に収まらない場合は**イベント候補から除外**する（§3.4 C-2）。窓長・上限の変更が必要と考えた場合は S に差し戻す（N-8）。
- **「60本」は常にギャップの本数を指し、営業日数でも価格点数でもない。** 採用した60本は隣接とは限らないため、**「60本のギャップ ⇔ 終値61個」という第1版の対応関係は第2版では成立しない**（隣接の場合にのみ成立した特殊ケースであった）。正しい対応は **60本 ⇔ 始値60個 ＋ 終値60個** である。**この対応関係を実装者が独自に読み替えてはならない**（T-8。EXP-OBS000037 で窓長が独自解釈された経路そのもの）。

### 4.6 寄付の約定モデルの妥当性は日足では検証できない（T-5④。**必ず明記して申し送る**）

- 本EXPは「板寄せの始値で、目標建玉25万円を、片道0.10%のスリッページで買える」という前提に全面的に依拠する。
- しかし **(a) 自分の成行注文は板寄せの清算価格の形成に参加する、(b) 極端ギャップ銘柄は板が薄い、(c) 特別気配により寄付が遅延する**という3点は、**日足データからは原理的に測定できない**（prescreen §4.4）。
- **したがって G1・G2 の合格は「日足上では成立する」ことしか意味しない。** 本EXPの採用可否に対し、**STEP3 フォワード較正における寄付約定の実測（想定約定価格と実約定価格の乖離分布）を必須条件とする。** C品質チームは、G1・G2 が合格した場合でも判定文にこの条件を明記すること。

---

## 5. パイプライン統合の設計

### 5.1 シグナル（必須条件①の実装）

- ロング候補 = §3.4 の C-1〜C-4・E-1〜E-6 をすべて満たすイベント候補。
- **テクニカル指標を一切参照しない。** MACD / RSI / ボリンジャーバンド / CCI / 移動平均 / ADX / ATR / Stochastic / 出来高指標のいずれも、エントリー・エグジット・フィルタ・サイジングの判断に用いない。
- **PEAD（SUE）・その他いかなるシグナルとも合成・ゲートしない。** 在庫ゼロのため足し算する相手が物理的に存在しないが、それを理由に緩めない（N-2）。
- 決済は §4.3 のトレーリングストップおよび §4.5 の最大保有5営業日のみ。

### 5.2 方向（必須条件③）

- **ロングオンリー。** 上方ギャップのショートは本EXPでは**組まない**（逆日歩・貸株規制が事前算定不能でコストモデルが成立しない。prescreen §0.4）。
- **上方ギャップ（`S` が負側、すなわち `z ≥ +|z*|`）の予測単位のみ**（グロス平均リターン・件数）は生データとして出力する。**この結果を根拠に G1 を合格にしてはならない**（N-10）。

### 5.3 建玉・リスク集中上限（T-4）

| # | 項目 | 固定値 | 根拠 |
|---|---|---|---|
| 1 | 運用資本 | 1,000,000円 | PJ000001 司令塔判断1 |
| 2 | 1銘柄あたり目標建玉 | 250,000円 | prescreen §4.6 |
| 3 | 単元数の決定 | `u = max(1, round(250,000 / (C(D) × 100)))`（四捨五入・半数切り上げ）。`u × C(D) × 100 > 375,000` なら `u` を1減らす。それでも 375,000 を超えるなら**見送り**（件数記録）。**基準となる終値は `C(D)`**（イベント日の終値。`t_1` 始値エントリーより前に確定しており先読みにならない） | 単元100株の離散性 |
| 4 | 1銘柄あたり建玉上限 | 375,000円 | 目標の1.5倍 |
| 5 | **同時保有銘柄数上限** | **5銘柄** | prescreen §4.7 は「8ではなく6以下」を要求。PEAD §5.3-5 と同じ 5 を採り、実効 f=0.25 が Kelly の約2倍であることへの対応とする |
| 6 | **建玉総額上限** | **1,250,000円**（実効レバレッジ 1.25倍） | 5銘柄 × 25万円。同時に −10% を被弾しても損失 12.5万円 = 資本の12.5% で、PJ000001 §8.1 の DD 上限15%を単発で突破しない |
| 7 | **同一 `S33` 業種の同時建玉上限** | **2銘柄** | セクター共通要因の同時被弾抑制 |
| 8 | **同一イベント日 `D` に由来する新規建て上限** | **2銘柄** | **OBS000029 の時間方向再発の防止（T-4①）。** 市場急落日に全枠が同一方向で埋まる経路を数値で塞ぐ。<br>**容量の検算**: 2件/日 × 245日 = 490件/年 ≫ 想定215件/年 であり、平均的には制約にならない。制約が効くのは**イベントが集中した日だけ**であり、これは意図した効果である |
| 9 | 候補が上限を超える場合の優先順位 | `S`（＝`−z`）の**降順**（深いギャップ優先）→ 同値なら `Code` 昇順（決定的） | 再現性 |
| 10 | サーキットブレーカー | 日次エクイティの直近ピークからの下落率が **10%** 到達で新規建て停止（DDが10%未満に回復するまで）。**15%** 到達日に全ポジションを翌営業日始値で清算し、**その日でバックテストを打ち切る**（自動再開しない） | PJ000001 §8.1 |

- 見送った候補はすべて**スロット競合**として件数・上限別の内訳を記録する（P-3）。
- 各トレードについて、**離散化後の実効建玉金額／目標建玉金額**と**実効 f（建玉金額 ÷ 資本）**を毎回記録する（P-5）。

### 5.4 ユニバース定義（**PEAD spec §5.4 を一字一句そのまま流用する**）

**`research/EXP-OBS000001/01-spec.md` §5.4（U-1〜U-7 および「U-4 の窓に関する補則」）を本 spec の一部として参照により取り込む。** 要点:

| 手順 | 条件 |
|---|---|
| U-1 | 規模区分が **TOPIX500 相当**（`ScaleCat`） |
| U-2 | **制度信用で買建て可能**（`Mrgn` / `MrgnNm`。信用取引不可を除外、貸借銘柄は含める） |
| U-3 | 業種区分 `S33` / `S33Nm` を記録（§5.3-7 のセクター上限と §4.2 のセクター中立リターンに使用） |
| U-4 | 確定日 `D_u` の**直前60営業日**（`D_u` を含まない）の**日次売買代金 `Va` の中央値 ≥ 5億円**。有効な `Va` が30個未満の銘柄は不通過 |
| U-5 | 確定日 `D_u` 時点の終値が **1,000円以上 3,500円以下** |
| U-6 | 175を超える場合、売買代金中央値の降順で**上位175銘柄** |
| U-7 | 150未満の場合、株価帯を **700円以上 8,000円以下**に**一度だけ**緩和。それでも150未満なら**ユニバース不成立として S に差し戻す** |

- **確認期間のユニバースは、PEAD が 2026-09-13 に実測・確定した175銘柄（`research/EXP-OBS000001/10-result/universe.json` の `confirmation_universe`）をそのまま用いる。** 確定日 2025-07-01、候補511・U-1/U-2通過497・主株価帯通過271・上位175に打ち切り、`missing_bars_codes` 空・欠損率 0.0%。
  - **これは規則の再定義ではなく、同一規則の再利用である。** U-1〜U-7 は前方リターンを一切参照しないため、流用に先読みは生じない。
  - B実装チームは、**流用したコードリストと、本EXPのデータから U-1〜U-7 を再適用して得たリストが完全一致することを §9-4 で確認する**（一致しなければ S に差し戻す）。
- **選定期間のユニバースは `D_u = T[61]` で新規に構築する**（PEAD 側は未構築のまま凍結されている）。U-4 の窓は `T[1]`〜`T[60]` のちょうど60営業日となる。150銘柄未満なら S に差し戻す。
- **ユニバースを TOPIX500 相当の外へ広げて有効 n を稼ぐことを禁止する**（trading-app-v2 OBS000030・N-5）。U-7 の緩和は株価帯のみ。
- 生存バイアス防止: 期間中に上場廃止・売買停止となった銘柄は、**発生日以降のイベントのみ除外**し、遡及的にユニバースから外さない。

---

## 6. 成功 / 不採用の数値基準（**回す前に確定**）

### 6.0 データ十分性ゲート（**リターンを見る前に評価する**）

以下を**すべて**満たさない場合、G1 は「**判定不能**」とし、**Kill ではなく S へ差し戻す**（K-5）。**この判定はイベント件数・銘柄数・日数・欠損率のみを入力とし、リターンを一切参照せずに行う**（PEAD K-5 / N-10 と同じ不可逆性の配慮）。

| # | 条件 | 閾値 | 根拠 |
|---|---|---|---|
| **DS-1** | 確認期間の**プール**観測数（§6.1 の `z ≤ −1.5`・全除外後） | **≥ 1,500件** | 日次ブロック Spearman の母集団 |
| **DS-2a** | 選定期間で `N_ann(z*) ∈ [150, 300]` を満たすグリッド点が**存在する** | 存在すること | T-1。存在しなければ S に差し戻す |
| **DS-2b** | **確認期間**の候補イベント年換算件数 `N_ann` | **100 ≤ N_ann ≤ 400** | T-1。400を超えると建玉枠（5枠×245÷5＝245件/年）に対し件数が過大となり、**イベント駆動から常時フル投資へ退化して EXP-OBS000002 と同じコスト構造で死ぬ**（prescreen §5.3）。100 未満は検出力不足。**範囲外なら G1 へ進まず S に差し戻す** |
| **DS-3** | 確認期間のユニバース銘柄数 | **≥ 150銘柄** | PEAD DS-3 と同一（実測175） |
| **DS-4** | ユニバース × 全期間の日足の欠損営業日率 | **≤ 5%** | PEAD DS-4 と同一（実測 0.0%） |
| **DS-5** | 確認期間の候補イベントが属する**相異なるイベント日数** `K_event` | **≥ 80日** | §6.2 G1-3 の水準検定のブロック数。イベントが少数日に集中していれば、件数が足りていても検定は成立しない（**OBS000029 の時間方向再発をゲートで検知する**） |
| **DS-6** | 確認期間の**プール**日次ブロックのうち、**1日あたり5件以上**を持つ日数 `K` | **≥ 100日** | §6.1 `IC_bar` の算出対象日数。PEAD が DS-2（実測27日）で座礁した箇所に相当する |
| **DS-7** | 確認期間の候補イベント総件数に対する、**最大の1日**が占める件数シェア | **≤ 25%** | **第2版で新設。** S の実測（候補511銘柄・除外規則未適用の上限値）では、`z* = −2.5` において単日 `2025-04-07` が全イベントの **10.7%**、上位3日で **27%** を占めた。イベントが単一日に集中している場合、`IC_bar` が日等加重であっても、またパイプラインが §5.3 で 1日2件に制限されていても、**「その1日に何が起きたか」以上のことを本EXPは測定していない**（OBS000029 の時間方向再発）。**25% を超えたら G1 へ進まず S に差し戻す**（K-5） |

- B実装チームは §9-6 の先行タスクとしてこれらのカウントを先に出力すること。**DS ゲート未達のまま G1 以降を実行することを禁止する**（N-11）。
- **DS-1〜DS-7 はすべてカウントのみを入力とし、リターンを一切参照せずに評価する。**

### 6.0.1 DS ゲートの実測結果と S戦略チームの判定（**第3版で新設・記録専用**）

**2026-09-14、B実装チームが §9-1〜§9-9 を実行し、§9-6 で停止した**（`10-result/feasibility.json` `9-6_z_star_calibration_and_ds_gates`、`run.log`）。**確認期間の前方リターンは一度も参照されていない。**

| # | 実測値 | 閾値 | 合否 |
|---|---|---|---|
| DS-1 | 確認期間プール（`z ≤ −1.5`）**1,586件** | ≥ 1,500件 | 合格 |
| **DS-2a** | 事前登録グリッド11点の `N_ann` は **356.12 〜 1,800.68**。`[150, 300]` に入る点は**存在しない**（最も近い `z*=−4.00` でも 356.12） | 存在すること | **不合格** |
| DS-2b | 凍結 `z*=−4.00` の確認期間 `N_ann` = **109.58** | `[100, 400]` | 合格 |
| DS-3 | 確認期間ユニバース **175銘柄** | ≥ 150銘柄 | 合格 |
| DS-4 | 欠損営業日率 **0.0%** | ≤ 5% | 合格 |
| **DS-5** | 確認期間の相異なるイベント日数 `K_event` = **25日** | ≥ 80日 | **不合格** |
| **DS-6** | 確認期間のプール日次ブロックのうち5件以上を持つ日数 `K` = **62日**（プール1,586件は相異なる **167日**に分布） | ≥ 100日 | **不合格** |
| **DS-7** | 最大単日イベントシェア **52.83%**（`2026-03-09` に 56/106件） | ≤ 25% | **不合格** |

**⇒ §6.8 K-5 により、Kill ではなく「判定不能・保留」。** 以下、S戦略チームが検討した再設計の可否を記録する。

#### (a) 決定的根拠: **DS-6 は `z*` に依存しない量であり、較正をどう引き直しても動かない**

DS-6 の計数対象は**イベント集合 `E` ではなくプール `P`** である。`P` は §6.1.1 により `z ≤ −1.5` で定義され、この `−1.5` は**回す前に固定した値であって結果を見て動かさない**（N-9）。`z*` は `P` の定義に一切入らない。

> **したがって `z*` の較正グリッドを −4.25 まで延ばそうと、刻みを細かくしようと、目標件数215を変えようと、DS-6 の実測値は 62日のままである。** `z*` に関わるいかなる再設計も §6.0 を充足させることはできない。**これは推測ではなく、DS-6 の定義から導かれる論理的帰結である。**

#### (b) DS-2a の修復は可能だが、無意味である

DS-2a の不合格は、**事前登録グリッドの設計が実測のテールの厚さに対して誤っていた**ことの表面化であり、この事実自体は**選定期間のみの情報から確認できる**（確認期間を見る必要がない）。実測の `z` 分位点は以下のとおりで、標準正規を下半分に切断した集合の理論分位点（p01 ≈ −2.58 / p05 ≈ −1.96）から大きく外れている。

| 期間 | `z` の p01 | `z` の p05 | `z` の最小値 | 候補件数 |
|---|---|---|---|---|
| 選定 | **−6.235** | −2.624 | −15.87 | 11,679 |
| 確認 | **−3.689** | −2.075 | −22.50 | 14,958 |

グリッド上限を `−1.50` とし、目標を 215件/年としたのは、**テールが概ね正規に近いという暗黙の前提**に立っていた。実測では選定期間の `z ≤ −4.00` が候補の **2.28%**（266/11,679）を占める。**この前提の誤りはグリッドを −5.0 程度まで延ばせば形式的には修復できる。**

**しかし修復しても意味がない。** より極端な `z*` は (i) イベント件数を減らし、(ii) 相異なるイベント日数 `K_event`（DS-5）をさらに減らし、(iii) 単日集中シェア（DS-7）をさらに高める。**DS-2a の修復は DS-5・DS-7 を一方的に悪化させる方向にしか働かない。** そのうえ (a) により DS-6 は不変である。

#### (c) DS-5・DS-7 を満たす方向の再較正は、S の職務として**行わない**

DS-5（相異なる日数を増やす）と DS-7（単日シェアを下げる）を改善するには `z*` を**緩める**しかないが、それは DS-2b の上限 400件/年と逆向きに働く。**両立する `z*` の窓が存在するか否かは、確認期間の中間 `z` における件数分布を測らなければ分からない。**

> **S はこれを測らない。** 確認期間のカウント分布を観測した上で「そこを通るように」較正グリッドを引き直すことは、入力がリターンでなくカウントであっても、**「結果を見てから設計を選ぶ」ことそのもの**であり、K-4（HARKing）と外形上区別できない。§3.6 が `z*` の較正を**選定期間のみ**に閉じ込めた設計意図を破る。
> **加えて、仮にその窓が存在したとしても (a) により DS-6 は 62日のままで不合格である。** すなわち測る動機も存在しない。

#### (d) 選定/確認の分割を解消して DS-6 の母数を稼ぐ案 — **禁止**

確認期間だけで 62日なら、選定期間（183営業日・プール1,345件）を合算すれば 100日に届く可能性がある。**しかしこれは採らない。** 選定データと確認データの分離は PJ000002 §6 の鉄則であり、trading-app-v2 の8連敗が確立した偽陽性対策の中核そのものである。合算すれば `z*` は in-sample 較正になり、本EXPが持つ唯一の自由度が未見データの保護を失う。**DS-6 が不合格と判明した後にこれを行えば、動機の純粋さにかかわらず K-4 である。**

#### (e) イベント定義・母集団の取り方の変更 — **本EXPの改訂としては不可。新規EXPとしてのみ可能だが、事前確率は改善しない**

検討した3案と結論:

| 案 | 内容 | 結論 |
|---|---|---|
| **R1** | **市場共通因子を除いた残差ギャップ**で `z` を定義する（`g` から市場成分を差し引いてから正規化） | 集中の原因に直接効くが、**§3.2 の事前登録シグナル定義そのものの変更**であり、PJ000002 §6 により**本 spec の改訂ではなく新規EXP**である。かつ**事前確率は本EXPより低い**（下記） |
| **R2** | **1日あたりのイベント数を定義側で上限する**（各日の `z` 最深 k 件のみをイベントとする） | DS-5・DS-7 は改善しうるが、**DS-6 はプールの量であり全く動かない**（(a)）。無効 |
| **R3** | **プール下限 `−1.5` を緩めて DS-6 の対象日を増やす** | **N-9 が明示的に禁止**（「特に `z*`、**プール下限 −1.5**」）。K-4 |

> **R1 の事前確率が改善しない理由**: 本EXPが賭けていたのは prescreen §7.6 のとおり「**市場ストレス時の売り需給の偏りが数営業日かけて解消する**」ことであり、**賭けの対象そのものが市場共通因子**である。残差ギャップ版は共通因子を取り除いた残りに賭ける設計であり、賭けの中心を外している。効果量は prescreen §7.5 が「必要グロス0.45%に対し6.4倍不足」として却下した文献値（0.07%）の側へ寄ると考えるのが自然であり、**損益分岐 0.45%/トレードに対してより不利になる。** R1 を起票するには、この点を prescreen で正面から論じ、日本株固有の根拠を示す必要がある（CLAUDE.md「同型のアイデアを起票する場合は…」）。

#### (f) 実測が明らかにした構造（**本EXPの主要な産物。後継EXPへ引き継ぐ**）

1. **日本株のオーバーナイト・ギャップは市場共通因子に支配されており、`σ` 正規化した極端下方ギャップは「日ごとにばらける点過程」ではなく「少数の市場ストレス日に集中する群発過程」である。** 実測: 確認期間のプール1,586件が相異なる167日に分布し、そのうち5件以上を持つ日は62日しかない。イベント集合では106件が25日に集中し、単日 `2026-03-09` が 56件（52.8%）を占める。
2. **選定期間と確認期間のテール分布が構造的に異なる。** `z` の p01 は選定 **−6.235** / 確認 **−3.689**。`z ≤ −4.00` の候補比率は選定 **2.28%**（266/11,679）に対し確認 **0.71%**（106/14,958）で **3.2倍**の差がある。**選定期間で較正した `z*` は確認期間へ転送されない。** これは §3.6 の較正設計が依拠していた「イベント発生率が期間をまたいで概ね一定」という暗黙の前提の反証である。原因は選定期間が2025年4月の市場ストレス局面（`2025-04-07` 中央値ギャップ −7.26%）を含み、確認期間のストレス日が実質 `2026-03-09` の1日であることにある。
3. **§6.1.4(a) が検出力計算の前提とした「`K ≈ 180日`・`n_d ≈ 10`」は実データから大きく外れている**（実測 `K = 62日`）。**ただしこれをもって DS-6 の閾値100を再計算・再設定することはしない**（N-9・K-4）。PEAD spec §6.1.4 が DS-2 について記した「閾値は `K ≈ 60` の仮定の下で設定されたものであり、実データはその前提から大きく外れている」という所見と、**本EXPは同じ壁の別の面に当たっている。**

#### (g) 結論

**本 spec の枠内で §6.0 を充足させる再設計は存在しない。** 根拠は (a)（DS-6 の `z*` 非依存性）であり、これは実測値と DS-6 の定義のみから導かれ、推測を含まない。

**本EXPは §6.8 K-5 に従い「判定不能・保留（凍結）」とする。不採用ではない。** 確認期間（2025-07-01〜）のリターンは未見のまま温存されており、データ拡張の目処が立てば再開できる。**`portfolio-ledger.md` の「不採用」に記録してはならない**（エッジの不在を示す証拠を一切得ていないため）。

**司令塔判断に上げる事項は §13 の D-9・D-10 に記す。**

### 6.1 G1 主検定（必須条件④・T-2）

#### 6.1.1 測定母集団

- **プール `P`**（連続値IC・デシル分析の母集団）: §3.4 の C-1〜C-3・E-1〜E-6 をすべて満たし、**`z ≤ −1.5`** を満たす銘柄日。**`−1.5` は回す前に固定した値であり、結果を見て動かさない**（N-9）。
- **イベント集合 `E`**（水準検定・パイプラインの母集団）: プールのうち **`z ≤ z*`**（§3.6 で較正・凍結）を満たすもの。`E ⊆ P` である。

> **なぜ母集団を2段にするか**: T-2 は「主指標は閾値ではなく `z` の連続値に対する Spearman IC」と要求する（OBS000018 対策）。一方で必須条件⑤は「実際に建てる集合のグロス平均 ≥ 0.45%」を要求する。前者は**プール全体での単調性**を、後者は**イベント集合での水準**を問うものであり、母集団が異なるのは設計上必然である。両者を同時に課すことで、「閾値を1点に決め打ちしたら偶然良かった」（OBS000018 の蜃気楼）と「単調ではあるが水準が経済的に足りない」の**両方を落とす**。

#### 6.1.2 主指標: イベント日内クロスセクション Spearman 順位相関の等加重平均（`IC_bar`）

1. 確認期間の各イベント日 `d` について、その日にプール観測を持つ銘柄群の **`S`（＝`−z`）と `R_5^excess`（§4.2）の Spearman 順位相関 `IC_d`** を算出する。タイは平均順位で処理する。
2. **1日あたりのプール観測数が5件未満の日は `IC_bar` の算出から除外**する（除外日数・除外観測数を記録）。残った日数が **`K`（＝DS-6 の計数対象と同一の量）**である。**両者が食い違う場合は S に差し戻す。**
3. **`IC_bar` = 対象日の `IC_d` の単純平均**（日を等加重。観測数の多い日に引っ張られないようにする）。
4. **有意性検定 = イベント日ブロック内 permutation**:
   - 帰無仮説: 「同一日の銘柄間で、`S` の順位は前方リターンの順位を説明しない」
   - 手続き: **同一イベント日ブロックの内部でのみ `S` をシャッフル**し（ブロック間の入れ替えは行わない）、`IC_bar` を再計算する。**10,000回**繰り返す。
   - 乱数シード: **20260914**（固定。変更禁止）
   - 片側 p 値 = ( 1 + #{ `IC_bar_perm` ≥ `IC_bar_obs` } ) / ( 1 + 10,000 )
   - **日内でのみ並べ替えることにより、同日の銘柄が共有するマクロ・市場全体の共通要因が p 値を過小評価させる問題を構造的に排除する**（本EXPは市場急落日にイベントが集中するため、この配慮は PEAD 以上に重要である）。

#### 6.1.3 水準検定（G1-3 の統計量。**p 値は報告するが合否ゲートにはしない**）

1. 確認期間の各イベント日 `d` について、その日のイベント集合 `E` の `R_5^excess` の**等加重平均** `M_d` を算出する。
2. **`M_bar` = 対象日の `M_d` の単純平均**（日を等加重）。対象日数は `K_event`（＝DS-5）。
3. **ラベル permutation**: 各イベント日 `d` について、その日に `t_1`〜`t_5` が確保できるユニバース銘柄の中から `k_d` 銘柄（＝その日の実イベント数）を**無作為抽出**して仮イベント集合とし、`M_bar` を再計算する。**10,000回**、シード **20260914**。
4. 片側 p 値を報告する。**この p 値は G1 の合否ゲートではない**（理由は §6.1.4 の検出力開示）。

#### 6.1.4 検出力の事前開示（MDE。**基準を緩める根拠にも結果を解釈し直す根拠にも使ってはならない**）

**(a) `IC_bar`（G1-1）**

- プールをユニバースの約6%（175銘柄 × 6% ≈ 10.5銘柄/日）と仮定し `n_d ≈ 10`。`SD(IC_d) ≈ 1/√(n_d − 1) ≈ 0.333`。
- 想定 `K ≈ 180日` ⇒ `SE(IC_bar) ≈ 0.333/√180 ≈ 0.0248` ⇒ **MDE ≈ 1.645 × 0.0248 ≈ 0.041**
- DS-6 の下限 `K = 100日` でも ⇒ `SE ≈ 0.0333` ⇒ **MDE ≈ 0.055**
- **PEAD の実測 MDE（0.11〜0.13）の半分以下であり、本EXPの検出力は本プロジェクトで最も高い**（prescreen §8.1-2）。**したがって本EXPが G1 未達となった場合、それは「検出力が足りない」ではなく「エッジが無い」証拠として `portfolio-ledger.md` に不採用で記録できる。** これは本EXPの主要な価値の1つである。

**(b) 水準 `M_bar`（G1-3 の統計量）**

- 1イベントあたり `R_5^excess` の標準偏差を **4.0%** と仮定（5営業日の個別株リターン σ ≈ 日次2.0%×√5 = 4.47% から市場成分を除いた概算）。
- 1日あたりイベント数 `k_d ≈ 2.5` ⇒ 日次平均の SD ≈ `4.0/√2.5 ≈ 2.53%`
- 想定 `K_event ≈ 120日` ⇒ `SE ≈ 2.53/√120 ≈ 0.231%` ⇒ **MDE ≈ 0.38%**
- DS-5 の下限 `K_event = 80日` ⇒ `SE ≈ 0.283%` ⇒ **MDE ≈ 0.47%**
- **⇒ ゲート下限では、検出可能な最小効果（0.47%）が経済的に必要な最小効果（0.45%）をわずかに上回る。** すなわち**損益分岐ぎりぎりのエッジは統計的に検出できない。** この限界を**回す前に受け入れる。**
- **この開示が G1-3 を「点推定の閾値」として設計した理由である。** 水準に有意性を要求すると、経済的に必要な水準と統計的に検出可能な水準がほぼ一致してしまい、ゲートが二重になって検出力をさらに削る。必須条件⑤が求めているのは「統計的に有意でも 0.45% 未満なら不採用」であって「0.45% 以上かつ有意」ではない。

- **B実装チームは実測の `K` / `K_event` / `n_d` / `k_d` / permutation 帰無分布の標準偏差を出力すること**（C品質チームが実測 MDE を再計算できるようにするため）。

### 6.2 G1 合格条件（**すべて満たすこと**）

| # | 条件 | 閾値 |
|---|---|---|
| **G1-1** | 確認期間の `IC_bar`（§6.1.2） | **> 0 かつ イベント日ブロック内 permutation の片側 p < 0.05** |
| **G1-2** | 選定期間の `IC_bar` | **> 0**（符号一致。有意性は問わない） |
| **G1-3** | 確認期間のイベント集合 `E` の `R_5` **グロス平均**（イベント等加重） | **≥ +0.45%**（prescreen §7.4 の損益分岐。**必須条件⑤**。統計的に有意でもこれを下回れば不採用） |
| **G1-4** | 確認期間のイベント集合 `E` の **市場超過** `R_5` 平均 `M_bar`（日等加重・§6.1.3） | **> 0**（市場全体の反発だけで合格することを防ぐ） |
| **G1-5** | プール内**デシル単調性**（§6.3） | **`S` のデシル番号とデシル平均 `R_5^excess` の Spearman 順位相関 `ρ_dec` ≥ +0.30** |
| **G1-6** | 選定期間のイベント集合 `E` の `R_5` グロス平均 | **> 0**（符号一致。水準は問わない） |
| **G1-7** | **単日依存性の除去**（第2版で新設）。確認期間から**イベント件数が最大の1日を丸ごと除いて** `IC_bar` と `R_5` グロス平均を再計算する | **`IC_bar` の符号が正のまま かつ `R_5` グロス平均が > 0**（水準・有意性は問わない） |

**G1-1 〜 G1-7 のいずれか1つでも未達なら G1 不合格とし、G2 以降は測定しない**（PJ000001 §5.3・N-12）。

> **G1-7 を必須にする理由（OBS000029 対策の核心・第2版で追加）**: 本EXPのイベントは市場全体のストレス日に構造的に集中する（S の実測では `z* = −2.5` で単日 `2025-04-07` が候補イベントの 10.7%、上位3日で 27%）。`IC_bar` の日等加重と §5.3 の1日2件上限は集中を**緩和**するが、**「合格が単一日の出来事に依存していないこと」を直接証明はしない。** G1-7 はそれを直接検定する。**除く日は「結果が悪くなる日」ではなく「イベント件数が最大の日」であり、リターンを見ずに一意に決まる**（HARKing の余地がない）。

> **G1-5 を必須にする理由（OBS000018 対策の核心）**: `S` が深いほどリターンが大きい、という単調性が無いのに `z*` という1点だけで水準が出ている場合、その閾値は**蜃気楼**である。prescreen T-2 はこれを「単調でなければ、統計的有意性にかかわらず不採用」と明記している。**本 spec はそれを G1-5 として数値でゲート化した。**

### 6.3 デシル分析・記述統計（必須出力）

1. **デシル**: プール `P` の観測を `S` の昇順で10分割し、各デシルの `R_5`（グロス）・`R_5^excess`・`R_5` セクター中立の**平均・中央値・件数・標準偏差**を出力する。`ρ_dec`（G1-5）を算出する。
2. **イベント日の集中度（OBS000029 診断）**: 1日あたりイベント数の度数分布、最大値、上位10日のイベント日と件数、**確認期間の総 `R_5` 合計に対する上位3イベント日の寄与率**。
3. **セクター分布**: イベントの `S33` 別件数と平均 `R_5^excess`。
4. **閾値スイープ（T-2・記述統計）**: `z ≤ −1.5 / −2.0 / −2.5 / −3.0 / −3.5` および `z*` の各閾値での件数・`R_5` グロス平均・`R_5^excess` 平均。**合否は `z*`（事前固定）のみで判定し、他の閾値の結果を合格根拠にしてはならない**（N-10）。
5. **レジーム別分解（要）**: 確認期間を四半期ごとに分けたリターン、および市場プロキシの当日リターンの符号別（市場下落日 / 上昇日）のリターン。**部分集合の好成績を全体の合格根拠にしてはならない**（OBS000038・N-10）。
6. **取引時間変更（B-5）の前後別**: 2024-11-05 より前 / 以後で、`g` と `σ_gap` の記述統計、イベント件数を分けて出力する。
7. **生の % 版（記述統計）**: `z` の代わりに `g` をそのまま用いた場合の閾値別件数・平均リターン。**合否判定に使うことを禁止**（N-10）。
8. **ホライズン SA**: H = 3 / 10 の `IC_bar`・デシル平均。**合否判定に使うことを禁止**（N-10）。
9. **ギャップの内訳（記述統計のみ・判定に使わない）**: イベント集合について、`C(D)/O(D) − 1`（`D` の日中セッションのリターン）と `O(t_1)/C(D) − 1`（`D` → `t_1` のギャップ）を出力する。**これは本 spec が構造的に捨てている部分の大きさを C品質チームが把握するための解剖情報であり、「`O(D)` でエントリーできていれば」という反実仮想を合否・採否の根拠にすることを明確に禁止する**（N-4・N-10）。

### 6.4 G2 合格条件（**Sharpe の統計的有意性を基準にしない**）

| # | 条件 | 閾値 |
|---|---|---|
| **G2-1** | 確認期間の**コスト控除後**平均トレードリターン（建玉金額比・**中心ケース往復0.45%**） | **> 0.00%** |
| **G2-2** | 確認期間の**コスト控除後**累積純リターン（資本100万円比） | **> 0.0%** |
| **G2-3** | 選定期間と確認期間で G2-1 の符号が一致 | **両期間とも > 0.00%** |
| **G2-4** | 確認期間の最大ドローダウン（資本比） | **≤ 15.0%**（PJ000001 §8.1 の運用停止水準。到達時点で §5.3-10 によりバックテストは打ち切られる） |
| **G2-5** | 確認期間の実約定トレード数 | **≥ 60件** |
| **G2-6** | 確認期間の約定不能率（イベント件数に対し §4.1 E-7・E-8 で建てられなかった割合） | **≤ 50%** |
| **G2-7** | **往復コスト 0.60% ケース**での確認期間の平均トレードリターン（建玉金額比） | **> 0.00%**（**T-10。中心ケースで合格していても、0.60% で損益分岐を割るなら採用可としない**。寄付のスリッページが本EXP最大の不確実性であり、かつ §4.6 の通り日足では検証できないため） |

**Sharpe の扱い**: 年率 Sharpe は**算出・報告するが、G2 の合否判定には一切用いない**（2年・年100〜200トレードの観測から Sharpe を有意に識別することが原理的に不可能であるため）。報告時は必ず 95% 信頼区間を併記する。**Sharpe の再現性判定は STEP3 フォワード較正へ送る。**

**North Star との距離の報告（判定には使わない）**: prescreen §7.4 が固定した「Sharpe 1.0 到達に必要なグロス **0.85%/トレード**」に対する実測グロス平均の比を報告する。

### 6.5 G2 で必ず出力する生データ（**欠けていれば C品質チームは自動的に差し戻し**）

| # | 項目 |
|---|---|
| P-1 | コスト控除後の平均トレードリターン・中央値・標準偏差・勝率・トレード数（選定/確認それぞれ） |
| P-2 | **約定不能率**（分母＝イベント件数、分子＝E-7）と内訳（`UL=1 かつ O=H` / `Vo=0` / `O` が null） |
| P-2d | **ストップ安寄り付きによる見送り**（E-8）の件数。参考値として `LL=1 だが O>L` で約定したとみなした件数 |
| P-2b | **決済側の約定不能**（§4.4）: 件数・内訳、持ち越し営業日数の分布、5営業日超過による「強制評価」件数 |
| P-2c | `UL` / `LL` の全ユニーク値と件数、null / 欠損行の件数と欠損率 |
| P-3 | **スロット競合率**（分母＝イベント件数、分子＝§5.3-5〜8 および単元離散化で見送った件数）と上限別の内訳（同時保有数 / 建玉総額 / 業種 / **同一イベント日** / 単元離散化） |
| P-4 | **約定不能・スロット競合サンプルを除いた場合と、仮に全件建てられたとした場合の期待値の差**（pp表示） |
| P-5 | **離散化後の実効 f** の全トレード分布（平均・中央値・最小・最大）と目標建玉金額比の乖離 |
| P-6 | **決済理由の内訳**（初期損切 / トレール / 最大保有5営業日の時間切れ / ストップ安持ち越し / 強制評価 / 決算跨ぎ回避 / サーキットブレーカー清算） |
| P-7 | **トレール発動率**と発動後の平均到達最高含み益 |
| P-8 | 平均保有営業日数・平均保有暦日数（金利計算の検証用） |
| P-9 | 日次エクイティ曲線、最大DD、DD 10%・15% への到達有無と到達日 |
| P-10 | **日次のスリーブPnL系列**（将来 G3/G4 を再評価するために必須） |
| P-11 | 年率 Sharpe と 95%信頼区間（**参考値。判定には使わない**） |
| P-12 | **コスト感度 3点**（往復 0.35% / 0.45%（主） / 0.60%）ごとの P-1 および G2-1 相当値 |
| P-13 | 日次の委託保証金率と、30% を下回った日数。最低委託保証金30万円を下回った日数 |
| P-14 | **同一イベント日に複数建玉を持った日数**と、その日の合計損益の分布（OBS000029 診断） |
| P-15 | E-1〜E-9 の**除外・見送り件数の全内訳**（除外の総和がイベント候補の総和と一致することの検算を含む） |

### 6.6 G3（低相関）

**本 EXP では判定不能。** スリーブ在庫がゼロであり比較対象が存在しない（EXP-OBS000001 は保留・凍結であって採用ではない）。

- 数値基準は PJ000001 §5.3 のまま据え置く（既存スリーブとの **|ρ| < 0.3**）。
- **本 EXP では測定しない。** ただし将来の測定のため、**確認期間の日次スリーブPnL系列を `pipeline.json` に保存すること**（P-10）。
- **再評価条件**: 2本目のスリーブが C品質チームにより「採用可」と宣告された時点で再評価する。

### 6.7 G4（限界寄与）

**本 EXP では判定不能**（同上）。数値基準は PJ000001 §5.3 のまま据え置く（**合成 Sharpe が既存構成 +0.15 以上、または 最大DD が既存構成 −5pt 以下**）。再評価条件は §6.6 と同じ。

**G3・G4 が判定不能であるため、本 EXP は G1・G2 のみで暫定判定する。G1・G2 の合格は「暫定採用候補」であり、PJ000001 §5.3 の採否基準の半分（G3・G4）が未充足のままであること、および §4.6 により STEP3 フォワードでの寄付約定の実測が必須であることを、C品質チームの判定文および `portfolio-ledger.md` に必ず明記すること。**

### 6.8 Kill 基準（明示）

| # | 該当したら | 扱い |
|---|---|---|
| **K-1** | §6.2 の G1-1〜G1-7 のいずれかが未達 | **Kill**。G2 以降は測定しない。`portfolio-ledger.md` の「不採用」に記録 |
| **K-2** | 選定期間で `IC_bar` > 0 だが確認期間で消失（G1-1 未達） | **Kill**（過学習）。同上 |
| **K-3** | G1 合格だが §6.4 の G2-1〜G2-7 のいずれかが未達 | **Kill**（trading-app-v2 OBS000025 型＝予測単位は生きているがパイプラインで死ぬ。**prescreen §5.2 が本EXP最有力の死因と名指しした経路**）。同上 |
| **K-4** | 本 spec の基準を事後に緩めないと通らない | **Kill**（HARKing）。同上 |
| **K-5** | §6.0 のデータ十分性ゲート（DS-1〜**DS-7**）未達 | **Kill ではなく S に差し戻す**（判定不能）。リターンを見ていないため確認期間は未見のまま温存される |
| **K-6** | §3.1 の権利落ち/配当落ち規約が確定できない、**§9-2b の検証ゲート V-1〜V-5 のいずれかが未達**、§4.0 のパース規則を満たさない値が出現、§5.4 のユニバース再現が一致しない、§4.5 の算術整合性が破れる | **実験を回さず S に差し戻し**（結果を作らない） |

---

## 7. 日本株固有の模擬条件（バックテストに必ず含める）

| # | 項目 | 実装ルール（数値で固定） | 状態 |
|---|---|---|---|
| **A** | **単元100株の離散性** | §5.3-3 の単元数決定ルール（基準は `C(D)`）。**実効ポジションサイズと目標の乖離、実効 f を毎トレード記録**（P-5） | [ ] 実装必須 |
| **B** | **値幅制限（ストップ高/安）** | §4.0 の2式のみで判定（PEAD spec §4.0 を参照により取り込み）。<br>エントリー: `BUY_BLOCKED(t_1)` → 見送り（E-7）。**加えて本EXP固有の追加除外** `LL(t_1)=1 かつ O(t_1)=L(t_1)` → 見送り（E-8）。<br>イベント側: `UL(T[i−1])=1 or LL(T[i−1])=1` → 除外（E-4）、`LL(D)=1 かつ O(D)=L(D)` → 除外（E-5）。<br>決済: `SELL_BLOCKED(t_exit)` → 持ち越し、最大5営業日（§4.4）。**損切注文が約定しないケースを必ず模擬する。**<br>JPX 制限値幅表からの上限/下限価格の自前再構成は**禁止** | [ ] 実装必須 |
| **C** | **取引時間と約定タイミングの固定** | 前場 9:00–11:30 / 後場 12:30–15:30（**2024-11-05 以降。それ以前の大引けは 15:00**）。本 EXP は日足完結であり、`O` = 寄り付き（板寄せ）、`C` = 大引けとのみ対応づける。**オーバーナイト窓長の期中変化（18.0h → 17.5h）を測定範囲の空白 B-5 として記録し、§6.3-6 で前後別の記述統計を出力する** | [ ] 実装必須 |
| **D** | **決算発表日を跨がない制約（PJ000001 §8.1 H-3）** | **本EXPでは H-3 を字義通り適用する**（PEAD が必要とした「未発表の」という読み替えは不要。本EXPのイベントは決算と排反であるため）。<br>実装: **`/fins/earnings-date`**（`code` / `date` / `scheduled_date` で引ける。`PubDate`＝公表日／`SchDate`＝発表予定日。Free で 2024-06-22〜2026-06-22 が取得可能・point-in-time）から保有銘柄の次回決算発表予定日 `E` を取得する。**`E` は判定時点までに公表された最新の `PubDate` のレコードを使う**（未来の改訂を先読みしない）。`E ∈ [t_1, t_6]` の場合、**`E` の前営業日の引け時点で手仕舞いを決定し、`E` 当日の始値で約定**とする。抵触件数を必ず記録する。<br>**`/equities/earnings-calendar` を使ってはならない**（3・9月期決算会社のみ・翌営業日分のみを返す設計であり、point-in-time のバックテスト参照が原理的に不可能。EXP-OBS000001 §7 D項の既知の誤り）。<br>保有期間中の業績修正・その他適時開示は本EXPのスコープ外とし、**ルール上の手仕舞いは行わない**（識別手段がない。§3.7） | [ ] 実装必須 |
| **E** | **委託保証金率・最低委託保証金30万円** | 制度信用・委託保証金率30%。建玉総額上限125万円に対し必要保証金37.5万円（資本100万円で充足）。**日次で保証金率を計算し30%を下回った日数を記録**（P-13）。最低委託保証金30万円を下回る日があれば記録 | [ ] 実装必須 |
| **F** | **信用規制（日々公表銘柄・増担保規制）** | **模擬不能**（C-5・HTTP 403）。**「模擬していない」ことを `params.json` に明記し、測定範囲の空白 B-4 として記録する。** 増担保規制は**大きく動いた銘柄に発動しやすく、本EXPのイベント銘柄はまさにその条件に当たる**ため、**本バックテストは楽観側に偏っている**ことを申し送る | [ ] 明記必須 |
| **G** | **制度信用 買方金利** | 年率 **2.8%** × **実保有暦日数** ÷ 365（固定値ではなく保有日数に比例させる） | [ ] 実装必須 |
| **H** | **ショート時の貸株料・逆日歩・空売り規制銘柄の除外** | **ロングオンリーのため適用なし**（§5.2・必須条件③） | [x] 該当なし |
| **I** | **権利落ち・配当落ち** | **E-1（分割・併合）**: §3.1。`ExRT='1' かつ AdjFactor≠1` で識別（§9-2a で実測確定済み）。<br>**E-2A（配当・銘柄別）**: §3.1.1 の step 1〜5 で `/fins/summary` の `CurFYSt`/`CurFYEn` と実績配当 `Div1Q`/`Div2Q`/`Div3Q`/`DivFY` から基準日を構成し、T+2 決済の算術で権利落ち日へ写す。<br>**E-2B（暦ベース・一括）**: 各暦月の最終営業日の1営業日前を全銘柄共通で除外。<br>**E-1 / E-2A / E-2B はイベント候補・プール `P`・`σ_gap` の窓のすべてに適用する。**<br>**§9-2b の V-1〜V-5 が1つでも未達なら S に差し戻す**（K-6） | [ ] 実装必須 |

---

## 8. コスト前提

### 8.1 適用するコストモデル（`cost-model.json` に出力）

**prescreen §7.1 が本EXP固有に設定した往復 0.45% をそのまま採る**（プロジェクト標準の 0.35% より厳しい側。緩める方向の変更ではない）。

| 項目 | 片道 | 往復 | 織り込み方 | 出典 |
|---|---|---|---|---|
| スプレッド（呼値の半値） | 0.05% | **0.10%** | 損益から控除 | 株価1,000〜3,500円帯・呼値1円。保守側 |
| **スリッページ（寄付の板寄せ）** | **0.100%** | **0.20%** | **約定価格に直接織り込む**（買 `×(1+s)` / 売 `×(1−s)`） | prescreen §7.1。**本EXP最大の不確実性であり、日足では検証不能**（§4.6） |
| 委託手数料 | 0.05% | **0.10%** | 損益から控除 | 主要ネット証券の信用取引。保守側 |
| 信用買方金利 | — | **0.05%** | 損益から控除（年2.8% × 実保有暦日数 ÷ 365） | 5営業日 ≒ 7暦日 で 0.054% |
| 貸株料・逆日歩 | — | **0.00%** | — | ロングオンリーのため発生しない |
| **往復コスト合計（5営業日保有時）** | | **0.45%** | | |

- **二重計上しないこと**: スリッページは約定価格に織り込む。スプレッド・手数料・金利は損益から控除する。

### 8.2 感度分析（**必須出力。T-10**）

**スリッページのみを振り、他の3項目（往復0.25%）は固定する。** これにより往復合計が3点となる。

| ケース | 片道スリッページ `s` | 固定控除（往復） | **往復合計** | 扱い |
|---|---|---|---|---|
| 低 | 0.050% | 0.25% | **0.35%** | 記述統計。**良いことを理由に合格にしてはならない**（N-10） |
| **中心** | **0.100%** | **0.25%** | **0.45%** | **G2-1〜G2-6 の判定に使う** |
| 高 | 0.175% | 0.25% | **0.60%** | **G2-7 の判定に使う（損益分岐を割れば不合格）** |

### 8.3 損益分岐勝率（**回す前に算出**）

§4.3 のトレール4パラメータ（初期損切 −4.0% / 発動 +4.0% / 追随 −2.0% / 日足終値判定・翌営業日始値約定）と往復コスト 0.45% に基づく。**翌営業日始値約定による約定ギャップを保守側に 0.30% 見込む。**

| ケース | 名目 | コスト後 |
|---|---|---|
| 損失側（トレール未発動で初期損切） | −4.0% | −4.0 − 0.45 − 0.30 = **−4.75%** |
| 利益側の**最小**（発動直後に追随幅まで戻す） | +4.0% − 2.0% = +2.0% | +2.0 − 0.45 − 0.30 = **+1.25%** |

2値近似での損益分岐勝率（利益側の平均値を振る）:

| 利益側の名目平均 | 利益側コスト後 | 損失側コスト後 | 損益分岐勝率 |
|---|---|---|---|
| +2.0%（最小・発動直後戻し） | +1.25% | −4.75% | **79.2%** |
| +4.0% | +3.25% | −4.75% | **59.4%** |
| +6.0% | +5.25% | −4.75% | **47.5%** |
| +8.0% | +7.25% | −4.75% | **39.6%** |

> **prescreen §7.2 の 55.6% との差の説明（回す前に明記する）**: prescreen は「±4.0% 対称・往復コスト0.45%」で必要勝率 55.6% を算出したが、**翌営業日始値約定による約定ギャップ 0.30% を織り込んでいなかった。** これを両側に織り込むと同条件で **59.4%** となる。**これは基準の変更ではなく、より保守的な再計算である。** 本 spec はこの 59.4% を前提として採り、**これを理由にトレール4パラメータや損切幅を緩めることを禁止する**（N-9）。
>
> **設計の厳しさを正直に記す**: 時間切れ決済（最大保有5営業日）は上表の2値近似に含まれない第3の帰結であり、**本EXPでは実際にはこれが決済の多数を占める可能性が高い**（§4.3 の見立て）。上表は目安であり、実測は P-1・P-6・P-7 で確認する。**パラメータが構造的に不適合だったのか、エッジが不在だったのか**の切り分けは、P-6（決済理由の内訳）と P-7（トレール発動率と発動後の平均到達最高含み益）を用いて C品質チームが行う。

### 8.4 年間コストと必要グロス・エッジ（**必須条件⑤。回す前に固定**）

| 指標 | 値 |
|---|---|
| 想定年間トレード数 | **215件**（§3.6 の較正目標） |
| 年間売買代金（建玉ベース） | 215 × 25万円 = **5,375万円** |
| **年間コスト** | 5,375万 × 0.45% = **24.2万円 ＝ 資本比 24.2%/年** |
| **損益分岐（ネット期待値ゼロ）** | **1トレードあたり平均グロス・リターン ≥ 0.45%** ← **G1-3 のゲート値** |
| **North Star（Sharpe 1.0）到達に必要な水準** | **1トレードあたり平均グロス・リターン ≥ 0.85%** ← **報告基準（ゲートではない）** |

---

## 9. B実装チームが「回す前」に先に潰すタスク

以下は**検証の実行より前**に実施し、結果を `research/EXP-OBS000003/10-result/` に出力すること。**いずれかが不成立なら、実験を回さずに S へ差し戻す。**

| # | タスク | 合格条件 | 不成立時 |
|---|---|---|---|
| **9-1** | **参照営業日カレンダー `T` の再構成**（§2.0）。`T[1]` / `|T|` / `T[60]` / `T[61]` / `T[68]` / `T[477]` / `T[481]` / `T[482]` / `T[|T|]` の**実日付**を `params.json` に出力する。既知値（`T[1]=2024-06-21`, `|T|=487`, `T[487]=2026-06-22`）と一致するか確認する | `|T| ≥ 480` かつ既知値と一致 | **S に差し戻す**（推定日付のハードコード禁止） |
| **9-2a** | **価格調整規約の確定（§3.1）**。**第1版の 9-2。2026-09-14 に B実装チームが実行済み・完了。** `AdjFactor` 非1.0の91件がすべて整数分の1（1/2〜1/15）のクリーンな分割比率であり、`ExRT='1'` と完全一致、累積調整の継続性 `prev_AdjC = prev_C × AdjFactor` が91/91件で成立。**⇒ E-1（分割・併合の権利落ち）は識別可能。`AdjFactor` は配当を調整しない。** 再実行は不要だが、値は `params.json` に保持する | 分割・併合の除外（E-1）が一意に決まる | **達成済み**（feasibility.json `9-2_price_adjustment_convention`） |
| **9-2b** | **配当落ち日の構成と検証（§3.1.1。第2版で新設。本EXP固有の最重要タスク）**。§3.1.1 の step 1〜5 を実装して E-2A を構成し、E-2B を `T` から構成する。そのうえで**下表 V-1〜V-5 をすべて評価**し、全数値を `feasibility.json` に出力する。**前方リターンを一切参照してはならない**（使ってよいのはオーバーナイト・ギャップ `g` と実績配当 `Div*` のみ） | **V-1〜V-5 をすべて充足** | **S に差し戻す**（K-6。この場合は識別手段が無いことが確定するため、S は本EXPを Kill する） |
| **9-3** | **`UL` / `LL` の再検証**（§4.0）。全ユニーク値と件数、null / 欠損率を出力する | ユニーク値が `"1"` / `"0"` / `""` / null のみ **かつ** 欠損率 ≤ 1% | **S に差し戻す**（K-6） |
| **9-4** | **ユニバースの構築**（§5.4）。(a) 確認期間: PEAD の `universe.json` `confirmation_universe`（175銘柄）を流用し、**本EXPのデータから U-1〜U-7 を再適用した結果と完全一致することを確認**。(b) 選定期間: `D_u = T[61]` で新規構築 | (a) 完全一致 **かつ** (b) 選定ユニバースが **150銘柄以上**（U-7 の株価帯緩和は1回だけ許容） | **S に差し戻す**（K-6。窓の短縮・利用可能分での代用を絶対に行わない） |
| **9-5** | **`g` / `σ_gap` / `z` の記述統計**。銘柄日数、`σ_gap` の分布（分位点）、`z` の分布（分位点・裾の件数）、`σ_gap = 0` / NaN の件数、E-1 / **E-2A** / **E-2B** / E-3〜E-6 の除外件数の全内訳と**総和の検算**。**加えて、有効ギャップ60本の収集に要した遡り営業日数 `K` の分布（最大値を含む）を出力し、`K ≤ 66` が全銘柄日で成立することを示す** | — （記録のみ。ただし §4.5 の算術整合性の検算結果と `K` の最大値を必ず含める） | 算術が合わない、または `K > 66` が1件でも出れば **S に差し戻す**（K-6） |
| **9-6** | **`z*` の較正（§3.6）と §6.0 データ十分性ゲート（DS-1〜DS-7）の評価**。**リターンを一切参照せずにカウントのみで行う。** グリッド11点それぞれの選定期間件数・`N_ann` を全点出力し、選ばれた `z*` を `params.json` に凍結して記録する。**加えて、確認期間のイベント件数上位10日とその件数シェア（DS-7 の根拠）を出力する** | DS-1〜DS-7 をすべて充足 | **S に差し戻す**（K-5。**この時点では確認期間のリターンを見ていないため、確認期間は未見のまま温存される**） |
| **9-7** | **決算排反性の実データ確認**（§3.5）。E-3 による除外件数を**銘柄日ベース**と**`DiscDate` ベース**の両方で出力し、本EXPのイベント日集合と PEAD のイベント日・エントリー日集合の積集合が**空であること**を数値で示す。併せて `/fins/earnings-date` の取得範囲（`2024-06-22`〜`2026-06-22`）と、§7 D項の抵触件数の見込みを出力する | 積集合が**空** | **S に差し戻す**（K-6） |
| **9-8** | **未識別ニュース混入の記述（§3.7・T-7）**。(a) TDnet スナップショット（`research/_snapshots/tdnet/`）と J-Quants 日足の**カレンダーが1日も重複しないこと**を実データで示す、(b) 確認期間ユニバース175銘柄における非決算適時開示の**無条件発生率**、(c) E-3 による除外率 | — （記録のみ） | — |
| **9-9** | **出力の自己整合チェック**（PEAD 9-6 の教訓）。`feasibility.json` 内で、(a) 各カウントの母集団がキー名と一致すること、(b) 除外件数の総和が候補件数と一致すること、(c) `K`（§6.1.2）と DS-6 が**同一の値**であること、(d) `K_event` と DS-5 が**同一の値**であること | すべて整合 | **S に差し戻す**（K-6） |

### 9-2b の検証ゲート V-1〜V-5（**配当落ちの識別が本物であることの証明**）

`d(j,X) := DPS(j,X) / C(j, X−1)` を**理論落ち幅（比率）**、`g(j,X)` を §3.2 のオーバーナイト・ギャップとする。回帰は `g = a + b·d` の単純OLS。**分割・併合の権利落ち（E-1）に該当する銘柄日は全ゲートから除く。**

| # | ゲート | 閾値（**回す前に確定**） | 閾値の根拠（**観測値からではなく理論から導出**） |
|---|---|---|---|
| **V-1** | 構成された配当落ち日**全体をプール**した回帰の傾き `b̂` | **`−1.15 ≤ b̂ ≤ −0.70`** | 配当落ちの理論落ち幅は配当全額（`b = −1`）。実際の落ち率は源泉徴収 **20.315%** による税制クライアンテレ効果で `−1` と `−(1−0.20315) = −0.797` の間に入るのが標準的な実証結果である。この理論区間 `[−1.000, −0.797]` に推定誤差と優待・フロー効果の余地を両側に加えて **`[−1.15, −0.70]`** とした |
| **V-2** | **プラセボ（日の特定精度）**。`shift ∈ {−2, −1, +1, +2}` 営業日ずらした日で同じ回帰を行ったときの傾き `b̂_shift` | **4つすべてで `|b̂_shift| ≤ 0.35`** | 構成が**正しい単一の日**を指しているなら、配当額に比例した落ちは前後の営業日には存在し得ない。0.35 は理論値 `1` の約1/3であり、「ある」と「ない」を明確に分離する水準 |
| **V-3** | **日別の再現性**。構成された配当銘柄が **30銘柄以上**ある権利落ち日それぞれで日内クロスセクション回帰を行い、`b̂_day ∈ [−1.25, −0.60]` を満たす日の割合 | **≥ 70%** | 日内回帰は市場全体の動きを切片へ吸収するため傾きが識別の主指標となる。ただし市場が大きく動いた日は銘柄ベータと配当利回りの交絡で傾きが減衰するため、全日100%は要求しない |
| **V-4** | **カバレッジ**。候補銘柄のうち、実績配当（`> 0`）または明示的な `0.0` 申告の**いずれかが1件以上**構成できた銘柄の割合 | **≥ 95%** | 構成できない銘柄は E-2A が救えず E-2B のみに依存する。5% を超えるなら E-2A が機能しておらず、識別は成立していない |
| **V-5** | **期末日の算術的自己検算**。`CurPerType = '2Q'` の全行について、`CurFYSt + 6ヶ月 − 1日` が開示された `CurPerEn` と一致する割合 | **≥ 99.5%** | §3.1.1 step 2 の暦月加算が、開示側が名乗る期末日と一致することの直接検算。ここが崩れれば基準日の構成そのものが誤り |

> **S が設計時に実測した値（透明性のため全数開示する。B実装チームはこれを**参照せずに独立に**再実行し、値が一致することを確認すること）**
>
> | ゲート | S の設計時実測値 | 判定 |
> |---|---|---|
> | V-1 | `b̂ = −0.942`（se 0.071、`n = 1,792`）。`−1` との差は `t = +0.82` で有意差なし | 充足 |
> | V-2 | `b̂` = `+0.075`（−2営業日）／`−0.070`（−1）／`+0.018`（+1）／`+0.056`（+2）。いずれも `−1` から **20〜35σ** 離れている | 充足 |
> | V-3 | 対象8日中 **7日**（87.5%）が `[−1.25, −0.60]` に入る。日別中央値 `b̂ = −0.850`。外れた1日は **2026-03-30**（`b̂ = −0.485`・切片 −4.23%＝市場全体が大きく下落した日） | 充足 |
> | V-4 | 511銘柄中 **510銘柄**（99.8%）。内訳: 実績配当 `>0` が496、明示的 `0.0` のみが14、いずれも無しが1 | 充足 |
> | V-5 | **1,015 / 1,015 = 100.0%**（不一致0件） | 充足 |
> | 参考 | 構成された配当落ち銘柄日 **1,859件**。暦月分布は 3月 757・9月 707・12月 146・6月 134・2月 43・8月 40 ほかであり、**日本の決算期構成（3月期決算が約7割、その中間が9月）と整合する** | — |
>
> **この開示の位置づけ**: V-1〜V-5 は**リターンの合否ゲートではなく、シグナル構成が正しいかを問うデータ検証ゲート**である。閾値は上表のとおり**税制・T+2決済・算術的自己検算という理論から導出**しており、観測値に合わせて設定したものではない。**本 spec 改訂の時点で、S・B・C の誰も前方リターンを一度も観測していない**（B は §9-2 で停止しており 9-4 以降を実行していない）。したがって G1・G2 の合否基準に対する HARKing は発生していない。C品質チームはこの点を独立に監査すること。

---

## 10. B実装チームへの指示（禁止事項）

### 10.1 共通の禁止事項

- 判定語（「良好」「有望」「改善」「期待できる」等）を書かない。**生データのみ**を出力する
- パラメータ・閾値・分割・seed をチューニングしない
- 成功基準を事後に緩めない
- 成果物の実在を確認してから完了報告する

### 10.2 本 EXP 固有の禁止事項

| # | 禁止事項 | 出典 |
|---|---|---|
| **N-1** | **テクニカル指標を一切使わない。** MACD / RSI / ボリンジャーバンド / CCI / 移動平均 / ADX / **ATR** / Stochastic / 出来高指標を、エントリー・エグジット・フィルタ・サイジングに用いない。**初期損切を ATR 連動にすることも禁止**（§4.3-1） | 必須条件① |
| **N-2** | **PEAD（SUE）その他いかなるシグナルとも合成・ゲート・足し算しない。** 在庫ゼロで相手が存在しないことを理由に緩めない | 必須条件①・OBS000027 |
| **N-3** | **1営業日（寄付→引け）版・日中完結版を実装しない。ショート脚を実装しない。** 保有期間は 3 / 5 / 10 営業日のみ（主は 5） | 必須条件②③・prescreen §7.5 |
| **N-4** | **エントリーを `O(D)` に変更しない。** `g(j,D)` は `O(D)` から計算される量であり、`O(D)` 約定は**明確な先読み**である。§6.3-9 が出力する `D` 日中リターンを「`O(D)` で入れていれば」という反実仮想の根拠に使うことも禁止する | §4.1 |
| **N-5** | **ユニバースを TOPIX500 相当（`ScaleCat` 条件）の外へ広げない。** 有効 n が不足しても銘柄数で救おうとしない | OBS000030 |
| **N-6** | **`z*` をリターンで選ばない。** §3.6 の較正はカウントのみを入力とする決定的手続きであり、グリッドの追加・細分化、目標件数215の変更、年換算定数245の変更をいずれも禁止する | T-1 |
| **N-7** | **選定期間のリターンを見てから spec の値を変更しない** | §2.3 |
| **N-8** | **spec の定義に矛盾・欠落・曖昧さを見つけたら、自分で「安全側だろう」と解釈して埋めず、即座に S に差し戻す。** 特に窓長・ラグ・オフセット（§4.5）、`AdjFactor`/`ExRT` の意味（§9-2）、`UL`/`LL` のパース（§4.0）、`ScaleCat`/`Mrgn` の対応付け（§5.4）について。<br>**差し戻しは正しい動作であり、ペナルティではない。** EXP-OBS000001 は同じ経路で2回差し戻され、いずれも spec 側の誤りが正された | trading-app-v2 EXP-OBS000037 |
| **N-9** | **閾値・パラメータを事後に動かさない。** 特に `z*`、プール下限 `−1.5`、E-3 の N=2、初期損切 −4.0%、トレール4パラメータ、DS ゲートの各閾値、G1/G2 の各閾値 | K-4 |
| **N-10** | **記述統計・感度分析の好結果を合格の根拠にしない。** 対象: 閾値スイープ（§6.3-4）、レジーム別分解（§6.3-5）、生の%版（§6.3-7）、ホライズン SA（§6.3-8）、`D` 日中リターン（§6.3-9）、コスト 0.35% ケース（§8.2）、上方ギャップ（§5.2）、セクター中立リターン（§4.2） | OBS000018 / OBS000038 |
| **N-11** | **DS ゲート（§6.0）未達のまま G1（§6.1）以降を実行しない。** 「参考として回してみる」ことも含めて禁止する。<br>**理由（不可逆性）**: 確認期間のリターンを一度でも見た時点で、当該窓は永久に「未見の確認期間」ではなくなる | PEAD N-10 |
| **N-12** | **G1 が未達の場合、G2 を測定しない** | K-1・PJ000001 §5.3 |

### 10.3 許可される価格データの利用範囲（明示）

本EXPの予測変数は価格由来であるため、PEAD の N-2（価格由来変数の禁止）は**適用されない**。ただし**使ってよい価格の用途は以下に限る**:

- シグナル `g` / `σ_gap` / `z` の生成（§3.2）— **`O` と `C` のみ。`H` / `L` は使わない**
- ユニバースの流動性フィルタ（`Va`）・株価帯フィルタと単元数計算（`C`）（§5.4・§5.3-3）
- 値幅制限による約定可否の判定（`UL` / `LL` / 同一日の `O` / `H` / `L` / `Vo`）（§4.0）
- 前方リターン・損益の計算（`O` / `C`）（§4.2・§4.3）
- 市場プロキシ・セクター中立リターンの算出（§4.2）

**上記以外の用途で価格データを使うこと（特に `H` / `L` を用いた指標化、日中高値でのトレール判定、出来高の異常検知によるニュース代理変数の構築）は N-1 違反である。**

> **補足（出来高によるニュース検知を禁止する理由）**: §3.7 の「未識別ニュース」を出来高急増で代理する設計は一見魅力的だが、(a) 出来高指標は N-1 が禁じるテクニカル指標であり、(b) 新しいフィルタ＝新しい自由パラメータの事後導入であり、(c) 極端ギャップ日の出来高は定義上ほぼ常に急増しているため識別力がない。**必要と考える場合は本EXP内で実装せず、別EXPとして事前登録し直すこと。**

---

## 11. 使用スクリプト / 再現方法（想定）

| スクリプト | 役割 | 出力先 |
|---|---|---|
| `scripts/gap_fetch_data.py` | J-Quants からユニバース候補の日足・銘柄マスタ・`/fins/summary`・`/fins/earnings-date` を取得しキャッシュ（HTTP 429 は指数バックオフでリトライ） | `data/raw/gap/` |
| `scripts/gap_build_universe.py` | §5.4 のユニバース確定（確認期間は PEAD の流用＋再現一致確認、選定期間は `T[61]` で新規構築）＋ §9-1 の `T` 出力 | `10-result/universe.json` / `10-result/params.json` |
| `scripts/gap_feasibility.py` | §9-2a〜§9-9（調整規約・**配当落ちの構成と V-1〜V-5 の検証**・UL/LL・記述統計・`z*` 較正・DS ゲート・排反性・混入記述・自己整合） | `10-result/feasibility.json` |
| `scripts/gap_prediction_unit.py` | §6.1〜§6.3（`IC_bar`・permutation・水準検定・デシル・全記述統計） | `10-result/prediction-unit.json` |
| `scripts/gap_pipeline.py` | §4〜§5・§6.4〜§6.5（トレール・約定模擬・生データ全項目・コスト感度3点） | `10-result/pipeline.json` / `10-result/cost-model.json` |

実行コマンド想定:

```bash
python scripts/gap_fetch_data.py       --config research/EXP-OBS000003/01-spec.md --out data/raw/gap/
python scripts/gap_build_universe.py   --out research/EXP-OBS000003/10-result/
python scripts/gap_feasibility.py      --out research/EXP-OBS000003/10-result/
python scripts/gap_prediction_unit.py  --seed 20260914 --out research/EXP-OBS000003/10-result/
python scripts/gap_pipeline.py         --seed 20260914 --out research/EXP-OBS000003/10-result/
```

- **決定性**: 同じコマンドで同じ結果が出ること。乱数を使うのは permutation のみであり、シードは **20260914** に固定する。
- `run.log` に**再現用の実行コマンドを必ず含める**こと（`_templates/result.readme.md` 準拠）。
- 成果物: `prediction-unit.json` / `pipeline.json` / `params.json` / `cost-model.json` / `universe.json` / `feasibility.json` / `run.log`。**`prediction-unit.json` と `pipeline.json` の両方が揃っていない場合、C品質チームは自動的に「差し戻し」とする**（PJ000002 §6）。
- **実行順序の制約**: `gap_feasibility.py`（§9-6 の DS ゲート）が全項目合格を出力するまで、`gap_prediction_unit.py` を実行してはならない（N-11）。`gap_prediction_unit.py` が G1 合格を出力するまで `gap_pipeline.py` を実行してはならない（N-12）。

---

## 12. 本 spec が測定しないこと（申し送り）

| # | 未測定項目 | 理由 | 送り先 |
|---|---|---|---|
| 1 | **テール局面での挙動（B-1。第2版で内容を訂正）** | **第1版の「唯一のテール局面が窓外」という記述は誤りであった**（§2.5 B-1 参照）。実測では 2025-04-07（中央値ギャップ −7.26%・選定期間）・2026-03-09（−4.68%・確認期間）が窓の内側にある。**残る空白は「確認期間の独立なストレス・エピソードが実質1回（2026年3月）しかなく、テール時の挙動の再現性を確認する術がないこと」である** | STEP3 フォワード較正／データ拡張（司令塔判断） |
| 12 | **確認期間の第2ストレス日 2026-03-30 を測定しない（B-7。第2版で新設）** | 同日は確認期間で2番目に大きいストレス日であると同時に3月期決算会社の配当落ち日であり、市場要因（約 −3.2pp）と機械的な配当落ち（約 −1.39pp）を横断的に分離できない（当日の日内回帰 `b̂ = −0.485`）。E-2B により除外する | **司令塔判断事項 D-8** として申し送る／STEP3 フォワード較正 |
| 2 | **寄付の板寄せにおける実約定（B-2）** | 自分の注文が清算価格形成に参加する効果・板の薄さ・特別気配による寄付遅延は日足では原理的に測定不能（§4.6） | **STEP3 フォワードでの実測を採用の必須条件とする**（T-5④） |
| 3 | **未識別ニュース起因ギャップの混入（B-3）** | TDnet インデックスの保持期間（38日）と J-Quants 日足の12週遅延によりカレンダーが1日も重複せず、**条件付き推定が構造的に不可能**（§3.7）。**バイアスの符号を本 spec は決めない** | データ拡張（TDnetアドオン）／STEP3 以降 |
| 4 | G3（低相関）・G4（限界寄与） | スリーブ在庫がゼロで比較対象が存在しない（PEAD は保留・凍結であって採用ではない） | 2本目のスリーブ採用時に再評価（§6.6・P-10 を保存） |
| 5 | 信用規制（日々公表・増担保）の影響（B-4） | 現行契約でデータ取得不可（C-5・403）。**本EXPのイベント銘柄は増担保規制の発動条件に当たりやすく、楽観側への偏りは PEAD より大きい可能性がある** | 有料化判断時に再検討（§7 F項） |
| 6 | Sharpe の再現性 | 2年ではSharpeを有意に識別できない | STEP3 フォワード較正（§6.4） |
| 7 | ショート（上方ギャップ）のパイプライン | 逆日歩が事前算定不能でコストモデルが成立しない | 別EXP（§5.2） |
| 8 | 他シグナル（PEAD・テクニカル）との合成 | 本EXPのスコープ外（必須条件①） | G1 合格後に別EXPとして起票 |
| 9 | PTS・先物夜間セッション・板情報 | データ非保有（prescreen §3.4・§3.5） | C-6（データ予算）の司令塔判断 |
| 10 | **取引時間変更（2024-11-05）の影響（B-5）** | 確認期間は全期間が延長後のため合否には影響しないが、選定期間との比較可能性に影響しうる | §6.3-6 で記述統計のみ出力 |
| 11 | **PEAD 確認期間との窓の重複（B-6）** | 契約データ2年の制約により回避不能。S は「避けることの害の方が大きい」と判断したが、これは自明ではない | **司令塔判断事項 D-6** として申し送る。**【第3版で更新】実害は発生しなかった。** B は DS ゲートで停止しており（N-11）、当該窓の**株価リターンは一度も観測されていない**。PEAD の確認窓は未見のまま温存されている |
| 13 | **本EXPの主題そのもの（ギャップ反転のエッジの有無）を測定していない（B-8。第3版で新設）** | §6.0 データ十分性ゲート DS-2a / DS-5 / DS-6 / DS-7 未達により K-5「判定不能」。**G1 を一度も実行していないため、エッジの有無について本EXPは肯定・否定のいずれの証拠も持たない**（§6.1.4(a) が「G1 未達ならエッジの不在の証拠として記録できる」と書いた条件は、DS ゲートを通過した場合にのみ成立する） | **司令塔判断事項 D-9**（データ階層）／STEP3 フォワード較正 |
| 14 | **DS-5・DS-7 を満たす `z*` の窓が存在するか（第3版で新設）** | 確認期間の中間 `z` における件数分布を測れば判明するが、**測って窓を探すこと自体が K-4 に当たるため意図的に測定しない**（§6.0.1(c)）。加えて DS-6 は `z*` に依存しないため、窓が存在しても §6.0 は充足しない | 測定しない（恒久） |

---

## 13. 司令塔判断を要する事項（本 spec で新たに生じたもの）

| # | 判断事項 | S戦略チームの見解 |
|---|---|---|
| **D-6** | **本EXPの確認期間（2025-07-01〜）が、凍結中の EXP-OBS000001（PEAD）が未見として温存している確認窓と同一カレンダーである**（§2.5 B-6）。本EXPを実行すると当該窓の株価リターンは観測済みとなる | **実行してよい**と考える。理由: (a) 契約データ2年では2本目以降の実験が既存の確認窓を避けることは物理的に不可能、(b) 窓を避けるために分割点を動かすことは本EXP側に新たな自由度を持ち込み、より悪い、(c) PEAD は再開時に N-9 により新規EXPとして分割を事前登録し直すことが既に定められている。**ただしこれは S の判断であり自明な結論ではないため、司令塔の明示的な了解を求める** |
| **D-7**<br>**（2026-09-14 に発動済み。帰結は直下の第2版の節を参照）** | **§3.1 E-2（配当落ちの識別）が §9-2 で確定できなかった場合の扱い** | **S に差し戻し（K-6）とし、実験を回さない**ことを spec で既に定めている。差し戻された場合の代替策（暦月別分布による間接判定など）は S が判断するが、**配当落ちの混入は「効果の希釈」ではなく「3月末・9月末への集中と、そこでの確実な負け」であり安全側ではない**ため、妥協して回すことはしない |

**2026-09-14: 司令塔がD-6・D-7ともに了承。** S戦略チームの見解どおり実行してよい。B実装チームは§9の先行タスクへ着手してよい。

### 2026-09-14（第2版）: D-7 発動後の帰結と新規判断事項

**D-7 は実際に発動した。** B実装チームが §9-2 を実行し、`AdjFactor` が配当を調整しないこと・`ExRT` に配当落ちフラグが無いこと・`/fins/summary` に除権日フィールドが無いことを実測して S へ差し戻した（`10-result/feasibility.json`、`verdict: AMBIGUOUS_ESCALATE_TO_S`）。**この差し戻しは spec の設計どおりの正しい動作であり、B実装チームの判断に誤りはない。**

S は D-7 が定める「代替策は S が判断する」に従い、B が調べた範囲（除権日フィールドの有無）の**外側**を独立に調査した。結果、**`/fins/summary` には除権日フィールドは無いが、会計期間の期末日（`CurFYSt` / `CurFYEn` / `CurPerEn`）と実績配当額（`Div1Q` / `Div2Q` / `Div3Q` / `DivFY`）が存在し、日本株の配当基準日は会計期末日と一致するため、基準日は完全に構成できる**ことが判明した。基準日から権利落ち日への写像は T+2 決済のもとで一意であり、**推測の余地がない決定的手続き**である（§3.1.1）。S はこれを実データで検証し、V-1〜V-5 をすべて充足することを確認した（§9-2b）。

| # | 判断事項 | S戦略チームの決定 |
|---|---|---|
| **D-8** | **E-2B（暦ベースの一括除外）が、確認期間で2番目に大きいストレス日 `2026-03-30`（中央値ギャップ −4.556%）を測定対象から外す**（§2.5 B-7）。本EXPは「市場ストレス時の売り需給の偏りの解消」を賭けの中心に置いており、ストレス日を1日失うことは軽くない | **S は「除外する」と決定し、回す前に凍結した。** 理由: (a) 同日のギャップは市場要因（約 −3.2pp）と機械的な配当落ち（約 −1.39pp）の合成であり、当日の日内回帰の傾き `b̂ = −0.485`（他の落ち日は −0.80〜−0.90）が示すとおり**両者を横断的に分離できない**、(b) 除外しなければ全ユニバースの `z` が一律に約 1.2σ 押し下げられ、イベントがこの1日へ集中する（**OBS000029 の時間方向再発そのもの**）、(c) 確認期間には別のストレス日 `2026-03-09`（−4.68%・配当落ちではない）が残るため、テール局面の観測がゼロになるわけではない。<br>**本件は司令塔への「報告」であり「照会」ではない。** 判断を留保して B を走らせると「結果を見てから除外するか決める」という事後の自由度が生まれ、HARKing になる。**司令塔が S の決定に同意しない場合は、N-9 に従い別EXPとして分割を登録し直すこととし、本EXPの基準は動かさない。** |

**2026-09-14: 司令塔がD-8の報告を了承。** Sの決定どおり2026-03-30の除外を維持し、B実装チームは§9-2b以降へ進んでよい。

### 2026-09-14（第3版）: DS ゲート未達（K-5）後の新規判断事項

B実装チームが §9-2b〜§9-9 を実行し、**§9-2b の V-1〜V-5 は全ゲート合格・S の設計時実測値と精緻に一致**（配当落ち識別の再現性を確認）、**§9-6 の DS-2a / DS-5 / DS-6 / DS-7 が不合格**で停止した。S は §6.0.1 のとおり検討し、**本 spec の枠内で DS ゲートを満たす再設計は存在しないと結論した**（決定的根拠: DS-6 は `z*` に依存しない）。**基準は1つも緩めておらず、確認期間のリターンは未見のまま温存されている。**

| # | 判断事項 | S戦略チームの見解 |
|---|---|---|
| **D-9**<br>（**最重要・データ階層の投資判断**） | **契約データ期間（J-Quants Free・2年・487営業日）が、日次ブロック統計にもとづくイベント駆動戦略の検証に対して構造的に不足している。** 本PJは**2件連続**で、リターンを見る前のデータ十分性ゲートで座礁した。<br>・EXP-OBS000001（PEAD）: DS-2 = **27日** / 閾値30日<br>・EXP-OBS000003（本EXP）: DS-5 = **25日** / 閾値80日、DS-6 = **62日** / 閾値100日<br>いずれも「**確認期間に相異なるイベント日が足りない**」という同一の壁である。**これは戦略の当否ではなく、検証基盤の容量の問題である。** | **S は単独で決めるべきでないと判断し、司令塔判断に上げる。** 選択肢と S の見立て:<br>**① データ拡張（J-Quants 有料プランへの切替）**: 期間を延ばせば `K_event`・`K` は概ね期間に比例して増える。**本PJで最も直接的に効く投資**だが、C-6（データ予算）の管轄であり、資本100万円に対する年率ハードルの算術（`portfolio-ledger.md` 候補③の項を参照）を司令塔が引き受けるか否かの問題。**S は数値的根拠として「2件の座礁はいずれも期間の長さに帰着する」ことを提示するのみとし、課金の是非は判断しない**<br>**② イベント駆動族を当面凍結し、常時ポジション族へ資源を移す**: 常時ポジション型は日次ブロックの数が営業日数そのものになるため、同じ2年でも `K` が桁で増える。**ただし本PJの既存4戦略（A〜D）はすべて価格由来方向予測族であり、PJ000001 §4 C-3 の低事前確率族に含まれる**——この移動は「検証できる族」へ逃げて「勝てない族」に戻ることになりかねない<br>**③ 日次ブロック統計を要求しない検証設計へ切り替える**（STEP3 フォワード較正に直行）: 検出力は落ちるが前進はする。**ただし本PJが8連敗から輸入した偽陽性対策の中核を外す方向であり、S は推奨しない**<br>**S の推奨順位: ① > ③ > ②。** ただし①は費用の決裁であり、S の職掌を超える |
| **D-10** | **「イベントが市場共通因子で駆動されるか」を prescreen の事前スクリーニング項目に格上げするか。** 本EXPは spec を2版重ね、B実装チームが9タスクを完走した末に、**イベント過程の時間構造という、prescreen 段階でカウントだけで確認できたはずの理由**で停止した。同じ検査は EXP-OBS000002（却下済）§6.5 の「3月期決算2,164社の発表予定日が27通り・上位3日で62%」という測定と**完全に同型**であり、そのときは prescreen 段階で却下できている | **S は「格上げすべき」と考える。** 具体案: prescreen テンプレートに **「候補イベントの発生日分布を、リターンを見ずに件数のみで先に測る」ステップを必須化**し、(i) 相異なるイベント日数、(ii) 最大単日シェア、(iii) 選定期間と確認期間の発生率の比 の3点を **prescreen の GO/却下の判断材料**とする。**本EXPの実測（`K_event`=25日・単日シェア52.8%・発生率比3.2倍）は、prescreen 段階で測っていれば条件付きGOを出さなかった水準である。**<br>**これは運用ルール（`research/_templates/prescreen.template.md`）の変更であるため、S の一存では入れず司令塔の了解を求める。** 了解が出れば S が同テンプレートを改訂する |

---

## 変更履歴

- 2026-09-14: **第3版（§9-6 DS ゲート未達＝K-5 差し戻しへの対応。記録専用の改訂）。** S戦略チーム（chief-strategist・A設計官兼務）が、B実装チームの §9-6 停止（`escalate_kind: K-5`）を受けて改訂。
  - **【最重要】合否基準を1つも変更していない。** DS-1〜DS-7 / G1-1〜G1-7 / G2-1〜G2-7 / K-1〜K-6 の閾値・定義はすべて第2版のままである。`z*` の較正グリッド（11点・−1.50〜−4.00）も**拡張していない**。プール下限 `−1.5`・目標件数215・年換算定数245も不変である。**確認期間の前方リターンは本改訂の時点でも誰も観測していない。**
  - **§6.0.1 を新設し、DS ゲートの実測結果と「本 spec の枠内で §6.0 を充足させる再設計は存在しない」という S の結論を、根拠とともに記録した。** 決定的根拠は **DS-6 が `z*` に依存しない量である**こと——DS-6 はプール `P`（`z ≤ −1.5`・N-9 で凍結）の日次ブロック数を数える量であり、`z*` は `P` の定義に入らない。**したがって較正グリッドをどう引き直しても実測 62日は動かず、閾値100を満たすことはない。** これは推測ではなく DS-6 の定義からの論理的帰結である。
  - **DS-2a の修復（グリッド拡張）を行わなかった理由を明記した。** グリッドが実測のテールの厚さに対して誤って設計されていたこと自体は選定期間のみの情報から確認できる（`z` の p01 は選定 −6.235 / 確認 −3.689 であり、標準正規を下半分に切断した理論値 −2.58 から大きく外れる）。しかし**より極端な `z*` は DS-5・DS-7 を一方的に悪化させ**、かつ DS-6 は不変であるため、**修復しても §6.0 は充足しない。**
  - **DS-5・DS-7 を満たす `z*` の窓の探索を、意図的に行わないことを宣言した**（§6.0.1(c)・§12-14）。確認期間のカウント分布を見てから較正設計を選び直すことは、入力がリターンでなくカウントであっても K-4（HARKing）と外形上区別できない。**選定/確認の分割解消（§6.0.1(d)）・プール下限の緩和（R3）も同じ理由で禁止した。**
  - **イベント定義・母集団の変更案 R1（市場残差ギャップ）・R2（日次イベント数の上限）・R3（プール下限の緩和）を検討し、いずれも本 spec の改訂としては採らないと結論した。** R2 は DS-6 に無効、R3 は N-9 違反。**R1 は原因に直接効くが、事前登録シグナル定義そのものの変更であり PJ000002 §6 により新規EXP**であり、かつ**賭けの対象（市場ストレス時の需給の偏り）を取り除く設計であるため事前確率は本EXPより低い。**
  - **§2.2 の営業日算術の誤り2件を訂正した**（B実装チームの独立再計算により発覚）。確認期間の営業日 **232本 → 231本**（`T[481] − T[251] + 1 = 481 − 251 + 1`）、選定期間の E-2B 一括除外 **10本 → 9本**（`2024-09-27` は `T[68] = 2024-09-30` より前）。イベント日数は **173/221 → 174/220**。**分割の判断（両期間とも期待件数が T-1 の帯 150〜300 の内側）は変わらない。**
  - **§3.6 step 3 の `N_ann` の分母が一意に定義されていなかったことを記録した。** B は期間レンジの営業日数（183 / 237）を用い、§2.2 は E-2B 控除後（173 / 221）を意図していた。**3通りの読みすべてで DS-2a は不合格・DS-2b は合格であり、ゲートの結論は不変である**（選定 `N_ann(z*=−4.00)` = 356.12 / 376.71 / 374.54）。**結論が同じでも、結果を見た後に較正式の分母を選び直すことは K-4 と外形上区別できないため、第3版では「正しい値」への書き換えを行わず、曖昧であった事実と不変性の検算のみを記録した。** 本件は T-8（EXP-OBS000037 の教訓）が禁じた「導出済みの数値を散文で書き、実装者が再計算できる式を書かない」という**設計側（S）の過失**である。B の実装は N-8 違反ではない。
  - **測定範囲の空白 B-8（イベント過程が市場共通因子に支配される群発過程であること）を新設**し、§12 に未測定項目 13（本EXPの主題そのものを測定していない）・14（DS-5/DS-7 を満たす窓の存否を恒久的に測定しない）を追加した。**§12-11（PEAD 窓との重複）に、B が DS ゲートで停止したため実害が発生していないことを追記した。**
  - **司令塔判断事項 D-9（契約データ期間の構造的不足＝データ階層の投資判断。PEAD DS-2=27日 と本EXP DS-5=25日・DS-6=62日 の2件連続の座礁）・D-10（市場共通因子駆動イベントの prescreen 事前スクリーニング化）を新設した。**
  - **B実装チームの §9-2b 独立再実行（V-1〜V-5 全合格・S の設計時実測値と一致）により、第2版で確立した配当落ち識別手続きの再現性が確認された。** この部分に問題はない。
- 2026-09-14: **第2版（D-7 発動への対応）。** S戦略チーム（chief-strategist・A設計官兼務）が、B実装チームの §9-2 差し戻し（`verdict: AMBIGUOUS_ESCALATE_TO_S`）を受けて改訂。**本改訂の時点で S・B・C の誰も前方リターンを一度も観測していない**（B は §9-2 で停止し 9-4 以降を未実行）。したがって G1・G2 の合否基準に対する HARKing は発生していない。**G1-1〜G1-6 / G2-1〜G2-7 / DS-1〜DS-6 の既存の数値基準は一切緩めていない。**
  - **§3.1 E-2 を「識別できなければ差し戻す」から「§3.1.1 の決定的手続きで構成し除外する」へ確定させた。** 識別の原理は「配当基準日＝会計期末日」であり、会計期末日は `/fins/summary` の `CurFYSt` / `CurFYEn` / `CurPerEn` に、実績配当額は `Div1Q` / `Div2Q` / `Div3Q` / `DivFY` に存在する。基準日→権利落ち日の写像は **T+2 決済のもとで `b(R)` の1営業日前**と一意に決まる。**除権日フィールドが無いことは配当落ちが識別できないことを意味しない、というのが差し戻しに対する S の結論である。**
  - **E-2A（銘柄別・厳密）と E-2B（暦ベース・一括）の和集合**を除外することとした。E-2B を併置する理由は、優待等の同日権利落ち・配当落ち日の銘柄横断フロー歪み・`/fins` カバレッジ欠落を E-2A が救えないためである（実測: E-2A の1,859銘柄日のうち12件は非月末決算のため E-2B に覆われない＝両者は相互補完の関係にある）。
  - **「配当分を補正して使う」案を明示的に却下した。** 実際の落ち率は税制クライアンテレ効果により配当額の 0.7〜1.0 倍であり、補正は新たな自由パラメータを生む上、残差（≈0.21%、`σ_gap` の 0.17σ）が**配当利回りと相関して残る**。除外は規約、補正は自由度、という理由で除外を採った。
  - **§9-2b の検証ゲート V-1〜V-5 を新設した**（K-6 の対象に追加）。閾値は**税制（源泉徴収20.315%）・T+2決済・算術的自己検算という理論から導出**し、観測値に合わせて設定していない。S の設計時実測値（V-1 `b̂=−0.942`、V-2 プラセボ4点すべて `|b̂|≤0.075`、V-3 8日中7日、V-4 510/511、V-5 1015/1015）を**透明性のため全数開示**した上で、B実装チームに独立な再実行を要求する。
  - **§3.2 の `σ_gap` の窓を「直前60本（隣接）」から「`T[i−66]`〜`T[i−1]` の有効ギャップのうち新しい順にちょうど60本」へ改訂した。** 契機は推測ではなく実測である: 全銘柄日の **49.7%** が窓内に権利落ちギャップを含み、`σ` の膨張比は中央値 1.0033 ながら **p99 で 1.2725・最大 2.7873** に達する。膨張量は配当利回りと相関するため、高配当銘柄を系統的にイベント集合から押し出す横断的な選択歪みとなる。**「中央値が小さいから無視してよい」と推測せず、実測して対処した（N-6 の自己適用）。** 遡り上限 66 は実測された最大値 65 に 1 を足した値であり、カバー率 100.0000%（取りこぼし0件）。
  - **上記に伴い、イベント日の開始添字を `T[62]` から `T[68]` へ改めた**（`i−67 ≥ 1` の算術的帰結）。**§4.5 の算術整合性表を書き直し、「60本のギャップ ⇔ 終値61個」という第1版の対応が隣接の場合にのみ成立する特殊ケースであったことを明記**し、正しい対応（60本 ⇔ 始値60個＋終値60個）に修正した（T-8。EXP-OBS000037 で窓長が独自解釈された経路そのもの）。§2.2 のイベント日数を推定値 181/245 から実測値 **173/221** へ更新し、選定期間の期待件数が 159→152 と減っても T-1 の下限150を満たすことを確認した。
  - **§2.5 B-1 の事実誤認を訂正した。** 第1版は「契約2年で唯一のテール局面（2024年8月上旬）が測定窓の外」と書いたが、実測では **2025-04-07（−7.26%・選定期間）が 2024-08-05（−5.07%）より大きく、確認期間にも 2026-03-09（−4.68%）がある**。残る真の空白は「確認期間の独立なストレス・エピソードが実質1回」であると書き改めた。**これは本EXPに有利な方向の訂正であるため、合否ゲートは一切変更していないことを明記する。**
  - **追加・厳格化したゲート（いずれも緩める方向ではない）**: ① **DS-7**（確認期間の最大単日イベントシェア ≤ 25%。実測で `z*=−2.5` 時に単日 2025-04-07 が 10.7%・上位3日で27%を占めたため）、② **G1-7**（イベント件数が最大の1日を除いても `IC_bar` の符号と `R_5` グロス平均が正であること。除く日はリターンを見ずに一意に決まるためHARKingの余地がない）。
  - **司令塔判断事項 D-8**（E-2B が確認期間第2のストレス日 2026-03-30 を除外すること）を新設し、**S の決定として凍結した上で「報告」とした**（照会にすると事後の自由度が生まれるため）。**測定範囲の空白 B-7** を新設した。
- 2026-09-14: **初版作成。** S戦略チーム（chief-strategist・A設計官兼務）が `00-prescreen.md` の条件付きGO（必須条件①〜⑥・申し送り T-1〜T-10）を受けて起票。司令塔が 2026-09-14 に EXP-OBS000001（PEAD）を保留（凍結）とし本EXPへ着手する判断を下したことを受けたもの。
  - シグナル定義（`z = g/σ_gap`・60本窓）、イベント定義と除外規則 E-1〜E-9、約定モデル（**エントリーは `t_1` 始値に固定し `O(D)` 約定を先読みとして禁止**）、トレール4パラメータ（初期損切 **−4.0% 固定**・発動 +4.0%・追随 −2.0%・日足終値判定/翌営業日始値約定）、最大保有5営業日、リスク集中上限（同時5銘柄・建玉総額125万円・同一イベント日2件・同一 `S33` 2件）、コスト前提（往復0.45%・感度3点）、G1-1〜G1-6 / G2-1〜G2-7 / DS-1〜DS-6 / K-1〜K-6 の数値基準を**すべて回す前に固定**した。
  - **値幅制限の `UL`/`LL` 規約とユニバース定義 U-1〜U-7 は `research/EXP-OBS000001/01-spec.md` §4.0・§5.4 を参照により取り込み、独自に再解釈しない**こととした。確認期間ユニバース175銘柄は PEAD の実測結果を流用し、再現一致を §9-4 で確認する。
  - **決算発表予定日の取得口は `/fins/earnings-date` に固定した**（PEAD §7 D項が指定していた `/equities/earnings-calendar` は 3・9月期のみ・翌営業日分のみを返す設計で point-in-time 参照が不可能。既知の誤り）。
  - **選定期間の開始を `T[62]` としたのは、`σ_gap` の60本窓（終値61個・始値60個）と U-4 の60営業日窓が同時に要求する算術的帰結である**（§2.1・§4.5）。その代償として、契約2年で唯一のテール局面（2024年8月急落）が選定・確認のどちらの窓にも入らないことを B-1 として明記した。
  - **本 spec で prescreen から追加・厳格化した点**（いずれも緩める方向ではない）: ① **権利落ち・配当落ちの除外（E-1/E-2）を新設し、確定できない場合は実験を回さず差し戻す**ことを K-6 に規定（配当落ちは 3月末・9月末に集中し、方向が常に下向きで反転しないため、混入は安全側に働かない）、② **T-10 のコスト感度を G2-7 として合否ゲート化**（0.60% で損益分岐を割るなら中心ケース合格でも不合格）、③ **T-2 のデシル単調性を G1-5 として合否ゲート化**（`ρ_dec ≥ +0.30`）、④ **同一イベント日の新規建て上限を prescreen の例示 3件から 2件へ厳格化**（OBS000029 の時間方向再発の防止）、⑤ prescreen §7.2 の必要勝率 55.6% に**翌営業日始値約定ギャップ 0.30% を織り込んで 59.4% へ再計算**（§8.3）。
  - **司令塔判断事項 D-6（PEAD 確認窓との重複）・D-7（配当落ち識別が不能だった場合）**を新設した。

## 関連ドキュメント

- [00-prescreen.md](./00-prescreen.md) — 事前目利き（条件付きGO・必須条件①〜⑥・申し送り T-1〜T-10）
- [EXP-OBS000001/01-spec.md](../EXP-OBS000001/01-spec.md) — **§4.0（UL/LL 規約）と §5.4（ユニバース U-1〜U-7）を本 spec が参照により取り込む正本**
- [EXP-OBS000001/10-result/](../EXP-OBS000001/10-result/) — 参照営業日カレンダー `T`・確認期間ユニバース175銘柄・UL/LL 実測の出典
- [EXP-OBS000002/00-prescreen.md](../EXP-OBS000002/00-prescreen.md) — 却下済み。本EXPとのコスト構造の差は prescreen §5.3
- [PJ000001](../../obs/smart_wave_rider/00プロジェクト方針/PJ000001-日本株スイングトレードAI自動売買エンジン.md) — §4 C-3（エッジ族）/ C-4 / C-5 / §5.3（採否基準）/ §8（リスク管理・H-3）
- [PJ000002](../../obs/smart_wave_rider/00プロジェクト方針/PJ000002-マルチエージェント運営計画書.md) — 役割境界・差し戻し条件
- [portfolio-ledger.md](../portfolio-ledger.md) — 姉妹PJから輸入した負の資産（**OBS000022 の行が欠落中**。C品質チームは review 時に補完状況を確認すること）
- [STRATEGY-BRIEF.md](../STRATEGY-BRIEF.md) — 戦略的現在地
- [_templates/result.readme.md](../_templates/result.readme.md) — B実装チームの出力様式
