# 実験仕様書（Spec） - EXP-OBS000008（信用倍率〈信用取引残高〉単独の方向予測力検定）

> 担当: S戦略チーム（chief-strategist／A設計官を兼務）
> 鉄則: **成功基準は「回す前」に数値で確定する**（HARKing防止）。結果を見てから基準を緩めない。
> **本specは第1版であり、本ファイルのコミットをもってロックする。** 以降の改訂は変更履歴に理由とともに記録し、**§6の数値基準・§2の期間定義は変更しない。**

## 対応OBS番号

**OBS000008**（`research/`配下・`obs/smart_wave_rider/`配下のいずれにも既存の000008は無いことを確認して採番。EXP-OBS000004は「起票せず」で欠番）

## 変更履歴

- 2026-09-17: 初版作成（ロック）。

---

## 0. 本specの位置づけ（EXP-OBS000007の後継・単独G1検証限定）

**本EXPはEXP-OBS000007候補1（信用取引残高）の条件付きGO判定を受け、「信用倍率単独の方向予測力」のみを検定する。** PEAD（EXP-OBS000005・不採用）・ギャップ（EXP-OBS000006・不採用）のマグニチュード/タイミング検出器との複合戦略化は行わない（EXP-OBS000007 §3 原則1）。

| prescreenの条件 | 本specでの反映 |
|---|---|
| 符号仮説の凍結（変更禁止） | §1 |
| `T`（日次）の再利用・`W`（週次）の新設 | §2.0〜2.1 |
| 分割規則`m_w=floor(N_w/2)`・バッファ`b=4` | §2.2〜2.3 |
| 主指標`ΔM`の確定（`M`は感度分析のみ） | §3 |
| PIT公表ラグ＋安全マージン | §4 |
| データ十分性ゲートDS-1〜DS-6（`00-prescreen.md`で合格済み） | §6.0（B実装チームが候補集合再構成後に再評価） |
| K_eff（自己相関調整済み実効週数）ゲート | §6.1・G1-1b |
| ショート脚コストモデルの近似・限界開示 | §7 H項・§8 |

---

## 1. 仮説（1文）

**週次の信用倍率変化（`ΔM_w(j) = LongVol_w(j)/ShrtVol_w(j) − LongVol_{w'}(j)/ShrtVol_{w'}(j)`）が正に大きい（信用買い残が信用売り残に対して相対的に積み上がった）銘柄ほど、公表から一定の遅延を置いた後の5営業日株価リターンは、日本株TOPIX500相当ユニバースにおいて、統計的に有意かつ経済的に意味のある大きさで負に偏る。**

### 1.1 エッジ族の宣言

- 予測変数の生成過程: **信用取引残高（`LongVol`・`ShrtVol`という株数）のみ**。価格・出来高・ボラティリティを一切入力にしない。
- したがってPJ000001 §4 C-3が低事前確率とした「価格由来の特徴量から方向を予測する」族には属さない。
- **ただしPEAD（EXP-OBS000005）と同様、本EXPも「イベント後の一方向的な継続」という出力形を取る点で、価格由来のトレンドフォロー戦略と表面上似た統計的挙動を示しうる。この区別が本質的でなかった場合、本EXPも同じ天井に当たる可能性がある。この可能性はspec段階では排除できず、G1の結果で判定する。**

### 1.2 事前確率の見立て

**低〜中。** PEAD・ギャップがいずれもG1でKillされ、本PJの非価格系スリーブ在庫がゼロに戻った状況下での3件目の非価格系候補である。制度信用の返済期限という制度構造は演繹的に筋が通っているが、**日本株における信用取引残高の方向予測力について、一次文献による確認は本spec作成時点で行っていない**。文献の不在・存在いずれをも根拠に基準を緩めたり厳しくしたりしない。

---

## 2. 選定 / 確認プロトコル

### 2.0 参照カレンダー（2種類）

#### 2.0.1 日次カレンダー `T`（既存を再利用。再取得しない）

`T := research/EXP-OBS000005/01-spec.md §2.0が確定したTと同一`。`N=2,441`、`T[1]=2016-09-15`、`T[N]=2026-09-14`。**B実装チームは新規候補集合再構成後、`T`を再構成し上記と一致することを確認する。一致しなければS に差し戻す。**

#### 2.0.2 週次カレンダー `W`（本EXPで新設）

`W := data/raw/margin_interest/*.json`（候補554銘柄）に出現する`Date`の和集合を昇順に並べた列。1始まりの添字。`N_w := |W|`。**実測値（`00-prescreen.md` §9.1）**: `N_w=507`、`W[1]=2016-09-23`、`W[N_w]=2026-09-11`。

**B実装チームは§9-1の候補集合再構成後に`W`を再構成し、上記と一致することを確認する。一致しない場合は実験を回さずSに差し戻す。**

### 2.1 分割規則

`m_w := floor(N_w/2)`。バッファ`b := 4`（§2.2の算術から導出。`00-prescreen.md` §3.1参照）。

| 区分 | シグナル週の範囲 | 役割 |
|---|---|---|
| 選定（参照） | `w = 1 〜 (m_w − b) = 1〜249` | 事前固定設計の参照測定。何も選ばない |
| 確認（未見） | `w = (m_w+1) 〜 (N_w−b) = 254〜503` | G1・G2の合否はこの期間のみで判定する |

- 実測（`00-prescreen.md` §9.1）: `m_w=253`。選定範囲`w=1〜249`（`W[1]=2016-09-23`〜`W[249]`）、確認範囲`w=254〜503`（`W[254]=2021-09-24`〜`W[503]`）。**実日付・実添字はB実装チームが機械的に求め、ハードコードしない。**

### 2.2 前方窓の算術的整合性（**T-8・trading-app-v2 EXP-OBS000037の教訓**）

週`w`の基準日を`D=W[w]`、`k := idx_T(D)`（`T`内の位置）とする。

| # | 用語 | 営業日の**本数** | 対象となる`T`の添字 | 参照する価格の個数 |
|---|---|---|---|---|
| W-1 | 公表可能日（PIT availability） | 基準日の3営業日後 | `T[k+3]` | — |
| W-2 | エントリー日`t_1` | 公表可能日の翌営業日 | `T[k+4]` | 始値`O(T[k+4])`1個 |
| W-3 | **予測単位`R_5`（H=5営業日）** | ホライズン5本 | `t_1=T[k+4]`〜`t_5=T[k+9]` | **`R_5 = C(T[k+9]) / O(T[k+4]) − 1`。始値1個＋終値1個＝価格2個**（隣接差分の連鎖ではない） |
| W-4 | 最大保有（10営業日・G2のみ）＋決済1本 | 保有10本＋決済1本 | `t_1〜t_10`、決済は`t_11=T[k+14]`の始値 | 添字は`k+14`まで |
| W-5 | `BUY_BLOCKED`/`SELL_BLOCKED`による持ち越し上限 | ＋5本 | 最悪で`T[k+19]` | 添字は`k+19`まで |
| **W-6** | **⇒ 1週次イベントに必要な`T`の添字範囲** | — | **`k+3`〜`k+19`** | — |

- したがって週`w`は次を満たす必要がある: **選定期間 `k+19 ≤ idx_T(W[m_w−b])に対応する上限`／確認期間 `k+19 ≤ N`**。B実装チームは`w`ごとに`k+19`が`T`の範囲内であることを機械的に検証し、範囲外のイベントは前方窓が確保できないとして除外・件数記録する（除外が発生する見込みは低いが、発生した場合の扱いを明記しておく）。
- **選定期間側にも同じ余白（バッファ`b=4`週）を置く。** 余白が無いと選定期間末尾の週の前方窓（`k+19`）が確認期間先頭の週の価格を観測してしまう。これは基準の緩和ではなく分離の強化である（EXP-OBS000005 §2.2と同じ論理）。

### 2.3 本EXPにおける「選定」の自由度はゼロである

主指標（`ΔM`）・予測ホライズン（H=5）・PIT安全マージン（+1営業日）・トレール4パラメータ・閾値・ユニバース条件・コスト前提の**すべてを回す前に数値で固定している**。**選定期間で選ぶパラメータは1つも存在しない。** B実装チームは選定期間の結果を見てspecのいかなる値も変更してはならない。変更が必要と判断したら実装を止めてSに差し戻す。

### 2.4 測定範囲の空白（申し送り）

| # | 空白 | 内容 |
|---|---|---|
| B-1 | サバイバーシップ・バイアス（§9-1で解消するまで） | 候補554は既存候補集合の代理であり、期間内で上場廃止・規模区分脱落した銘柄が構造的に欠落しうる |
| B-2 | 公表ラグの祝日パターン未検証（`00-prescreen.md` L-3） | PIT安全マージン+1営業日でも吸収しきれない例外が理論上残る |
| B-3 | 信用規制（日々公表銘柄・増担保規制）模擬不能（C-5と同型） | Standardプランで解禁されている可能性があるが本EXPでは使わない |
| B-4 | ショート脚コスト（貸株料・逆日歩・空売り規制）の近似（`00-prescreen.md` §10.5） | G2は楽観側に偏る |
| B-5 | 2026-09-28の信用取引残高公表仕様変更（週次→毎営業日） | 本EXPのデータ・結果は現行仕様（週次）のみに基づき、新仕様への外挿はしない |
| B-6 | 取引時間の構造変化（2024-11-05 大引け15:00→15:30） | §7 C項で前後別記述統計を出力するが合否判定には使わない |

---

## 3. シグナル定義

### 3.1 主指標（`00-prescreen.md` §4.2で確定・変更禁止）

**`ΔM_w(j) := M_w(j) − M_{w'}(j)`、`M_w(j) := LongVol_w(j) / ShrtVol_w(j)`。`w'`は銘柄`j`自身の直前の有効観測（§3.2の除外規則を適用した後）。**

**`M_w(j)`（水準）は感度分析としてのみ算出し、合否判定には使わない。** `M`側の結果が仮に主指標より良くても、それを理由に主指標を差し替えることはHARKing（K-4）として禁止する。

### 3.2 欠損・異常値の除外規則（`00-prescreen.md` §4.3を継承。変更しない）

| # | 規則 |
|---|---|
| E-1 | `ShrtVol_w(j)=0`の週は当該銘柄・当該週を観測から除外 |
| E-2 | `ΔM`算出に用いる直前観測`w'`の`ShrtVol_{w'}(j)=0`の場合も除外 |
| E-3 | 直前観測`w'`との暦日差が21日を超える場合、`ΔM`を「連続した週次観測」とみなさず除外（記録は行うが除外理由の詳細特定は求めない） |
| E-4 | 同一銘柄・同一`Date`の重複レコードは`DiscNo`相当のフィールドが存在しないため、**後勝ち（配列内の後方の要素を採用）**とし件数を記録する（実測では0件） |

### 3.3 標準化

- 主検定（Spearman順位相関）には追加の標準化を行わない。
- `ΔM`・`R_H`ともにwinsorizeは行わない。
- **`ΔM_w(j)`の分布（極端値の有無）をB実装チームが§9タスクとして出力する**（合否判定には使わない。将来の再設計の参考情報）。

### 3.4 事前登録した感度分析（合否判定に使ってはならない）

| # | 軸 | 水準 |
|---|---|---|
| SA-1 | 主指標 | `ΔM_w`（**主**）／`M_w`（水準） |
| SA-2 | PIT安全マージン | +0営業日（確認済み最小ラグそのまま）／**+1営業日（主）** |
| SA-3 | 予測ホライズン H | 3営業日／**5営業日（主）**／10営業日 |
| SA-4 | 往復コスト | 0.25%／**0.35%（主）**／0.50% |

**合否判定に用いる検定は§6の主検定のみ**（`ΔM_w` × PIT+1営業日 × H=5 × コスト0.35%）。多重性の補正は不要。SA-1〜SA-4は記述統計であり検定として解釈してはならない。

---

## 4. Point-in-Time 規則・約定モデル

### 4.1 PIT公表可能日（`00-prescreen.md` §5.2を継承）

`AvailableDate(W[w]) := T[idx_T(W[w]) + 3]`。エントリー日`t_1 := T[idx_T(W[w]) + 4]`（公表可能日の翌営業日、始値でエントリー）。

### 4.2 値幅制限フラグの解釈（PEAD/ギャップと同一の判定式を再利用）

```
BUY_BLOCKED(t)  = (Vo(t)==0) or (O(t) is null) or (UL(t)==1 and O(t)==H(t))
SELL_BLOCKED(t) = (Vo(t)==0) or (O(t) is null) or (LL(t)==1 and O(t)==L(t))
```

`UL`/`LL`は0/1フラグ。JPX制限値幅表からの自前再構成は禁止。

### 4.3 予測単位（G1）の前方リターン

`R_5 = C(T[k+9]) / O(T[k+4]) − 1`（グロス。コスト・スリッページを一切引かない）。**ポジションを取らない純粋な統計量であり、`BUY_BLOCKED`/`SELL_BLOCKED`による約定可否はG1には適用しない**（G1はPJ000001の定義どおり「手数料ゼロのグロス段階」の測定であり、執行可能性はG2で扱う）。

### 4.4 G2の約定モデル（**長短両建て。ショート脚は本PJで初めて必要になる規則**）

デシル1（`ΔM`最小＝将来上昇を予測）を**ロング**、デシル10（`ΔM`最大＝将来下落を予測）を**ショート**とする、市場中立の長短ポートフォリオ。

#### 4.4.1 ロング脚（デシル1）

| # | パラメータ | 固定値 |
|---|---|---|
| 1 | エントリー | `t_1=T[k+4]`の始値。`BUY_BLOCKED(t_1)`なら見送り |
| 2 | 初期損切幅 | エントリー価格の**−3.0%** |
| 3 | トレール発動水準 | 終値ベースの含み益が**+4.0%**以上に到達した日の翌営業日から有効 |
| 4 | 追随幅 | 発動後、日次終値の最高値から**−2.0%** |
| 5 | 決済判定 | `SELL_BLOCKED(t_exit)`なら約定不能・持ち越し（最大5営業日、超過時は5営業日目終値で強制評価） |
| 6 | 最大保有 | 10営業日（`t_10`終値時点で未決済なら`t_11`始値で強制手仕舞い） |

#### 4.4.2 ショート脚（デシル10・PJ本PJ初のショート約定モデル）

ロング脚のすべてのパラメータを**符号鏡像**で適用する。

| # | パラメータ | 固定値（ロング脚との対応） |
|---|---|---|
| 1 | エントリー（空売り建て） | `t_1=T[k+4]`の始値。**`SELL_BLOCKED(t_1)`なら見送り**（空売りは「売る」行為であり、ロングのエントリー〈買う〉ではなく決済〈売る〉と同じ値幅制限が適用される） |
| 2 | 初期損切幅 | エントリー価格の**+3.0%**（価格上昇＝ショートにとって不利） |
| 3 | トレール発動水準 | 終値ベースの含み益（価格下落）が**−4.0%**以上に到達した日の翌営業日から有効 |
| 4 | 追随幅 | 発動後、日次終値の**最安値**から**+2.0%**（価格が最安値から+2.0%戻したら手仕舞い） |
| 5 | 決済判定（買い戻し） | **`BUY_BLOCKED(t_exit)`なら約定不能・持ち越し**（買い戻すために「買う」行為であり、ロングのエントリーと同じ値幅制限が適用される。最大5営業日、超過時は5営業日目終値で強制評価） |
| 6 | 最大保有 | 10営業日（`t_10`終値時点で未決済なら`t_11`始値で強制買い戻し） |

**この鏡像対応（エントリー=SELL_BLOCKED判定・決済=BUY_BLOCKED判定）は、既存の`BUY_BLOCKED`/`SELL_BLOCKED`定義を「売買方向」ではなく「売り注文か買い注文か」で解釈することにより、新しい判定式を1つも追加せずに導出される。** ロングのエントリー（買い注文）とショートの決済（買い戻し注文）は同じ`BUY_BLOCKED`、ロングの決済（売り注文）とショートのエントリー（空売り注文）は同じ`SELL_BLOCKED`を使う。

### 4.5 決済できない場合（値幅制限の模擬。ロング・ショート共通の一般規則）

該当する`BLOCKED`条件が真なら約定できず持ち越し。以降の各営業日で再評価し、偽となった最初の営業日の始値で約定する。その間の含み損益はすべて損失として計上する。最大5営業日持ち越しても約定できない場合、5営業日目の終値で強制的に評価損益を確定し「強制評価」として件数を記録する。

---

## 5. パイプライン統合

### 5.1 シグナル・ポジション

- 各週`w`（確認期間）について、有効`ΔM_w`を持つ銘柄をデシルに分割し、**デシル1（最小・ロング）とデシル10（最大・ショート）に等金額を配分する市場中立ポートフォリオ**を組む。
- **ロングオンリー版・ショートオンリー版も感度分析として別集計する**（合否判定には使わない。§4.5 B-4の限界がショートオンリー版により顕著に現れる可能性があるため）。

### 5.2 ユニバース（EXP-OBS000005 §5.4のU-1〜U-7を再利用。追加でU-8を新設）

| 手順 | 条件 | 出典 |
|---|---|---|
| U-1 | 規模区分がTOPIX500相当（`ScaleCat`） | EXP-OBS000005を継承 |
| U-2 | 制度信用で買建て可能（`Mrgn`/`MrgnNm`） | 同上 |
| U-3 | 業種区分を記録（`S33`/`S33Nm`） | 同上 |
| U-4 | 確定日直前60営業日の日次売買代金`Va`の中央値≥5億円 | 同上 |
| U-5 | 確定日時点の終値が1,000円以上3,500円以下 | 同上 |
| U-6 | 通過銘柄が175超なら`Va`中央値降順で上位175銘柄 | 同上 |
| U-7 | 通過銘柄が150未満なら株価帯を700〜8,000円に緩和 | 同上 |
| **U-8（新設）** | **当該週`w`について有効`ΔM_w(j)`が算出できること**（§3.2のE-1〜E-4いずれにも該当しない） | 新設。信用倍率固有のデータ可用性フィルタ |

**U-2（信用買建て可能）はロング脚のみに直接対応する。ショート脚（空売り建て）が可能かどうかを示すフィールド（一般信用売り可能銘柄・貸借銘柄の別）はU-2の`Mrgn`区分だけでは正確に判定できない可能性がある。B実装チームは§9タスクとして`Mrgn`/`MrgnNm`の値がショート建て可否とどう対応するかを実測し、対応が不明確な場合は「制度信用の貸借銘柄のみをショート対象とする」という保守的な近似を適用してS に報告する。**

### 5.3 リスク・建玉上限

運用資本100万円・レバレッジ2.0倍（建玉枠200万円）。ロング脚・ショート脚それぞれに建玉枠の半分（100万円相当）を上限とする（市場中立設計のため）。同時保有上限・業種上限・単元100株の離散化はEXP-OBS000005 §5.3の固定値を継承する。

---

## 6. 成功 / 不採用の数値基準（**回す前に確定**）

### 6.0 データ十分性ゲート（`00-prescreen.md` §6で全項目合格済み。**B実装チームは候補集合再構成後に再評価する**）

| # | 条件 | 閾値 | prescreen実測 |
|---|---|---|---|
| DS-1 | 確認期間の有効「銘柄週」観測数 | ≥300件 | 132,310件（暫定・再評価要） |
| DS-2 | 確認期間の「5銘柄以上の有効`ΔM`を持つ週」数`K` | ≥30週 | 254週（暫定） |
| DS-3 | 確認期間に1回以上出現する候補銘柄数 | ≥150銘柄 | 554銘柄中502〜536／週（暫定） |
| DS-4 | 候補ユニバース×全期間の日足欠損営業日率 | ≤5% | 0.00000%（EXP-OBS000005実測の援用） |
| DS-5 | 確認期間の`ShrtVol=0`除外率 | ≤10% | 0.0446%（暫定） |
| DS-6 | 週次グリッドの異常ギャップ率 | ≤1% | 0.0007% |

**1つでも未達なら G1 は「判定不能」とし、Kill ではなく保留（K-5）とする。** ゲート未達のまま分割規則・バッファ・閾値を変えて再測定することを禁止する（K-4）。

### 6.1 G1主検定

**主指標: 週内クロスセクションSpearman順位相関の等加重平均（`IC_bar`）**

1. 確認期間の各週`w`について、その週に有効`ΔM_w`を持つ銘柄群の`ΔM_w`と`R_5`の**Spearman順位相関`IC_w`**を算出する。タイは平均順位で処理する。
2. **1週あたりの有効銘柄数が5未満の週は`IC_bar`の算出から除外**する（除外週数・除外銘柄数を記録）。残った週数が`K`（DS-2/C7-1と同一の量）。
3. **`IC_bar` = 対象週の`IC_w`の単純平均**（週を等加重）。
4. **有意性検定 = 週内permutation**: 同一週ブロックの内部でのみ`ΔM_w`をシャッフルし`IC_bar`を再計算。**10,000回**。**乱数シード = 20260917**（固定・変更禁止）。**片側p値**（`IC_bar`が観測値以下に出た比率。**仮説は負の相関を予測するため、帰無分布より観測値が小さい側を検定する**）= `(1 + #{IC_bar_perm ≤ IC_bar_obs}) / (1 + 10,000)`。
5. **`K_eff`の算出（`00-prescreen.md` §6.1で規則を凍結済み）**: `IC_w`系列（週次時系列）のラグ1〜`L`自己相関`ρ_l`を算出し、`L = floor(4 × (K/100)^(2/9))`（Newey-West自動帯域幅）。`K_eff := K / (1 + 2×Σ_{l=1}^{L}(1−l/L)×ρ_l)`。

### 6.2 G1合格条件（**すべて満たすこと**）

| # | 条件 | 閾値 |
|---|---|---|
| **G1-1** | 確認期間の`IC_bar` | **< 0 かつ 週内permutationの片側p < 0.05**（観測値が帰無分布より負側に有意） |
| **G1-1b（新設）** | 自己相関調整済み実効週数`K_eff` | **≥ 15** |
| **G1-2** | 選定期間の`IC_bar` | **< 0**（符号一致。有意性は問わない） |
| **G1-3** | 確認期間のデシル1（最小`ΔM`）平均`R_5` − デシル10（最大`ΔM`）平均`R_5` | **≥ +0.70%**（片道往復コスト0.35%×2脚分。事前に機械的に導出した値） |
| **G1-4** | 確認期間のデシル10の市場超過`R_5`平均 **< 0** かつ デシル1の市場超過`R_5`平均 **> 0** | 両方成立 |
| **G1-5** | デシル単調性 | デシル番号とデシル平均`R_5`のSpearman順位相関`ρ_dec` **≤ −0.30** |

**G1-1〜G1-5（G1-1bを含む）のいずれか1つでも未達ならG1不合格とし、G2以降は測定しない。**

### 6.3 デシル分析・記述統計（必須出力）

1. `ΔM_w`で確認期間の観測を10デシルに分け、各デシルの`R_5`グロス平均・中央値・件数・標準偏差を出力。
2. 市場超過リターン: ユニバース全銘柄の等加重平均リターン（同一週`t_1`〜`t_5`）を市場プロキシとする。
3. セクター中立リターンも出力（合否判定には使わない）。
4. 閾値非単調性チェック（SA相当）: 上位/下位5%・10%・20%・30%の各閾値での平均リターン。
5. **週集中度チェック（OBS000029診断）**: 1週あたり有効銘柄数の度数分布、確認期間の総`R_5`合計に対する上位3週の寄与率（`00-prescreen.md` §9.4の実測ではC7-2が0.405%と極めて低く、構造的にこの罠は起きにくいと見込まれるが、正式な数値として出力する）。
6. **`IC_w`系列の自己相関プロット相当（ラグ1〜10の`ρ_l`の表）**を出力し、`K_eff`の計算過程を再検証可能にする。
7. 取引時間変更（B-6）前後別の記述統計（合否判定には使わない）。
8. レジーム別分解（暦年・四半期別）。部分集合の好成績を全体の合格根拠にしない。
9. `M_w`（水準）を用いた同一分析一式（SA-1。合否判定には使わない）。
10. PIT安全マージン+0営業日版の`IC_bar`（SA-2。合否判定には使わない）。

### 6.4 G2合格条件

| # | 条件 | 閾値 |
|---|---|---|
| G2-1 | 確認期間のコスト控除後平均トレードリターン（建玉金額比・ロング/ショート両脚込み） | > 0.00% |
| G2-2 | 確認期間のコスト控除後累積純リターン（資本100万円比） | > 0.0% |
| G2-3 | 選定期間と確認期間でG2-1の符号が一致 | 両期間とも > 0.00% |
| G2-4 | 確認期間の最大ドローダウン（資本比） | ≤ 15.0% |
| G2-5 | 確認期間の実約定トレード数（両脚合計） | ≥ 30件 |
| G2-6 | 確認期間の約定不能率 | ≤ 50% |
| **G2-7（新設）** | ロング脚単独・ショート脚単独それぞれのコスト控除後平均トレードリターン | **両方を個別に出力**（合否判定には使わない。§7 H項の近似がショート脚にどう影響したかを切り分けるための必須開示） |

**Sharpeは算出・報告するがG2の合否判定には用いない**（PEAD/ギャップと同じ扱い）。95%信頼区間を併記する。

### 6.5 G2で必ず出力する生データ

`research/EXP-OBS000005/01-spec.md` §6.5のP-1〜P-13をそのまま要求する。加えて**ロング脚・ショート脚別の内訳**（P-14、新設）を要求する。欠けている場合、C品質チームは自動的に「差し戻し」とする。

### 6.6 / 6.7 G3（低相関）・G4（限界寄与）

**本EXPでは判定不能**（スリーブ在庫ゼロ）。数値基準はPJ000001 §5.3のまま据え置く。**測定はしないが、確認期間の日次スリーブPnL系列を`pipeline.json`に保存する。**

### 6.8 Kill基準

| # | 該当したら | 扱い |
|---|---|---|
| K-1 | §6.2のG1-1〜G1-5（G1-1b含む）のいずれかが未達 | **Kill**。G2以降は測定しない |
| K-2 | 選定期間で`IC_bar`が符号仮説と一致するが確認期間で消失 | **Kill**（過学習） |
| K-3 | G1合格だが§6.4のG2-1〜G2-6のいずれかが未達 | **Kill**（trading-app-v2 OBS000025型） |
| K-4 | 本specの基準を事後に緩めないと通らない | **Kill**（HARKing） |
| K-5 | §6.0のデータ十分性ゲート未達 | Killではなく保留。司令塔判断に上げる |
| K-6 | §9タスクで解消できない矛盾・欠落が見つかる | 実験を回さずSに差し戻し |
| **K-7（新設）** | **主指標を`ΔM`から`M`（水準）へ、または符号を反転させて再検証する** | **禁止行為そのものであり、行った場合は当該変更をHARKingとして扱いK-4に該当** |

---

## 7. 日本株固有の模擬条件

| # | 項目 | 実装ルール | 状態 |
|---|---|---|---|
| A | 単元100株の離散性 | EXP-OBS000005 §7 A項を継承。ロング・ショート双方で実効ポジションサイズと目標の乖離を記録 | [ ] 実装必須 |
| B | 値幅制限 | §4.2の判定式。ロングのエントリー=BUY_BLOCKED／決済=SELL_BLOCKED、ショートのエントリー=SELL_BLOCKED／決済=BUY_BLOCKED（§4.4.2） | [ ] 実装必須 |
| C | 取引時間と約定タイミング | EXP-OBS000005 §7 C項を継承（`O`=寄り付き、`C`=大引け） | [ ] 実装必須 |
| D | 決算跨ぎ制約 | `/fins/earnings-date`を使用。EXP-OBS000005 §7 D項と同一の読み替え規則（未発表の予定日を跨ぐポジションを持たない）を、ロング・ショート双方に適用 | [ ] 実装必須 |
| E | 委託保証金率・最低委託保証金30万円 | EXP-OBS000005 §7 E項を継承 | [ ] 実装必須 |
| F | 信用規制（日々公表銘柄・増担保規制） | 模擬不能（B-3）。バックテストは楽観側に偏る | [ ] 明記必須 |
| G | 制度信用買方金利（ロング脚） | 年率2.8%×実保有暦日数÷365 | [ ] 実装必須 |
| **H** | **ショート時の貸株料・逆日歩・空売り規制銘柄の除外（本PJで初めて適用対象）** | **貸株料: 年率3.0%×実保有暦日数÷365（固定近似）。逆日歩: モデル化しない（データ未取得・楽観側の偏り）。空売り規制フラグ: 適用しない（データ未取得・楽観側の偏り）。両偏りをG2結果の解釈時に必ず注記する** | [ ] 近似あり・限界明記必須 |

---

## 8. コスト前提

### 8.1 適用するコストモデル（`cost-model.json`に出力）

| 項目 | 値 | 出典 |
|---|---|---|
| スリッページ（片道・両脚共通） | 0.075% | EXP-OBS000005 §8.1を継承 |
| 往復コスト（主・片道脚あたり） | 0.35% | 同上 |
| 買方金利（ロング脚） | 年率2.8%×実保有暦日数÷365 | 同上 |
| **貸株料（ショート脚・新設）** | **年率3.0%×実保有暦日数÷365** | `00-prescreen.md` §10.5 |
| 逆日歩 | モデル化しない（限界開示） | 同上 |

### 8.2 感度分析（必須出力・合否判定には使わない）

往復0.25%/0.35%（主）/0.50%の3点。貸株料についても2.0%/3.0%（主）/5.0%の3点を追加感度として出力する。

### 8.3 損益分岐

G1-3が要求するデシル間スプレッドの下限＝+0.70%/トレードペア。これを下回るグロスエッジは構造上G2を通過し得ない。

---

## 9. B実装チームが「回す前」に先に潰すタスク

**§9-0〜§9-10をすべて完了し`10-result/feasibility.json`に出力してからG1に進む。1つでも未達ならSに差し戻す。**

| # | タスク | 出力 | 未達時 |
|---|---|---|---|
| 9-0 | 既存の`data/raw/margin_interest/`・`data/raw/pead/bars_daily/`を**上書き・再取得・削除しない**（`--force`禁止）。作業前にバックアップを取得 | `params.json`の`raw_data_backup_path` | バックアップ未完了のまま進むことを禁止 |
| 9-1 | `D_sel`（選定期間ユニバース確定日）・`D_conf`（確認期間ユニバース確定日）時点の`/equities/master`を追加取得し、候補銘柄集合を「両確定日のU-1・U-2通過銘柄の和集合」として再構成する | `candidate_codes_v3.json` | 取得不能ならSに差し戻す（K-6） |
| 9-2 | 再構成した候補集合で`T`・`W`を再構成し、§2.0.1・§2.0.2の実測値と照合する | `params.json` | 一致しなければSに差し戻す（K-6）。実日付をハードコードして辻褄を合わせない |
| 9-3 | DS-1〜DS-6を再評価する。**リターンを一切参照しない** | `feasibility.json` | 1つでも未達ならG1へ進まずSに差し戻す（K-5） |
| 9-4 | `Mrgn`/`MrgnNm`の値とショート建て可否（一般信用売り可能・貸借銘柄の別）の対応関係を実測し、§5.2のU-2注記の近似が必要かどうかを判定する | `params.json`の`field_value_mapping` | 一意に決まらなければ保守的近似（貸借銘柄のみ）を採用しSに報告 |
| 9-5 | 全507週（再構成後の候補集合ベース）について`W[w] ∈ T`であることを検証する | `feasibility.json` | 含まれない週があれば件数を記録しSに差し戻す（K-6） |
| 9-6 | §2.2のバッファ`b=4`が実際に選定/確認境界で前方窓の重なりを排除できているかを、`T`の実添字で全数検証する | `feasibility.json` | 重なりが検出されたら`b`を機械的に拡大しSに差し戻す（K-6。緩和ではなく安全側拡大のみ許容） |
| 9-7 | `ΔM_w(j)`の分布（極端値・ロングテールの有無）を出力する | `feasibility.json` | — |
| 9-8 | 決算跨ぎ制約（§7 D項）に`/fins/earnings-date`を使用する。`/equities/earnings-calendar`は使用禁止 | `feasibility.json` | — |
| 9-9 | 価格系列の人為的な段差（分割・併合）による前方リターン汚染の検査（EXP-OBS000005 §9-10と同型）。確認期間・選定期間それぞれについて、イベントの`[t_1,t_19]`窓に`AdjFactor≠1`の行を含む件数を出力する | `feasibility.json` | 1件でも存在すればG1解釈前にSに差し戻す |
| 9-10 | `UL`/`LL`の全ユニーク値・null/欠損行の件数と欠損率を出力する | `feasibility.json` | §4.2のパース規則を満たさない値が出現したらSに差し戻す（K-6） |

---

## 10. B実装チームへの指示（禁止事項）

### 10.1 共通

判定語を書かない。生データのみを出力する。パラメータ・閾値・分割・seedをチューニングしない。成功基準を事後に緩めない。成果物の実在を確認してから完了報告する。

### 10.2 本EXP固有

| # | 禁止事項 |
|---|---|
| N-1 | 選定期間の結果を見てspecのいかなる値も変更しない |
| N-2 | 分割規則`m_w=floor(N_w/2)`・バッファ`b=4`・§6.0のDS閾値・§6.2/§6.4の合格条件を変更しない |
| N-3 | `T`・`W`の再構成結果が§2の表と食い違った場合、表に合わせてハードコードしない |
| N-4 | U-4の窓長60営業日・PIT安全マージン+1営業日を短縮・延長・「取得できた分で代用」しない |
| N-5 | SA-1〜SA-4の感度分析の結果を合格根拠にしない |
| N-6 | §6.1-2の`K`と§6.0のDS-2/C7-1が同一の値になることを確認する。食い違えば差し戻す |
| N-7 | DSゲート未達のままG1以降を実行しない |
| N-8 | `/equities/earnings-calendar`を使わない |
| N-9 | 曖昧さを「安全側だろう」と推測して埋めない。一意に決まらなければSに差し戻す |
| N-10 | 既存の日足・信用取引残高キャッシュを上書き・再取得・削除しない（`--force`禁止） |
| **N-11（新設）** | **主指標`ΔM`を`M`（水準）へ、または相関の符号を反転させて再検証しない**（K-7・K-4） |
| **N-12（新設）** | **ショート脚の貸株料・逆日歩の近似（§7 H項）を、結果を良く見せる方向に調整しない**（例: 逆日歩を都合よく低く見積もって追加する等） |

---

## 11. 使用スクリプト / 再現方法（想定）

| 用途 | スクリプト |
|---|---|
| prescreen段階の代理測定（参考・再現用） | `scripts/margin_prescreen_counts.py` |
| 候補集合の再構成・master追加取得 | `scripts/margin_fetch_data.py`（`--step master`拡張）・`scripts/margin_build_universe.py`（新規） |
| 先行タスク§9-0〜§9-10 | `scripts/margin_feasibility.py`（新規） |
| G1測定 | `scripts/margin_g1.py`（新規） |
| G2測定（ロング・ショート両脚） | `scripts/margin_g2.py`（新規） |

すべて決定的であること。乱数を使うのは§6.1-4のpermutationのみで、シード20260917を固定する。出力先: `research/EXP-OBS000008/10-result/`。

---

## 12. 本specが測定しないこと（申し送り）

1. **G3（低相関）・G4（限界寄与）**（スリーブ在庫ゼロのため判定不能）。
2. **PEAD/ギャップ型検出器との複合戦略化**（EXP-OBS000007 §3 原則1により、本EXPがG1相当に合格し、かつ別のマグニチュード/タイミング検出器が用意できてから、別EXPとして起票する）。
3. **信用規制の影響**（模擬不能・B-3）。
4. **逆日歩・空売り規制の正確なコスト**（近似のみ・B-4）。
5. **2026-09-28以降の新仕様（毎営業日公表）下でのデータでの再検証**（B-5）。
6. **STEP3フォワード較正での実約定**。

---

## 13. 司令塔判断を要する事項

| # | 事項 |
|---|---|
| D-20（新設） | **ショート脚のコストモデル（貸株料年率3.0%・逆日歩モデル化なし）の妥当性。** より正確な逆日歩データの調達（有償の可能性）を検討するか、本EXPの近似のまま進めるかは司令塔判断を要する。ただしG1（予測単位）はショート脚のコストに一切依存しないため、**G1測定はこの判断を待たずに実行できる** |
| D-3（継続） | `portfolio-ledger.md`のOBS000022の行の欠落が未解消。C品質チームはreview時に補完状況を確認すること |
| D-21（新設） | **2026-09-28の公表仕様変更後、新仕様データでの再検証を新規EXPとして起票する優先度。** 本EXPがG1合格した場合、新仕様移行後のデータ蓄積を待つか、現行仕様の10年データのみで採否判断を確定するかは司令塔判断を要する |

---

## 関連ドキュメント

- [00-prescreen.md](00-prescreen.md)（本EXPのprescreen。§0〜§8は実測前に固定）
- [research/EXP-OBS000007/00-prescreen.md](../EXP-OBS000007/00-prescreen.md)（符号仮説の凍結元・条件付きGO判定）
- [research/EXP-OBS000005/01-spec.md](../EXP-OBS000005/01-spec.md)（`T`・U-1〜U-7・約定モデルの継承元）
- [research/EXP-OBS000006/01-spec.md](../EXP-OBS000006/01-spec.md)（同型の10年データ設計の参考）
- [research/STRATEGY-BRIEF.md](../STRATEGY-BRIEF.md)
- [research/ACTIVE.md](../ACTIVE.md)
