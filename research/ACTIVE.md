# ACTIVE - 進行状況の信号機

> 担当: E進行チーム（archivist-pm）が更新する**機械的な進行状況**。
> 「今どの実験が、誰の手にあり、次は誰待ちか」を1画面で表す。
> ※ 戦略的な勝敗判断・次の一手は `STRATEGY-BRIEF.md` を参照。

## 現在進行中の実験

| 実験ID | 仮説（要約） | 現在の担当 | 次の待ち | 更新日 |
|---|---|---|---|---|
| EXP-OBS000003 | 寄付の板寄せで前日終値から大きく**下方**に乖離して始まった銘柄のうち、**決算開示に起因しないもの**は、その乖離の一部を続く数営業日で回復し、往復コスト0.45%控除後も正の期待値を残すか（イベント駆動・ロングオンリー・日足） | **保留（判定不能・K-5）。S戦略チームが §9-6 DS ゲート不合格への対応を完了し、spec を第3版へ改訂（2026-09-14）。不採用ではない。**<br>**S の結論（`01-spec.md` §6.0.1）: 本 spec の枠内で §6.0 データ十分性ゲートを満たす再設計は存在しない。** 決定的根拠は **DS-6 が `z*` に依存しない量**であること——DS-6 はプール `P`（`z ≤ −1.5`・N-9 で凍結）の日次ブロック数であり、`z*` は `P` の定義に入らない。**較正グリッドをどう引き直しても実測 62日は動かず、閾値100を満たさない**（論理的帰結であり推測ではない）。<br>**再設計案の検討結果**: ①グリッド拡張（DS-2a の修復）は DS-5・DS-7 を一方的に悪化させ、DS-6 は不変 → 無意味 ／ ②DS-5・DS-7 を満たす `z*` の窓の探索は**意図的に行わない**（確認期間のカウント分布を見てから較正設計を選ぶことは K-4 と外形上区別できず、かつ DS-6 により無効） ／ ③選定/確認の分割解消は PJ000002 §6 の鉄則違反で禁止 ／ ④プール下限 `−1.5` の緩和は N-9 違反 ／ ⑤市場残差ギャップ版（R1）は原因に効くが**事前登録シグナル定義の変更＝新規EXP**であり、かつ**賭けの対象（市場ストレス時の需給の偏り）を取り除く設計のため事前確率は本EXPより低い**。<br>**基準は1つも緩めていない。確認期間（2025-07-01〜）のリターンは未見のまま温存**（PEAD の確認窓も同様に無傷）。<br>**実測が明らかにした構造（本EXPの主要な産物）**: 日本株のオーバーナイト・ギャップは市場共通因子に支配され、`σ` 正規化した極端下方ギャップは**少数の市場ストレス日に集中する群発過程**である（確認期間プール1,586件が167日に分布・5件以上は62日／イベント106件が25日に集中・単日 `2026-03-09` が52.8%）。加えて**選定と確認でテール分布が構造的に異なる**（`z` の p01 = −6.235 / −3.689、`z ≤ −4.00` の候補比率 2.28% / 0.71% ＝3.2倍）ため、**選定期間で較正した `z*` は確認期間へ転送されない**。<br>**第3版での訂正**: §2.2 の営業日算術2件（確認期間 232→**231**本、選定期間の E-2B 除外 10→**9**本 ⇒ イベント日数 173/221→**174/220**）。分割の判断は不変。§3.6 の `N_ann` 分母が一意に定義されていなかった事実も記録（B は 183/237 を使用。**3通りの読みすべてで DS-2a 不合格・DS-2b 合格＝ゲートの結論は不変**。設計側の過失であり B の N-8 違反ではない）。 | **司令塔判断待ち（S から2件を上申）**。<br>**D-9（最重要）**: **契約データ期間（Free・2年・487営業日）が日次ブロック統計にもとづくイベント駆動検証に構造的に不足**。本PJは**2件連続**で同じ壁に座礁した（PEAD DS-2=**27日**/30日、本EXP DS-5=**25日**/80日・DS-6=**62日**/100日。いずれも「確認期間に相異なるイベント日が足りない」）。選択肢: ①データ拡張（有料プラン）／②イベント駆動族を凍結し常時ポジション族へ／③日次ブロック統計を要求しない設計（STEP3 直行）。**S の推奨順位は ①>③>② だが、①は費用の決裁であり S の職掌外。**<br>**D-10**: **「イベントが市場共通因子で駆動されるか」を prescreen の必須スクリーニング項目に格上げするか**（発生日分布＝相異なる日数・最大単日シェア・選定/確認の発生率比 の3点をリターンを見ずに先に測る）。**本EXPの実測水準なら prescreen で条件付きGOを出していない。** 運用ルール（`_templates/prescreen.template.md`）の変更のため司令塔の了解を求める。<br>成果物: `research/EXP-OBS000003/10-result/{feasibility,params}.json`・`run.log`。**改訂後 spec: `research/EXP-OBS000003/01-spec.md`（第3版・§6.0.1／§13 D-9・D-10）** | 2026-09-14 |

## パイプライン段階の凡例

`S目利き → A設計 → B実装 → C検証 →(司令塔GO)→ D反映 → E記録`

## STEP0 進行状況（実験起票前の基盤整備）

| # | タスク | 状態 | 担当 |
|---|---|---|---|
| 0-1 | PJ000001 プロジェクト方針の起票 | ✅ 完了（2026-09-13） | Claude Code |
| 0-2 | ドキュメント体系・フォルダ構成の整備 | ✅ 完了（2026-09-13） | Claude Code |
| 0-3 | PJ000002 マルチエージェント運営計画書の作成 | ✅ **完了（2026-09-13）**: 縮小版3体（S/B/C）で起票。A設計は S 兼務、D/E は Claude Code 本体が当面代行。3体→6体の拡張条件を明記 | Claude Code |
| 0-4 | `.claude/agents/` 各エージェント定義の作成 | ✅ **完了（2026-09-13）**: chief-strategist(opus) / quant-researcher(sonnet) / adversarial-reviewer(opus) の3体を作成 | Claude Code |
| 0-5 | 重大論点 C-1（損切/利確比の逆転）の決着 | ✅ **決着済み（2026-09-13）**: 決済ルール＝トレーリングストップに変更（固定利確幅は置かない）。司令塔判断3 | — |
| 0-6 | 重大論点 C-2（単元株とポジションサイズ）の決着 | ✅ **決着済み（2026-09-13）**: 運用資本100万円・レバレッジ2.0倍（建玉枠200万円）。司令塔判断1 | — |
| 0-7 | 重大論点 C-3（4戦略の同族性）の再編 | 🔶 方針決定・着手可能（2026-09-13）: 時間軸併存構成（テクニカル系＝主軸1時間足/エントリー15分足、非価格系＝日足）。司令塔判断7 | S戦略 |
| 0-8 | kabu STATION API 実機疎通（localhost:18081 検証環境） | ✅ **完了（2026-09-13・司令塔実測）**: `scripts/check_kabu_station.py` でlocalhost:18081への接続・APIトークン発行に成功。H-2の単一障害点（常駐PC依存）自体は残存するが疎通自体は確認済み | 司令塔 |
| 0-9 | J-Quants API 疎通・プラン選定 | ✅ **完了（2026-09-13・司令塔実測）**: 銘柄コード72030は正しく、原因はHTTP 429（レート制限）だったと判明。日足は契約開始日2024-06-21〜2024-10-01の5ヶ月連続で取得を確認（C-4の結論を裏付け）。R-1（会社予想フィールド）は解消済み。**R-1c（equities/masterの構造）のみ、単体スクリプト`scripts/check_master_schema.py`での再確認が必要** | 司令塔 |
| 0-10 | `requirements.txt` の修正（インストール不能な行の除去） | ✅ **完了（2026-09-13）**: `Python>=3.11`／`pandas-ta>=0.3.14`／`numpy-financial>=1.21`／`asyncio>=3.4.3` の4点を修正。`pip install --dry-run` で全依存解決を確認済み | B実装 |
| 0-11 | `.env.example` の修正（API ベースURL の誤り） | ✅ **完了（2026-09-13）**: kabu API を localhost:18080/18081 へ修正、大引け 15:30 へ修正、資本100万円・同時保有8銘柄・時間足設定を反映 | B実装 |
| 0-12 | `README.md` の PJ000001 整合化（取引時間・STEP3期間・銘柄コード誤り） | ✅ **完了（2026-09-13）**: README と STEP1-planning.md を整合化（15:30 / STEP3=90暦日 / 4戦略→1スリーブ再編 / 7203トヨタの銘柄コード誤り修正） | E進行 |
| 0-13 | 重大論点 C-4（1時間足/15分足の過去データが無償で入手できない疑い）の決着 | ✅ **決着（2026-09-13）**: 楽天証券RSS（Excel要件で不採用）・Yahoo Finance系（規約・信頼性リスクで不採用）を検討のうえ、**J-Quants無料プラン（直近約2年）でバックテスト＋STEP3フォワード較正で補完**する方針に確定。選定/確認の事前分割は適用せず単一検証期間として扱う | 司令塔 |
| 0-14 | 重大論点 C-5（需給データが現行契約で取得不可）の決着 | ⬜ **新規（2026-09-13）**。投資部門別売買・信用残・空売り比率が403。有料化/代替情報源/Killの3択を司令塔判断待ち。PEADは実現性を維持しており着手は待たない | 司令塔 |
| 0-15 | 重大論点 **C-6（データ予算が戦略設計を構造的に制約している）**の決着 | ⬜ **新規（2026-09-13・EXP-OBS000003 prescreen で起票）**。真に非価格の情報源はすべて有償の壁の向こう（TDnetアドオン ¥12,650/月＝資本比**15.2%/年**、分足 ¥7,150/月＝8.6%/年、指数 ¥1,650/月、需給 ¥3,300/月、先物 ¥16,500/月＝19.8%/年、**板情報は全プランで提供なし**）。資本100万円ではデータ費用が North Star（Sharpe 1.0）の6〜10割を食う。選択肢: ①月額¥0維持 ②TDnet課金 ③資本増 ④**無償の自前蓄積を今日から開始**。S見解は**①＋④の併用**（判断事項 D-1）。<br>**2026-09-13 追記（S見解の下方修正）**: ④は着手済みだが、**無償で取れるのは適時開示インデックス（表題まで）であり本文ではない**と判明したため、**④は②の完全な代替ではない**（12ヶ月待っても候補③が賭ける「本文の解釈」は解禁されない）。さらに同日、非価格候補のうち「決算発表予定日の前倒し/後ろ倒し」と「TDnetインデックス単体」の2件が却下となり、**本PJの賭けを検証できる実験は EXP-OBS000001（PEAD）1本のみ**になった。その PEAD は 0-19（D-5）で停滞中である。**→ D-1 と D-5 は独立した2件ではなく「同じ制約に対する1つの判断」として扱うことを S として具申する**（`STRATEGY-BRIEF.md`「訂正にともなう戦略的現在地の更新」節） | 司令塔 |
| 0-16 | TDnetアドオンの月額（¥11,000）の目視確認 | ⬜ **新規（2026-09-13）**。一次ページ（jpx-jquants.com / jpx.co.jp）が Bot対策で HTTP 403 のため自動取得できず、金額は検索結果由来。**C-6 および候補③の保留判断の前提となる数値**。J-Quantsマイページで目視確認が必要（判断事項 D-2）。<br>**2026-09-13 追記・ほぼ解消（S戦略チームの実測。詳細 → `research/EXP-OBS000001/09-data-source-survey.md` §5）**: **403 はブラウザ相当の User-Agent を付けることで解消**し、一次ページ `https://jpx-jquants.com/`（→`/ja`）の料金セクションを機械取得できた。**TDnet/適時開示アドオン ¥11,000/月（5年）を一次情報で確認**（分足・Tickアドオンは ¥5,500/月・2年。いずれも Free には付けられず Light 以上が必要）。プラン本体は **Free ¥0（2年・12週遅延）／Light ¥1,650（5年）／Standard ¥3,300（10年）／Premium ¥16,500（最大20年）**。**重要な副次発見: `/fins/summary`（Financial Data (Summary only)）は Free を含む全プランに含まれており、PEAD は TDnetアドオンを必要としない**（→ 0-15 C-6 と 0-19 D-5 の前提が変わる）。**残る未確認はプラン別履歴長が `/fins/summary` にも適用されるか（S-1）のみ**で、これはマイページ／サポートでの確認が必要 | 司令塔 |
| 0-17 | `portfolio-ledger.md` の **OBS000022 の行の欠落**の補完 | ⬜ **未解消（2026-09-13・EXP-OBS000002 で発覚、EXP-OBS000003 でも解消できず）**。姉妹リポジトリ `trading-app-v2` が実行環境に存在せず内容確認不能。**未解消の間、いかなる prescreen も「輸入した負の資産との重複チェックを全件実施した」と主張できない**（EXP-OBS000003 §5.1 で明示的に留保済み）。C品質チームは review 時に補完状況を確認すること（判断事項 D-3） | 司令塔／E進行 |
| 0-19 | **EXP-OBS000001（PEAD）の DS-2 不足に対するデータ拡張の是非**（判断事項 **D-5**） | ✅ **司令塔判断済み（2026-09-14）: 選択肢①（保留のまま凍結）を選択。** PEADは「直近の完了/差し戻し」の項に記録。データ拡張（③Standard等）は将来再検討の余地として `09-data-source-survey.md` に材料を残したまま保留。以下は判断時点の記録。DS-2 実測27日／閾値30日（3日不足）で K-5「保留」。**3日不足そのものより、`K`（=DS-2）と `n_d` が spec §6.1 の MDE 前提（K≈60・n_d≈12 → MDE 0.064）から外れ、実測 MDE が 0.11〜0.13 に劣化していることが本質**（30日に届いても 0.10 までしか改善せず、単一会計ファクターの現実的 IC 0.02〜0.05 を構造的に検出できない）。選択肢: **①保留のまま凍結（¥0・PEADは不採用ではなく判定不能）／②期間の切り直し（＝K-4 HARKing。S は非推奨）／③J-Quants有料プランで履歴延伸（K≈135でMDE≒0.049・テール局面も確認期間に入る・12週遅延の解消でSTEP3も可能。要・実額と提供範囲の確認）／④U-6の175上限の見直し（271銘柄が U-1〜U-5 を通過済み。費用¥0でMDE≒0.068まで回復しうるが、確認期間側の規則変更のため本EXP内では不可＝新規EXPの再起票が必要）**。**①〜④のいずれを選んでも、決定が出るまで G1 以降は実行しない（N-10）**。<br>**2026-09-13 追記（司令塔の確認依頼「J-Quants以外に取得方法はないのか」への回答／S戦略チームが C-4 と同じ枠組みで実測調査）**: **調査結果 → `research/EXP-OBS000001/09-data-source-survey.md`**。要点3つ。<br>**(a) 会社予想データ自体は J-Quants 以外に無償で存在する（実測）。** 東証の2サイト（`www.release.tdnet.info` ＝直近38日／`www2.jpx.co.jp` 東証上場会社情報サービス ＝**約10年分**）から、`/fins/summary` の `FSales`/`FOdP`/`NxFSales`/`NxFOdP` と**同一の原本**（決算短信サマリーXBRL・`tse-ed-t` タクソノミ）がログイン不要・APIキー不要で取得できることを、2016-08-09 開示の実データ取得まで含めて確認した。<br>**(b) しかしそれは DS-2 を解決しない。** DS-2 を縛っているのは会社予想データではなく**日足株価の履歴長（無料プランの約2年）**である。SUE だけ10年あっても D+1〜D+10 のリターンも U-4 の直前60営業日売買代金も引けず、**イベントは1件も増えない**。株価側の無償延伸は C-4 で既に閉じている（本調査でも東証サイトに四本値時系列が無いことを実測）。**→ 選択肢⑤「J-Quants以外の無償源」は却下。**<br>**(c) 選択肢③のコストが一桁下方修正される（一次情報を実測）。** `jpx-jquants.com` の料金ページをブラウザ相当UAで取得（従来の403は解消）。**PEAD が使う `/fins/summary` は Free を含む全プランに含まれており、TDnetアドオン ¥11,000/月 は不要**。履歴延伸に必要なのはプラン引き上げのみで **Light ¥1,650/月（5年・MDE≒0.070）／Standard ¥3,300/月（10年・MDE≒0.049。かつ C-5 の信用残・空売り比率も同時解決）**。従来 C-6 が前提していた **¥12,650/月（資本比15.2%/年）は PEAD に関する限り誤った前提**であり、**Standard なら資本比 3.96%/年**。<br>**S の具申**: ③を採るなら Light ではなく **Standard**（Light の MDE 0.070 は現実的IC レンジ 0.02〜0.05 の**外側**で、2万円/年を払って再び「判定不能」を買うことになる）。**ただし Standard の MDE 0.049 もレンジ上端にかろうじて届くだけであり、課金は検出力を上げるがエッジの存在を保証しない。課金後も §6.0 の DS-2≥30 と §6.1 の合格基準は一切変えず、確認期間の切り直しは必ず新規EXPとして事前登録し直すこと（N-9）。** ③＋④の併用なら MDE≒0.03 で初めてレンジ中心に届くが、両方とも新規EXPの再起票が必要。<br>**課金判断の前提として未確定な1点（S-1）**: Light/Standard の「5年/10年」が **`/fins/summary` にも適用されるか**（料金表は全データ横断の行として書かれており個別注記が無い）。**購入前に司令塔がマイページ／サポートで確認すること** | 司令塔 |
| 0-20 | **EXP-OBS000001（PEAD）spec §7 D項のエンドポイント誤りの訂正** | 🔴 **新規・要対応（2026-09-13・訂正2 の波及）**。`research/EXP-OBS000001/01-spec.md` §7 D項（決算跨ぎ制約 S-3）は「`/equities/earnings-calendar` から保有銘柄の**次回**決算発表予定日を取得する」と実装指示している。**この指示は実行不可能である**——同エンドポイントは仕様上「**3・9月期決算会社のみ**」「**翌営業日**に決算発表が行われる銘柄のみ」を返す設計であり、(a) 12月期・6月期等の銘柄の予定日が原理的に取れず、(b) 過去日付を指定するパラメータが存在しないためバックテストで point-in-time に参照できない。<br>**正しくは `/fins/earnings-date`**（全上場銘柄・`code`/`date`/`scheduled_date` で引ける・2024-06-22〜2026-06-22 を `PubDate`/`SchDate` つきで返す）。<br>**性質**: これは**成功基準の変更ではなく、実装指示の事実誤りの訂正**であるため HARKing には当たらない（`PJ000002` §6 の「基準文言を書き換えない」に抵触しない）。ただし spec はロック中であり、S戦略チームが §7 D項のみを差し替える改訂を行う必要がある。**B実装チームは訂正前の §7 D項を実装してはならない**（`/equities/earnings-calendar` を使うと決算跨ぎ判定が静かに機能せず、H-3 のリスク制約が無効化される） | S戦略（spec訂正）／B実装 |
| 0-18 | **無償スナップショット蓄積ジョブの即時開始（費用¥0）** | 🔶 **(a) 着手済み・要継続／(b) 不要と確定（2026-09-13 更新）**。<br>**(a) TDnet適時開示インデックス**: ✅ `scripts/snapshot_tdnet.py` を実装し、`research/_snapshots/tdnet/` に **2026-08-06〜2026-09-14 の40日分（9,361件・1日平均234件）をバックフィル済み**。当初「ネットワークポリシーで到達不能」と誤診断していたが、原因は**アペックスドメイン `release.tdnet.info` にAレコードが存在しない**ことだけで、`www.release.tdnet.info` では到達可能と判明。**保持期間は約38日**（2026-08-05 以前は HTTP 404）＝これ以上遡れない。**取得できるのはインデックス（開示時刻・銘柄コード・会社名・表題・PDFリンク）のみで、テキスト本文（PDF/XBRL）は依然として TDnetアドオン（有償）が必要**。→ **日次実行の継続が必要**（止めると候補③の1年後解禁の道が閉じる）。ただし解禁されるのは表題レベルの分類までであり、**候補③が本来賭けている本文の解釈は12ヶ月待っても無償では解禁されない**（EXP-OBS000003 §3.2 訂正1・§6.6）<br>**2026-09-13 訂正（S戦略チームの実測。詳細 → `research/EXP-OBS000001/09-data-source-survey.md` §3.3・§6.1）: 「取得できるのはインデックスのみで、テキスト本文（PDF/XBRL）は TDnetアドオン（有償）が必要」は誤りである。** `www.release.tdnet.info` のインデックスには **XBRL 列が存在し、決算短信サマリー XBRL の zip が無償で直接ダウンロードできる**（実測: `081220260807515828.zip` = 53,220 bytes を取得し、`tse-ed-t:NetSales` / `tse-ed-t:OrdinaryIncome` @ `CurrentYearDuration_ConsolidatedMember_ForecastMember` ＝ `FSales`/`FOdP` 相当を抽出。FY開示では `NextYearDuration_…` ＝ `NxFSales`/`NxFOdP` 相当も確認）。無償で取れないのは**本文そのものではなく 38日より古い履歴**である。<br>**→ `scripts/snapshot_tdnet.py` を XBRL zip の取得・パースまで拡張することを S として具申する（追加費用¥0）。** これにより **無料プランの12週遅延を回避して STEP3 フォワード較正の SUE 入力を当日取得できる**（Free の12週遅延は PEAD のフォワードを原理的に不可能にするため、これは実現可能性そのものを担保する）。<br>**なお注意点2つ**: ①**業績予想の修正開示に XBRL が付くのは少数**（2026-08-07 実測で 25件中4件＝16.0%）で、付いた場合もタグ付け品質が低い実例を確認（ただし J-Quants も同じ XBRL を原本とするため無償経路が劣るわけではない＝パリティ）。②`www2.jpx.co.jp`（東証上場会社情報サービス。**約10年分**の決算短信 XBRL が無償で取れる）は JPX 利用規約が**商用目的のデータ収集・二次利用を禁じており**グレーなため、**恒常的な依存先にはしない**（フォワード用途で使うのは `release.tdnet.info` 側のみ）<br>**(b) 決算発表予定日**: ❌ **不要になった**。初版が指定していた `/equities/earnings-calendar` は**エンドポイントの選択が誤り**（仕様上「3・9月期決算会社のみ・翌営業日分のみ」を返す設計であり、常に同一1件が返るのは故障ではなく仕様通り）。正しい **`/fins/earnings-date`** は **Free で 2024-06-22〜2026-06-22 の約2年分を `PubDate`（公表日）／`SchDate`（発表予定日）の変更履歴つきで返す**と実測で確認。**自前蓄積を待たずに point-in-time 再構築が可能**（EXP-OBS000003 §3.4 訂正2）<br>**2026-09-14 追記（司令塔判断・容量問題の解消）**: テキスト本文抽出（`scripts/snapshot_tdnet_text.py`）を全銘柄対象で試みたところ、実測1件平均220KB(PDF)→抽出後数百〜数千文字とはいえ、**バックフィル処理が容器再起動等で繰り返し中断し40日×全銘柄（9,361件）のうち完了2件（0.02%）に留まった**。司令塔は**全銘柄追跡を一旦断念し、③LLM特徴量トラック（0-22）が実際に使う271銘柄ユニバースに限定**することを決裁。`--universe-file`オプションを追加し、ユニバース限定でバックフィルを再実行（**ユニバース該当561件中 ok 560件・empty 1件**＝2026-09-01・95080 九州電力のスキャン画像PDFで抽出文字数0）。<br>**2026-09-14 再変更（現行の運用）**: **テキスト抽出はLLMを呼ばず費用¥0であることから、日次実行の `--universe-file` を外し全銘柄対象に戻した。** 271銘柄ユニバース限定は**LLMによる特徴量抽出（有償APIコール）にのみ適用**する整理に改めた。したがって**ユニバース外銘柄の本文テキストも保存を継続する**（「ユニバース外の本文は失われる」という前回の記述は撤回）。ユニバース限定期の残骸として、ユニバース外に2件のテキスト（2026-08-11・67580 ソニーグループ2,577字／48890 Ｇ－レナサイエンス1,064字）が残っているが、全銘柄対象に戻したため不整合ではない | B実装（(a)の日次実行継続・ユニバース限定） |

| 0-21 | **検証哲学の転換（フォワード中心化）の可否**（判断事項 **D-15**／派生 **D-11〜D-14**） | ⬜ **新規・司令塔判断待ち（2026-09-14・S戦略チームが独立評価を実施）**。司令塔提案「ニュース・経済・財務を柔軟に見て都度判断する型はバックテストで厳密に検証すること自体が無理筋であり、フォワードを回すしかないのでは」を S が3分割して判定。**判定: (A)検証哲学のフォワード中心への転換＝不採択／(B)LLMにトレード判断そのものを委ねる＝不採択／(C)③LLM特徴量の無償フォワード蓄積＝条件付きで採択推奨。**<br>**(A) 不採択の決定打（本PJが回す前に固定済みの数値のみを使った算術）**: **90暦日フォワードの MDE = 1.02〜1.28%/トレード**（σ=4.0%・検出力80%・n=96 または相異なる建玉日60）に対し、本PJが EXP-OBS000003 で固定した目標は **0.85%/トレード**。⇒ **90日フォワードは目標水準ちょうどのエッジを原理的に検出できない。** 真の Sharpe 1.0 のフォワード実績 Sharpe の95%CI は 90日で **[−3.8, +5.8]**（有意化は検出力50%で**5.76年**・80%で**11.77年**）。加えて **C-7 はフォワードで悪化する**——「相異なるイベント日」の上限は経過営業日数そのもので、**231営業日で落ちた DS-6（100日）を60営業日で通すことはできない**。**⇒ D-9 の選択肢③（フォワード直行）を S の推奨順位から外し、①>③>② を ①>②>③ へ改めた。**<br>**(A)+(B) への内部整合性からの反対**: 本PJは既に「G2 の Sharpe では識別不能」と認定し判定の重心を **G1（予測単位）** へ移している。(B) は**予測単位そのものを消滅させる**（判断と執行が同一物）ため、**重心を失い識別不能な Sharpe だけが残る＝検証の放棄**になる。かつ PJ000002 §6 の鉄則「予測単位とパイプラインの両方を測定する」に**収まるレーンが無く、鉄則の改訂を要求する**（S は改訂を推奨しない）。<br>**【新規・重大論点 C-8】LLM判断型は現契約では原理的にバックテスト不能**: 判断モデルの知識カットオフ（**2026年5月**）が J-Quants 契約期間（2024-06〜2026-06）の**約96%を覆う**。**C-6（どの情報源を買うか）・C-7（どれだけの期間を買うか）と独立した第3の軸**であり、**TDnetアドオン課金は候補③のバックテストを解禁しない**（課金が買えるのはテキストと履歴であって検証可能性ではない）。**漏洩は存在証明はできても不在証明ができない**ため、プローブが陰性でも「検証可能」とは結論しない。<br>**【新規・実測】C-6 の前提を部分的に覆す所見**: `research/_snapshots/tdnet/` の**30営業日・9,361件を全件集計**し、**本文PDFリンク保持 9,360件＝100.0%**（XBRL 27.4%）と確定。**無償で取れないのは「本文」ではなく「38日より古い履歴」**。⇒ **候補③はバックテストには課金が要るが、フォワード運用には要らない**＝フォワード化に本物の価値がある唯一の場所であり、その価値は統計的ではなく**経済的**なもの。<br>**司令塔判断4件（本検討から新規発生）**: **D-11**=(C) 蓄積トラックの開始決裁（資本¥0・データ費¥0・LLM API費のみ。**今日始めないと永遠に始まらない**性質を持ち、他の判断を待たずに決裁可能）／**D-12**=PJ000001 §5.2 STEP3 KPI の穴（バックテスト非保有スリーブには未定義。S は「G1 未通過スリーブは STEP3 に進めない」と明記して**閉じる**ことを推奨）／**D-13**=PJ000001 §1.2 と台帳 OBS000027（方向一致49.6%・p=0.83）の不整合（**0-17 と同じ枠**。解消まで S は当該記述を GO 根拠に用いない）／**D-14**=トレード経路上のLLMのモデルID固定（**エイリアス禁止**。PJ000002 §2 の開発エージェント向け方針とは別管轄）。<br>**PJ000001 改訂案 R-A〜R-F を提示済み（S は方針文書を直接改訂していない）。** 詳細 → `research/STRATEGY-BRIEF.md`「検証哲学の転換（フォワード中心化）の検討と判定」 | 司令塔 |
| 0-22 | **③LLM特徴量の無償フォワード蓄積トラックの開始**（判断事項 **D-11** の実行タスク） | 🟢 **事前登録完了・司令塔の最終確認待ち（2026-09-14・D-11 に司令塔GO）**。<br>**正本: `research/_snapshots/llm_features/00-preregistration.md`（第1版・凍結済み）。** S戦略チーム（A設計官兼務）が 0-22 の要求5項目をすべて数値・仕様に落として凍結した。<br>**凍結物**: ①**モデルID `claude-haiku-4-5`**（決定打は**5系〈最新世代〉に `temperature 0` を受け付けるモデルが1つも存在しない**こと。Opus 5／Sonnet 5／Fable 5 系はサンプリング制御が撤去され `temperature` が400で拒否されるため、**停止基準③「temperature 0 の再実行での不一致率」を測れない＝凍結された抽出器と呼べない**。加えてエイリアス懸念には**応答の `model` を毎回照合し、初回確定値と異なれば即停止**する実装で対処。**2026-09-14 訂正**: 初版は「現行世代で受け付ける唯一のモデル」と書いていたが誤りで、Opus 4.6／Sonnet 4.6 も `temperature` を受け付ける〈`claude-api` スキルで確認〉。また Haiku 4.5 は Haiku 系列の最新であり「1世代前」ではない。**選定の結論は変わらない**） ②**プロンプト**（全文作成済み・SHA-256 `b24aac1e…ace98`。将来リターン・評価語・外部知識・単位換算・数値の補完をすべて禁止。**上方/下方の符号すらモデルに判定させず、文書記載の `previous`/`revised` の2数値として出させる**） ③**出力スキーマ**（JSON Schema・SHA-256 `7a049f98…2768`。`company_forecast[]` が PEAD の SUE 入力に直接対応） ④**ユニバース U-LLM = PEAD U-1〜U-5 通過の全271銘柄**（U-6 の175上限は執行側の制約のため継承しない。`codes_sha256 07f86898…cd6f`、`scripts/llm_features_build_universe.py` で決定的に再生成可）。<br>**back-fill 禁止の機械的強制**: `track_start_date = 2026-09-14`。**既存40日分（約9,000件）への遡及適用は実装上不可能**（回避フラグを持たない。`--date 2026-09-11` が exit 3 で拒否されることを実測確認）。<br>**費用（2026-09-14 訂正済み）**: 実測（U-LLM は**1営業日20.5件**・本文平均8,001字・出力前提800トークン/件）から **月額 US$5.94 ≒ ¥891（資本比 1.07%/年）**と見積もり、**上限を月 US$10.00（約¥1,500）・日 US$1.00** で登録（**上限は変更しない**）。**全市場346.7件/日を対象にすると ¥14,171/月となり、C-6 が「払えない」と判定した TDnetアドオン ¥11,000/月 を上回る壁に別名で戻る**——**ユニバース限定が本トラックを成立させている条件そのもの**である。<br>**└ 訂正の内容**: 標本期間 2026-08-06〜09-11 の営業日数を「30日」としていたのは**土曜(08-22・08-29)・日曜(08-30)を営業日に数えた誤り**で、正しくは**27営業日**（総件数 9,361件・554件は不変）。1日あたり件数が 312→346.7件／18.5→20.5件に、月額が $5.38(¥807)→$5.94(¥891) に変わる。**実測の訂正であり基準の緩和ではない**（上限 US$10.00 は据え置き、余裕は1.86倍→1.68倍）。**また、見積りの30%を占める出力トークンの前提（800トークン/件）が事前登録の本文に書かれていなかったため明記した。**<br>**先読み汚染プローブ（C-8）を設計・実施済み（※API実測ではない）**: N=200・±2%・閾値10%・時価総額で層別。**主判定 7.5%（15/200）＝ 漏洩は確定せず。** 事前登録した**プラセボ対照は 11.5%**（同じ回答を無関係な別営業日の終値に対して採点）で真値ヒット率を上回った。**事前登録どおり陰性は不在の証明とせず、C-8 は取り下げない。判定(B)（LLMにトレード判断を委ねる）も覆さない**（(B) の不採択根拠3本のうち本プローブが触れたのは1本のみ）。<br>**└ 2026-09-14 訂正（主判定の結論は不変）**: ①**本プローブは Anthropic API を1回も呼んでいない**——APIキーが無いため、**プローブを設計した `claude-opus-5` エージェント自身が200件に回答した自己採点**である（`probe/answers.json`・`result.json` に記録済みだが、事前登録の奥にしか書かれておらず結果表だけ読むとAPI実測に見えたため、**§9冒頭の警告ボックスに移した**）。設計者＝回答者であり**独立した測定ではない**。APIキー供与後の再実施を D-11e として起票。②「プラセボが真値を上回った→記憶による漏洩でないことを**強く示す**」は**統計的に過大な主張**だった。**McNemar 正確検定で両側 p=0.229**（真値のみ的中13件 vs プラセボのみ的中21件）＝**有意差なし**。正しくは「真値がプラセボを上回らなかった。記憶による漏洩とは整合しないが、**n=200 では検出力不足で断定できない**」。③**15/200 の Wilson 95%信頼区間は [4.6%, 12.0%] で閾値10%を含む**＝**真の漏洩率10%を棄却できていない**（事前登録の判定規則に従って「確定しなかった」と言えるだけ）。<br>**未実施・司令塔依頼（D-11a）**: **実行環境に `ANTHROPIC_API_KEY` が無く、実 API 呼び出しを1回も行っていない**（費用¥0）。モデル実体ID・実費・スキーマ適合率・再現性は**未確定**。キー供与後、`--limit 3` の1回の実行で確定する。**それまで1件も蓄積できず、毎日取り返せない開示を失い続ける。**<br>**副次で発覚（D-11d）→ 2026-09-14 対応済み**: `scripts/snapshot_tdnet_text.py` のテキスト抽出が **9,361件中2件（0.02%）しか進んでいない**ことが判明（「バックフィル実行中」ではなくほぼ未着手）。本トラックは PDF 再取得のフォールバックを持つため運用は止まらない。<br>**└ バックフィル結果の記録（全数調査に基づく訂正）**: ユニバース限定で再実行した結果は「560件成功」ではなく、**271銘柄ユニバース該当行 561件中 `status=ok` が560件・`status=empty` が1件**が正しい。空の1件は **2026-09-01・銘柄コード 95080 九州電力**で、**スキャン画像PDFのためテキスト層が無く抽出文字数0**（再実行しても解消しないため、テキストの存在しない行として恒久的に扱う。OCRはスコープ外）。<br>**└ ユニバース外の残存2件**: **2026-08-11・67580 ソニーグループ（2,577字）／48890 Ｇ－レナサイエンス（1,064字）**。ユニバース限定化より前の試験実行の残骸。**下記のとおり全銘柄対象に戻したため、これらは不整合ではなくなった**（経緯として記録）。<br>**└ 運用の確定（司令塔判断）**: **`snapshot_tdnet_text.py` の日次実行から `--universe-file` を外し、全銘柄対象のテキスト抽出に戻した**（LLMを呼ばず**費用¥0**のため）。**271銘柄ユニバース限定は、LLMによる特徴量抽出＝有償APIコールを伴う工程にのみ適用**と整理し直した。これにより事前登録 §4 の「ユニバース外のテキストは失われない」という記述は**実装と整合するようになった**（従来は `--universe-file` でスキップされており矛盾していた）。ユニバース外の本文（1営業日約326件）も保存を継続する。<br>**日次 Routine の更新は司令塔の確認後に行う（本タスクでは更新していない）。**<br>実装: `scripts/llm_feature_extract.py`（G-1〜G-6 を強制。G-1/G-2/G-3 は実測で拒否・限定を確認）／`scripts/leakage_probe.py`／`scripts/llm_features_build_universe.py`<br><br>以下は起票時（2026-09-14）の要求仕様の記録。<br>司令塔が「全銘柄・全開示種類は維持するが保存形式はPDF生バイナリではなくテキスト抽出のみ」と決裁したことを受け、**テキスト層のみ先行実装**: `scripts/snapshot_tdnet_text.py`（pdfminer.six。実測: PDF平均220KB→テキスト数百〜数千文字、容量1/50〜1/200に圧縮）。既存40日分（`research/_snapshots/tdnet/`）へバックフィル実行中（生PDFは保存しない。テキスト抽出は機械的処理でLLM判断を含まないため、下記③の「back-fill禁止」制約の対象外）。日次Routine（`trig_013zPVFsFNztniaVi3eN9Ru6`）もテキスト抽出を含む形に更新済み。<br>**ここから先（LLMによる特徴量抽出そのもの）は未着手。** 以下の事前登録が必要（開始後に変更するとHARKing）。0-18(a)（TDnetインデックス蓄積）で承認済みの枠を、**インデックス層からテキスト層へ拡張**する。**LLMは判断者ではなく凍結された特徴量抽出器として動かす**（PJ000001 §2.1 ③「トレード可否そのものをLLMに判断させない」を**緩めずに維持**）。**ポジションは取らない（資本¥0）。**<br>**位置づけ（重要）**: **エッジの検定ではなく「データ製造＋実現可能性検証」。したがって採用/不採用の宣告を一切出さない（C品質チームを起動しない）。EXP 番号も与えない**（0-18 と同じ扱い）。12ヶ月後に**課金でも買えない「先読み汚染のない point-in-time な LLMラベル付き適時開示データセット」**を所有することが成果物。<br>**開始前に凍結する事前登録（`research/_snapshots/llm_features/00-preregistration.md` として固定）**: ①**エッジ検定の開始条件** = 蓄積 **245営業日以上** かつ **相異なるイベント日 ≥ 100日**（EXP-OBS000003 DS-6 と同値）かつ **最大単日シェア ≤ 25%**（同 DS-7 と同値）かつ **総件数 ≥ 300件**（EXP-OBS000001 DS-1 と同値）を**すべて**満たすこと。満たして初めて通常のリレー（S:prescreen→S:spec→B→C）に乗せた**新規EXPとして起票**する。**本PJの既存閾値をそのまま再利用し、フォワードだからという理由で緩めた閾値は1つも置かない** ②**凍結対象** = モデルID（**エイリアス禁止・正確なID指定**）／プロンプト（ハッシュ記録）／出力スキーマ／ユニバース（PEAD の U-1〜U-5 通過リスト）／**抽出対象は当該文書だけから検証可能な事実に限定**（将来リターンを予測させない） ③**日次冪等追記のみ・back-fill 禁止**（PJ000001 §6.1 継承） ④**停止基準** = 直近20営業日の取得成功率 < 95% ／ 固定モデルIDの提供終了 ／ 同一文書・同一モデル・temperature 0 の再実行で 2%超のフィールド不一致 —— 該当時は**黙って継続せずバージョン境界を記録** ⑤**月額費用上限**を開始前に数値で登録し初月で実測。<br>**あわせて実施を推奨する費用¥0の先読み汚染プローブ（C-8 の実測）**: 確認期間の開示 **N=200件**について、**価格データを与えず**「5営業日後の**終値（水準）**」を問う。**真値の±2%以内が10%超なら漏洩を確定**。時価総額上位/下位で層別。**陽性は漏洩の証明になるが陰性は不在の証明にならない**ことを**事前に明記**する | **司令塔（最終確認 ＋ D-11a: APIキー供与）** → その後 Routine 組み込み |

## 直近の完了/差し戻し

| 実験ID | 結果 | 概要 | 記録先 |
|---|---|---|---|
| EXP-OBS000001 | **保留（凍結・2026-09-14・司令塔判断）** | **PEAD（決算サプライズ後ドリフト）**。0-19（判断事項D-5）に対し、司令塔が**選択肢①（保留のまま凍結・費用¥0）**を選択。**不採用ではなく判定不能**として棚上げする（K-5準拠）。確認期間（2025-07-01〜2026-06-21）は一度もリターンを参照していないため未見のまま温存されており、将来データ拡張（Standardプラン等）の目処が立てば再開できる。N-10により凍結中もG1以降は実行しない。次の実験（EXP-OBS000003）へ資源を移す | `research/EXP-OBS000001/01-spec.md`（第3版でロック中）／`research/EXP-OBS000001/09-data-source-survey.md`（再開時の判断材料） |
| （EXP-OBS000004 は**採番せず**） | **却下（prescreen 起票前に終了・2026-09-13）** | **決算発表予定日の前倒し/後ろ倒し**（純粋な非価格イベント）。EXP-OBS000003 §6.5 の保留候補。**保留理由（point-in-time 再構築が不可能）は `/fins/earnings-date` の発見により完全に解消した**が、S戦略チームが保留解除にともない自ら全市場を実測した結果**別の理由で却下**となった。<br>**決定打**: 101営業日の全市場スキャン（6,295企業四半期）で**予定日の変更は107件＝1.70%**、**175銘柄ユニバースでは263企業四半期中2件＝0.76%**＝契約2年換算で**約11件／2年**（実質的な3日以上の変更は約5件／2年）。PEAD の DS-1ゲート（300件）の**1/30〜1/100**であり、**検出力不足ではなく検定対象の事象が存在しない**。初回告知→予定日のリードタイム中央値63日＝**日本企業は事前確定した日程をほぼ動かさない**（制度的性質であり課金や期間延伸では解決しない）。<br>加えて ②高検出力版（"シーズン内で早い/遅い"）は**発表日を跨ぐ保有が構造的に必要**で **PJ000001 §8.1 H-3 と正面衝突**（PEADの読み替えは適用不可＝H-3 の廃止が必要）、③H-3準拠版（発表前に手仕舞う）は**シグナルが63日前に公知**で X-5 のコスト算術（グロス0.45%/トレード）を満たす根拠がなく**文献の効果は発表日当日に集中**、④3月期決算2,164社の予定日は**27通りしかなく上位3日で62%集中**するため **OBS000029（実質1本張り）の時間方向再発**を必ず踏む。<br>**PEAD・8連敗のいずれとも重複しない**（司令塔の見立てどおり）。**却下を覆す数値条件**: (a) 実質的な予定日変更が確認期間だけで150件以上（現実測の約30倍） **かつ** (b) H-3 廃止/緩和の司令塔判断。同時成立の道筋は現時点でない | `research/portfolio-ledger.md`（不採用行）／`research/EXP-OBS000003/00-prescreen.md` §6.5<br>実測データ: `research/_snapshots/earnings_date/scan_2026-02-02_2026-06-22.json`<br>再現スクリプト: `scripts/probe_earnings_date_scan.py` / `scripts/probe_earnings_date_analyze.py` |
| （採番せず） | **却下（2026-09-13）** | **TDnet適時開示インデックス（本文なし）単体を入力とする戦略**。`research/_snapshots/tdnet/` に40日分（9,361件）蓄積済みだが、①**表題から上方/下方修正の別が判別できず符号を持つシグナルにならない**、②無料サイトの保持期間の壁で**履歴が38日分しかない**、③符号が取れない以上「他に掛ける/足す」設計にしかならず **OBS000027 への最短経路**。開示頻度・密度の変種も方向を持たず **OBS000018（固定閾値＝蜃気楼）と同型**のため同時に却下。**ただし蓄積は継続する**（候補③の1年後解禁の道を閉じないため） | `research/portfolio-ledger.md`（不採用行）／`research/EXP-OBS000003/00-prescreen.md` §6.6 |
| EXP-OBS000002 | **却下（prescreen で終了・2026-09-13）** | 日本株クロスセクショナル価格ファクター（TOPIX500横断のモメンタム/リバーサル・ランキング）。S戦略チームが目利き段階で却下。決定打は**回す前に確定するコスト算術**（保有7営業日・往復0.35%・グロス2.0倍 → 年間コスト資本比24.5% → 損益分岐に必要なファクター・スプレッド24.5%/年。対して日本株実測は約0.5%/年で40〜50倍不足）。加えて建玉可能数8（ユニバースの1.6%）でクロスセクショナルの作動原理が成立せず、逆日歩によりショート脚が組めず、日本固有の学術エビデンスも逆方向。**B実装チームは起動しない** | `research/EXP-OBS000002/00-prescreen.md` |

## 変更履歴

- 2026-09-13: 初版作成。trading-app-v2 の `research/ACTIVE.md` 書式を踏襲。STEP0 タスク一覧を起票。
- 2026-09-13: 司令塔判断7件が確定（①運用資本100万円・レバレッジ2.0倍 ②ユニバースTOPIX500から150〜200銘柄 ③決済＝トレーリングストップ ④ニュース取得＝無償最大限 ⑤J-Quants＝無料プランでまず実機確認 ⑥体制＝縮小版3体（S・B・C） ⑦時間軸＝併存構成）。これを受け 0-5・0-6 を決着済みに、0-7 を方針決定・着手可能に更新。0-10（requirements.txt 修正）を完了に更新。0-3・0-4・0-11・0-12 は他エージェントが本日並行作業中のため状態を反映。新規タスク 0-13（重大論点 C-4: 分足過去データの入手可否）を追加し、0-9 への依存関係を明示。次に起票予定の実験（第1候補）を「現在進行中の実験」欄に注記。
- 2026-09-13: 0-9（J-Quants API V2 実機疎通）を司令塔が実測し完了。契約範囲2024-06-21〜2026-06-21（約2年）と判明し §6.2 の選定/確認分割が不成立と確定（0-13 一部決着）。需給データ（投資部門別売買・信用残・空売り比率）が403で取得不可と新たに判明し、新規タスク 0-14（重大論点 C-5）を追加。次に起票予定の実験を PEAD に更新（需給が使えないため）。分足の実在有無は403の解釈が未確定で、マイページ目視確認待ち。契約終了日が実行日より過去のため契約状態の確認も必要。
- 2026-09-13: 0-13（C-4）を決着に更新。代替情報源（楽天証券RSS＝Excel要件で不採用、Yahoo Finance系＝規約・信頼性リスクで不採用）を司令塔と協議のうえ、J-Quants無料プラン（直近約2年）でのバックテスト＋STEP3フォワード較正で補完する方針を確定。残る STEP0 タスクは 0-8（kabu疎通、ユーザ環境待ち）と 0-14（C-5判断）のみ。
- 2026-09-13: 0-8（kabu STATION API疎通）を司令塔が実測し完了。新設した `scripts/check_kabu_station.py`（J-Quantsの実測を待たずに単体で数秒確認できる軽量版）でlocalhost:18081への接続・トークン発行に成功。STEP0 タスクは残り 0-14（C-5判断）のみに。
- 2026-09-13: EXP-OBS000001（PEAD）の prescreen が完了し S戦略チームが条件付きGOと判定。ただし prescreen 中に 0-9 レポートの内部矛盾が発覚したため、0-9 を「再実測待ち」に差し戻し。プローブスクリプトを修正（レスポンス構造のデバッグ出力・銘柄コードのフォールバック・R-1/R-1c確認用プローブを追加）。「現在進行中の実験」に EXP-OBS000001 を追加。
- 2026-09-13: 司令塔がJ-Quantsマイページを目視確認し、R-1e（契約有効性）を解消（Freeプランで契約中）。EXP-OBS000001 の条件⓪必須項目（R-1・R-1c・R-1e解消、R-1d部分解消）が整い、Kill条件は不成立。次の担当を「S戦略チームによるspec作成」に更新。
- 2026-09-14: **EXP-OBS000003 が §9-6 DS ゲート未達（K-5）となり、S戦略チームが spec を第3版へ改訂して「保留（判定不能）」を確定。** 決定的根拠は **DS-6（実測62日／閾値100日）が `z*` に依存しない量**であること——プール `P` は `z ≤ −1.5`（N-9 で凍結）で定義され `z*` を含まないため、較正グリッドの再設計では絶対に動かない。**基準は1つも緩めておらず、確認期間のリターンは未見のまま温存。** DS-2a のグリッド拡張・DS-5/DS-7 を満たす窓の探索・分割解消・プール下限の緩和はいずれも K-4 または鉄則違反として**行わないことを明記**した。§2.2 の営業日算術2件（231本・9本）を訂正し、§3.6 の `N_ann` 分母が曖昧であった事実を「3通りの読みすべてでゲートの結論は不変」という検算とともに記録（結論が同じでも分母を選び直すことは K-4 と区別できないため書き換えない）。**司令塔判断 D-9（契約データ期間の構造的不足。PEAD と本EXP の2件連続の座礁）・D-10（市場共通因子駆動イベントの prescreen 事前スクリーニング化）を上申。**
- 2026-09-13: S戦略チーム（A設計官兼務）が EXP-OBS000001 の spec `research/EXP-OBS000001/01-spec.md` を起票。SUE定義・約定モデル・トレール4パラメータ・ユニバース・リスク集中上限・G1/G2 の数値基準を回す前に固定し、G3/G4 は在庫ゼロのため判定不能として申し送り。次の担当を「B実装チームによる実装」に更新。
- 2026-09-13: S戦略チームが EXP-OBS000003（司令塔提示テーマ「価格以外のインプット」）の prescreen を起票。司令塔が列挙した5情報源を**1本に束ねず分解**し（束ねる設計は OBS000027 と完全に同型と判定）、一次情報でデータ取得可能性を確認したうえで個別判定した。判定: **#4 オーバーナイト・ギャップのみ条件付きGO**／#1 業界情報・#3 マクロ・#5 板情報は**却下**／#2 経営情報テキストは**保留**（既存候補③と同一のため新規EXPを起票せず吸収）。「現在進行中の実験」に EXP-OBS000003 を追加（担当＝S戦略の spec 起票、ただし優先度は PEAD の次）。副産物として STEP0 タスク **0-15（重大論点 C-6: データ予算制約）・0-16（TDnetアドオン月額の目視確認）・0-17（OBS000022 欠落の補完・未解消）・0-18（無償スナップショット蓄積の即時開始）**の4件を追加。なお司令塔提示の前提「夜間取引は非価格インプット」は**誤りと判定**し（ギャップは価格由来）、本EXPを PJ000001 §4 C-3 の低事前確率族に属するものとして事前確率「低〜中（低寄り）」で扱う。文献が報告する**1日保有版は §7.5 のコスト算術（必要グロス0.45%に対し効果量0.07%＝6.4倍不足）により回す前に却下**し、スコープ外とした。
- 2026-09-13: S戦略チームが EXP-OBS000002（日本株クロスセクショナル価格ファクター）の prescreen を起票し **却下**と判定。spec を書かずに終了するため「現在進行中の実験」には載せず、「直近の完了/差し戻し」に記録。副次的な所見として、(a) 司令塔判断7（テクニカル系＝1時間足主軸）はクロスセクショナル設計と構造的に両立しない（1時間足で年間コスト857%）、(b) 建玉8スロットではスリーブ並走自体がブレッドを削る、(c) `portfolio-ledger.md` に **OBS000022 の詳細行が欠落**しており重複チェックが全件実施できない、の3点を司令塔申し送りとした。
- 2026-09-13: S戦略チーム（A設計官兼務）が EXP-OBS000001 の spec を **第3版**に改訂し、B実装チームの差し戻し2件のうち**停止理由B（選定期間ユニバース構築不能）を解消**。U-4 の窓長60営業日・閾値5億円・U-1〜U-7 の他条件・§6 の全数値基準は**一切変えず**、参照営業日カレンダー `T`（§9-2 で B が既に用いた「候補全銘柄の観測日付の和集合」と同一定義）を §2.1.0 に正本として明文化したうえで、**選定期間のユニバース確定日と開始日のみを `T[61]`（推定 2024-09-18）へ後退**させた（§2.1.1）。先読みバイアスを生まない理由を §2.1.2 に4点で、採用しなかった代替案3案の却下理由を §2.1.3 に明記。**確認期間（区切り・ユニバース175銘柄・DS-1〜DS-4 実測値）は一切変更せず、再計算を N-9 で禁止**（DS-2=27日と判明した後の確認期間側の規則変更は K-4 HARKing に該当するため）。代償として、契約2年間で唯一のテール局面（2024年8月急落）が選定・確認の**どちらの測定窓にも入らなくなり**、選定期間の決算シーズンが4回→3回に減ることを §2.3・§12 に明記した。併せて**実測 MDE の開示**（仮定 K≈60・n_d≈12 → 0.064 に対し、実測 K=27・n_d∈[5.0,9.1] → 0.11〜0.13）を §6.1 に追記（**閾値変更ではなく検出力の開示**）、先行タスク **9-0**（選定ユニバース再構築）・**9-6**（`feasibility.json` の 9-3 に見つかった2件の総和不整合の解消）を新設、禁止事項 **N-9・N-10** を追加。**停止理由A（DS-2=27日／閾値30日）は解決していない**。S は基準を緩めず、K-5 のとおり司令塔判断に上げ、STEP0 に **0-19（判断事項 D-5）**を起票した。
- 2026-09-13（**事実誤認2件の訂正**）: opusサブエージェントによる独立検証で `research/EXP-OBS000003/00-prescreen.md` の事実誤認2件が判明したため、prescreen・本ファイル・`portfolio-ledger.md`・`STRATEGY-BRIEF.md` を訂正した。**EXP-OBS000003 本体の判定（#4 条件付きGO／#1・#3・#5 却下／#2 保留吸収）と、そこで固定した数値基準は一切変更していない。**
  - **訂正1（TDnet）**: 無料TDnetサイトを「ネットワークポリシーで到達不能」と誤診断していた。原因は**アペックスドメイン `release.tdnet.info` にAレコードが存在しない**ことのみで、**`www.release.tdnet.info` では到達可能**。保持期間は**約38日**（約1ヶ月ではない）、1日平均234件。0-18(a) は **`scripts/snapshot_tdnet.py` として実装済み・40日分（9,361件）バックフィル済み**（保持期間の上限まで遡り済み）。**ただし無償で取れるのはインデックス（表題まで）であり本文（PDF/XBRL）は依然として有償**。したがって候補③の「無償代替は不可」という保留判断は変わらず、むしろ**自前蓄積は課金の完全な代替ではない**という下方修正が加わった。あわせて **TDnetインデックス単体を入力とする戦略を却下**（表題から上方/下方修正の別が取れず符号を持つシグナルにならない）。
  - **訂正2（決算発表予定日）**: `/equities/earnings-calendar` は**エンドポイントの選択そのものが誤り**だった（仕様上「3・9月期決算会社のみ」「翌営業日分のみ」を返す設計であり、常に同一1件が返るのは**故障ではなく仕様通り**）。正しい **`/fins/earnings-date`** は Free で **2024-06-22〜2026-06-22 の約2年分**を **`PubDate`／`SchDate` の変更履歴つき**で返す。これにより 0-18(b)（自前スナップショット）は**不要**となり、「決算発表予定日の前倒し/後ろ倒し」候補の**保留理由は完全に解消**した。
  - **保留解除にともなう採否判定**: S戦略チームが自ら全市場を実測（101営業日・6,295企業四半期・`research/_snapshots/earnings_date/`）したうえで、**EXP-OBS000004 は起票せず却下**と判定した。決定打は**予定日変更が175銘柄ユニバースで263企業四半期中2件＝0.76%＝約11件／2年**という実測であり、**検出力不足ではなく検定対象の事象が存在しない**。詳細と「却下を覆す数値条件」は `portfolio-ledger.md` に記録。
  - **波及して発見した実装上の欠陥**: `research/EXP-OBS000001/01-spec.md` §7 D項（決算跨ぎ制約 S-3）が `/equities/earnings-calendar` を参照しており**実行不可能**。放置すると決算跨ぎ判定が静かに機能せず **H-3 のリスク制約が無効化される**。新規タスク **0-20** として起票し、S戦略が spec を訂正するまで B実装は §7 D項に着手しないこととした。
- 2026-09-13: B実装チームが EXP-OBS000001（PEAD）の spec §9 先行タスクを実測。実測中に `/equities/bars/daily` の `UL`/`LL` が価格水準ではなく0/1フラグであることを発見し spec へ差し戻し、S戦略チームが spec §4.0（BUY_BLOCKED/SELL_BLOCKED・§9-1b新設）を改訂（N-6の正常動作）。改訂後spec で9-1（値の対応付け・一意に決定）・9-1b（UL/LL全ユニーク値`'0'`/`'1'`のみ・欠損率0.0%）・9-2（欠損営業日率0.0%、511候補銘柄・487参照営業日）・9-3（決算集中度・記録のみ）・9-5（RawSUE記述統計・記録のみ）を完了。9-4データ十分性ゲートはDS-2（確認期間「1日5件以上」日数 実測27日／閾値30日）で不成立（DS-1=341件≥300・DS-3=175銘柄≥150・DS-4=0.0%≤5%は充足）。さらに選定期間ユニバース構築中に、U-4の直前60営業日窓が選定期間開始日2024-06-21（契約データの最初の日そのもの）と重なり計算不能であることが判明し、選定ユニバースが0銘柄（U-7緩和後も0）で不成立と実測。両事象とも推測で埋めずS戦略チームへ差し戻し。G1は未実施（`prediction-unit.json`/`pipeline.json`は未生成）。詳細は `research/EXP-OBS000001/10-result/run.log`。
- 2026-09-13: S戦略チームが司令塔の確認依頼「J-Quants以外にこのデータ（決算短信の会社業績予想）を取得する方法はないのか」に対し、**C-4（株価データの代替情報源調査）と同一の枠組みで10候補を実測調査**し、`research/EXP-OBS000001/09-data-source-survey.md` を起票。**(a) 会社予想データ自体は J-Quants 以外に無償で存在する**（`www.release.tdnet.info` 直近38日／`www2.jpx.co.jp` 東証上場会社情報サービス **約10年分**。2016-08-09 開示の実データで `FSales`/`FOdP` 相当の取得まで確認）。**(b) しかし DS-2 は解決しない**——拘束条件は会社予想データではなく**日足株価の履歴長**であり、SUE だけ延ばしてもリターンも U-4 も引けずイベントは1件も増えない（**選択肢⑤「無償の代替源」は却下**）。**(c) 選択肢③のコストを一桁下方修正**——`jpx-jquants.com` の一次料金ページを実測し、**PEAD が使う `/fins/summary` は Free を含む全プランに含まれ TDnetアドオン ¥11,000/月 は不要**と判明。履歴延伸は **Light ¥1,650/月（5年・MDE≒0.070）／Standard ¥3,300/月（10年・MDE≒0.049・C-5も同時解決）**で足り、従来 C-6 が前提した ¥12,650/月（15.2%/年）は PEAD に関する限り誤り（Standard なら資本比 3.96%/年）。**S の具申は Standard**だが、**MDE 0.049 は現実的IC レンジの上端に触れるだけであり、課金はエッジを保証しない。合格基準は一切変えず、確認期間の切り直しは必ず新規EXPとして事前登録し直す（N-9）**。副産物として **0-16 をほぼ解消**（403 はブラウザ相当UAで回避でき、アドオン ¥11,000/月 を一次情報で確認）、**0-18(a) を訂正**（TDnet の本文XBRLは無償で取得可能。無償で取れないのは38日より古い履歴のみ。`scripts/snapshot_tdnet.py` の XBRL 拡張により**追加費用¥0で STEP3 の12週遅延問題を解消できる**）、**0-15（C-6）の前提を更新**（TDnetアドオンが本当に必要なのは候補③の本文解釈だけであり、PEAD・需給とは分離して判断すべき）。EDINET は**四半期報告書の廃止（2024-04-01〜）を2銘柄の法定開示一覧で実測**し、業績予想が法定開示に存在しないことと合わせて却下。kabu STATION API は OpenAPI 全30エンドポイントを列挙し財務・業績予想の口が無いことを確定。
- 2026-09-14: 司令塔が0-19（判断事項D-5）に対し**選択肢①（保留のまま凍結）**を選択し、EXP-OBS000001（PEAD）を「直近の完了/差し戻し」へ移動。「現在進行中の実験」はEXP-OBS000003（オーバーナイト・ギャップ）1本のみとなり、S戦略チームのspec起票に着手する。
- 2026-09-14: S戦略チーム（A設計官兼務）が EXP-OBS000003 の spec `research/EXP-OBS000003/01-spec.md`（第1版）を起票。シグナル定義（`z = g/σ_gap`・60本窓）・除外規則 E-1〜E-9・約定モデル・トレール4パラメータ（初期損切 **−4.0% 固定**／発動+4.0%／追随−2.0%／日足終値判定・翌営業日始値約定）・最大保有5営業日・リスク集中上限（同時5銘柄／建玉総額125万円／**同一イベント日2件**／同一 `S33` 2件）・コスト前提（往復0.45%・感度3点）・**G1-1〜G1-6 / G2-1〜G2-7 / DS-1〜DS-6 / K-1〜K-6** を回す前に数値で固定。担当を「B実装チームによる実装」に更新。要点:
  - **先読みの構造的排除**: `g` は `O(D)` から計算されるため `O(D)` での約定は明確な先読みである。エントリーを **`t_1 = D の翌営業日始値**」に固定し、`O(D)` 約定を N-4 で禁止した。代償として文献が報告する「`D` 日中で完結する反転」を構造的に捨てており、本設計は文献の効果に対し**厳しい側に倒れている**。
  - **PEAD 資産の流用（再解釈しない）**: `UL`/`LL` の約定可否判定（`BUY_BLOCKED`/`SELL_BLOCKED`）とユニバース定義 U-1〜U-7 は `EXP-OBS000001/01-spec.md` §4.0・§5.4 を**参照により取り込み**、確認期間ユニバース175銘柄は PEAD の実測結果を流用（§9-4 で再現一致を確認）。決算発表予定日は **`/fins/earnings-date`** に固定（`/equities/earnings-calendar` は point-in-time 参照が不可能なため禁止）。
  - **選定期間の開始を `T[62]`（推定 2024-09-19）とした**のは、`σ_gap` の60本窓（終値61個・始値60個）と U-4 の60営業日窓が同時に要求する**算術的帰結**（PEAD が §2.1 で踏んだ罠を事前に回避）。代償として**契約2年で唯一のテール局面（2024年8月急落）が選定・確認のどちらの窓にも入らない**ことを測定範囲の空白 B-1 として明記。ストレス時の需給偏りを賭けの中心に置く本EXPにとって、この空白は PEAD より重い。
  - **prescreen から追加・厳格化した5点（いずれも緩める方向ではない）**: ① **権利落ち・配当落ちの除外 E-1/E-2 を新設**し、識別できない場合は実験を回さず差し戻す（K-6。配当落ちは3月末・9月末に集中し方向が常に下向きで反転しないため、混入は安全側に働かない）／② T-10 のコスト感度を **G2-7 として合否ゲート化**（0.60%で損益分岐を割るなら中心ケース合格でも不合格）／③ T-2 のデシル単調性を **G1-5 として合否ゲート化**（`ρ_dec ≥ +0.30`）／④ 同一イベント日の新規建て上限を prescreen 例示の3件から **2件**へ厳格化（OBS000029 の時間方向再発の防止）／⑤ prescreen §7.2 の必要勝率 55.6% に**翌営業日始値約定ギャップ0.30%を織り込んで 59.4% へ再計算**。
  - **検出力の事前開示**: `IC_bar` の MDE は想定 `K≈180日` で **0.041**、DS-6 下限 `K=100日` でも **0.055** であり、**PEAD の実測 MDE（0.11〜0.13）の半分以下＝本PJ最高の検出力**。したがって**本EXPが G1 未達なら「検出力不足」ではなく「エッジ不在」として台帳に不採用記録できる**。一方、水準検定の MDE は DS-5 下限で **0.47%** であり経済的必要水準 0.45% をわずかに上回る（＝損益分岐ぎりぎりのエッジは検出できない）ことを正直に開示した。
  - **司令塔判断が必要な新規2件**: **D-6**（本EXPの確認期間が**凍結中PEADの未見確認窓と同一カレンダー**であること。Sは「契約2年では回避不能であり、窓を避けるために分割点を動かす方が害が大きい」として実行を推すが自明ではない）・**D-7**（配当落ちが識別できない場合、妥協して回さず差し戻す方針の了解）。
- 2026-09-14: **EXP-OBS000003 が D-7 差し戻しを経て spec 第2版へ。実験は継続（Kill ではない）。** B実装チームが §9-2 で「配当落ち（E-2）の識別手段が存在しない」と判定し、K-6・D-7 に従って**実験を回さずに** S へ差し戻した（正しい動作）。S は D-7 が定める「代替策は S が判断する」に従い、B が調べた範囲（除権日フィールドの有無）の**外側**を独立に調査。
  - **結論: 配当落ちは識別可能であり、本EXPは Kill しない。** `/fins/summary` に除権日フィールドは無いが、**会計期末日（`CurFYSt`/`CurFYEn`/`CurPerEn`）と実績配当額（`Div1Q`/`Div2Q`/`Div3Q`/`DivFY`）が存在**する。日本株の配当基準日は会計期末日と一致し、基準日→権利落ち日の写像は **2019-07-16 以降の T+2 決済のもとで一意**（`b(R)` の1営業日前）。**推測を含まない決定的手続き**として spec §3.1.1 に確定した。
  - **識別が本物であることを実データで検証済み**（spec §9-2b の V-1〜V-5）: 傾き `b̂ = −0.942`（se 0.071・理論値 −1 との `t = +0.82`）／**前後±1・±2営業日のプラセボはすべて `|b̂| ≤ 0.075`（−1 から20〜35σ）＝日の特定精度が分割・併合と同等**／日別再現性 8日中7日／カバレッジ 510/511／期末日の算術検算 1015/1015。構成された配当落ちの暦月分布（3月757・9月707・12月146・6月134）も日本の決算期構成と整合。
  - **「安全側だろう」で埋めなかった箇所を実測して2件の修正**: ① `σ_gap` の窓に権利落ちギャップが混入する件を、当初 S は「中央値では無視できる」と見積もったが**実測すると p99 で `σ` が +27% 膨張**し、膨張量が配当利回りと相関する（高配当銘柄を系統的にイベント集合から押し出す横断的歪み）ため、窓定義を改訂した（イベント開始 `T[62]`→`T[68]`。遡り上限66は実測最大値65+1でカバー率100.0000%）。② **§2.5 B-1 の事実誤認を訂正**——「契約2年で唯一のテール局面が測定窓外」は誤りで、**2025-04-07（中央値ギャップ −7.26%・選定期間）は 2024-08-05（−5.07%）より大きく、確認期間にも 2026-03-09（−4.68%）がある**。残る真の空白は「確認期間の独立なストレス・エピソードが実質1回」であると書き改めた。
  - **厳格化（緩和は一切なし）**: **DS-7**（確認期間の最大単日イベントシェア ≤ 25%。実測で `z*=−2.5` 時に単日 2025-04-07 が 10.7%・上位3日で27%）／**G1-7**（イベント件数が最大の1日を除いても `IC_bar` の符号と `R_5` グロス平均が正であること。除く日はリターンを見ずに一意に決まるためHARKingの余地なし）／**V-1〜V-5 を K-6 の対象に追加**。
  - **HARKing 不成立の根拠**: B は §9-2 で停止しており 9-4 以降を未実行。**本改訂時点で S・B・C の誰も前方リターンを一度も観測していない。** G1-1〜G1-6 / G2-1〜G2-7 / DS-1〜DS-6 の既存の数値基準は一切変更していない。C品質チームはこの点を独立に監査すること。
  - **司令塔への報告事項 D-8（照会ではない）**: E-2B が確認期間で2番目に大きいストレス日 **2026-03-30**（中央値ギャップ −4.556%）を除外する。同日は3月期決算会社の配当落ち日でもあり、市場要因（約 −3.2pp）と機械的な配当落ち（約 −1.39pp）を横断的に分離できない（当日の日内回帰 `b̂ = −0.485`）。除外しなければ全ユニバースの `z` が一律約1.2σ押し下げられ**イベントが1日へ集中する＝OBS000029 の時間方向再発**となるため、**S は「除外する」と決定し回す前に凍結した**。確認期間には別のストレス日 2026-03-09（配当落ちではない）が残る。**判断を留保すると「結果を見てから決める」自由度が生まれるため照会にしない。** 司令塔が同意しない場合は N-9 に従い別EXPとして登録し直す。
  - **次**: B実装チームが §9-2b（E-2A/E-2B の構成と V-1〜V-5）を独立に再実行 → 全ゲート充足なら 9-4〜9-9 へ。**V-1〜V-5 のいずれかが未達なら再度 S へ差し戻し、その場合 S は本EXPを Kill する。**
- 2026-09-14: **司令塔提案「検証哲学をバックテスト中心からフォワード中心へ切り替えるべきか」に対する S戦略チームの独立評価を反映。** STEP0 に **0-21（D-15 本体および派生 D-11〜D-14 の司令塔判断待ち）**と **0-22（D-11 の実行タスク＝③LLM特徴量の無償フォワード蓄積。D-11 決裁まで着手しない）**の2件を追加した。**判定は (A)哲学の転換＝不採択／(B)LLMにトレード判断を委ねる＝不採択／(C)③LLM特徴量の無償フォワード蓄積＝条件付きで採択推奨。** 決定打は本PJが回す前に固定済みの数値のみを使った算術で、**90暦日フォワードの MDE（1.02〜1.28%/トレード）が目標水準（0.85%/トレード）を上回る＝目標ちょうどのエッジを原理的に検出できない**こと、および**「相異なるイベント日」の上限は経過営業日数そのものであるため 231営業日で落ちた DS-6 を60営業日で通せない**こと。あわせて**新規の重大論点 C-8（LLM判断型は知識カットオフによる先読みで原理的にバックテスト不能。課金でも期間延伸でも消えない第3の軸）**を起票し、実測（TDnet蓄積30営業日9,361件の**100.0%が本文PDFリンクを保持**）にもとづき **C-6 の「本文が有償」という前提を「38日より古い履歴が有償」へ訂正**した。**D-9 の S 推奨順位を ①>③>② から ①>②>③ へ改めた**（選択肢③＝フォワード直行を推奨順位から外した）。PJ000001 改訂案 **R-A〜R-F** は `STRATEGY-BRIEF.md` に提示のみ（方針文書は直接改訂していない）。
- 2026-09-14: **司令塔が D-11（③LLM特徴量の無償フォワード蓄積トラック）に GO を出し、S戦略チーム（A設計官兼務）が事前登録 `research/_snapshots/llm_features/00-preregistration.md`（第1版）を起票・凍結した。** 0-22 を「決裁待ち」から「**事前登録完了・司令塔の最終確認待ち**」へ更新。
  - **凍結した5点**: ①**エッジ検定の開始条件**（ST-1 蓄積245営業日／ST-2 相異なるイベント日100日＝DS-6同値／ST-3 最大単日シェア25%＝DS-7同値／ST-4 総件数300件＝DS-1同値。**本PJ既存の閾値の再利用であり、フォワードだからという理由で緩めた閾値は1つも置いていない**。あわせて **ST-A**「ST-2〜ST-4 は蓄積の生件数ではなく、新規EXPが事前登録するイベント定義に合致した件数で測る」を明記——本PJは2件連続で生件数ではなく相異なるイベント日で落ちており、同じ取り違えを防ぐため） ②**モデルID `claude-haiku-4-5`**（**現行世代で `temperature 0` を受け付ける唯一のモデル**であることが決定打。Sonnet 5／Opus 5 系はサンプリング制御が撤去され `temperature` が400で拒否されるため**停止基準③を測れない＝凍結された抽出器と呼べない**。エイリアス懸念には**応答 `model` の毎回照合＋初回確定値からの逸脱で即停止**する実装で対処し、提供終了時は**黙って後継へ移らずバージョン境界を記録**する手順を規定） ③**プロンプト全文**（SHA-256 `b24aac1e…ace98`。将来リターン・評価語・外部知識・単位換算・数値補完をすべて禁止。**上方/下方の符号すらモデルに判定させず、文書記載の2数値として出させる**——符号の決め方は下流のシグナル設計そのものであり、抽出器に埋め込むと事前登録の外で設計が固まるため）・**出力スキーマ**（JSON Schema・SHA-256 `7a049f98…2768`）・**ユニバース U-LLM = PEAD U-1〜U-5 通過の全271銘柄**（U-6 の175上限は執行側の制約のため継承しない。`codes_sha256 07f86898…cd6f`） ④**back-fill 禁止の機械的強制**（`track_start_date = 2026-09-14`。**既存40日分への遡及適用は回避フラグを実装していないため不可能**。`--date 2026-09-11` が exit 3 で拒否されることを実測確認） ⑤**費用上限 月 US$10.00（約¥1,500）・日 US$1.00**。
  - **費用の実測に基づく見積り**（**下記は起票時の値。2026-09-14 に営業日数の誤りを訂正済み——現行値はこの直後の訂正項を見ること**）: U-LLM 271銘柄は **1営業日18.5件**（30営業日554件）、本文テキストは**平均8,001字**（無作為30件を実取得。中央値5,450字・最大61,753字）。16,000字上限適用後で **月額 US$5.38 ≒ ¥807（資本比 0.97%/年）**。**全市場312件/日を対象にすると ¥12,472/月**となり、**C-6 が「払えない」と判定した TDnetアドオン ¥11,000/月 とほぼ同額の壁に別名で戻る**。⇒ **ユニバース限定が本トラックを成立させている条件そのものである。**
  - **先読み汚染プローブ（C-8）を設計・実施**（**下記のうち「示した」という表現と実施形態の記載は 2026-09-14 に訂正済み——直後の訂正項を見ること**）: N=200・±2%・閾値10%・時価総額で層別。**主判定 7.5%（15/200）＝漏洩は確定せず。** 事前登録した**プラセボ対照（同じ回答を同一銘柄の無関係な別営業日の終値に対して採点）は 11.5%（23/200）で真値ヒット率を上回り**、少数のヒットが**日付固有の記憶ではなく粗い株価水準の知識**で説明されることを示した（絶対相対誤差の中央値13.6%、下位半分では真値7.0%対プラセボ13.0%）。**事前登録どおり陰性は不在の証明とせず、C-8 は取り下げない。判定(B)（LLMにトレード判断を委ねる）も覆さない**——(B) の不採択根拠3本（①G1に収まる場所がない ②知識カットオフ ③モデル版更新が無管理なレジーム変化）のうち、本プローブが触れたのは②のみで①③は無傷。
  - **実施できなかったことを正直に記録（D-11a）**: **実行環境に `ANTHROPIC_API_KEY` も `ant` 認証プロファイルも無く、`anthropic` パッケージも未導入のため、実 API 呼び出しを1回も行っていない**（費用¥0）。モデル実体ID・実費・スキーマ適合率・再現性は**未確定**。動作確認は dry-run とガード検証（G-1 back-fill 拒否／G-2 ハッシュ不一致で実行拒否／G-3 当日363件→ユニバース内7件への限定と本文PDFの実取得）にとどまる。**キー供与後、`--limit 3` の1回の実行で確定する。**<br>**2026-09-14 追記（司令塔確認）**: 本トラックは**back-fill禁止**の設計そのものにより、開始が遅れても既存データが失われることはない（今日以降の分のみを対象にするため、遅延は単に評価完了時期が後ろ倒しになるだけ）。**緊急性は低いと判断し、司令塔が別途 `ANTHROPIC_API_KEY` を環境変数として設定する予定**（当セッションからは代行不可）。設定完了後に本格運用（日次Routineへの追加）を再検討する
  - **副次で発覚（D-11d）→ 対応済み**: `scripts/snapshot_tdnet_text.py` によるテキスト抽出が全銘柄対象では**9,361件中2件（0.02%）しか進んでいない**ことが判明（0-22 の「バックフィル実行中」は実態と異なり、ほぼ未着手）。**司令塔が全銘柄追跡を断念し271銘柄ユニバース限定に決裁**（0-18に反映）。`--universe-file`オプションを追加しユニバース限定でバックフィルを再実行（**結果: ユニバース該当561件中 ok 560件・empty 1件**＝2026-09-01・95080 九州電力のスキャン画像PDF。ユニバース外にも試験実行の残骸2件〈2026-08-11・67580 ソニーグループ2,577字／48890 Ｇ－レナサイエンス1,064字〉が残存）。<br>**2026-09-14 追記・運用を再変更**: **テキスト抽出はLLMを呼ばず費用¥0であるため、日次実行から `--universe-file` を外し全銘柄対象に戻した。271銘柄ユニバース限定は、LLMによる特徴量抽出（有償APIコール）にのみ適用する**と整理し直した（事前登録 §4 の「ユニバース外のテキストは失われない」という記述と実装の矛盾は、**運用を記述に合わせる**ことで解消）。ユニバース外銘柄の本文も引き続き保存される
  - **日次 Routine は更新していない**（司令塔の最終確認後に組み込む）。新規スクリプト: `scripts/llm_feature_extract.py` / `scripts/leakage_probe.py` / `scripts/llm_features_build_universe.py`。
- 2026-09-14: **opus モデルのサブエージェントによる独立検証で見つかった誤りを、S戦略チームが訂正した（③LLM特徴量トラックの文書群）。凍結物・登録上限・停止基準・開始条件（ST-1〜ST-4）は1つも変更していないため、バージョン境界には当たらない。**
  - **(1) 営業日数の誤り → コスト見積りの再計算**: 標本期間 2026-08-06〜09-11 の営業日を「30日」としていたが、**土曜(08-22・08-29)・日曜(08-30)を営業日に数えた誤り**で、正しくは **27営業日**（総件数 9,361件・554件は不変）。⇒ 全市場 312→**346.7件/日**、U-LLM 18.5→**20.5件/日**、月額 US$5.38(¥807・0.97%/年)→**US$5.94(¥891・1.07%/年)**、全市場参考値 ¥12,472→**¥14,171(17.0%/年)**。**実測の訂正であり基準の緩和ではない（HARKing に当たらない）。月額上限 US$10.00・日次上限 US$1.00 は据え置き**（余裕 1.86→1.68倍）。**結論（ユニバース限定が本トラック成立の条件）は変わらず、むしろ差は広がった。**
  - **(2) 出力トークンの前提が未開示だった件**: 見積りの **30.0%** を占める1件あたり出力トークン数（**800トークン**）が事前登録の本文に書かれていなかったため、1件あたり単価の内訳（入力8,876／キャッシュ読出4,703／出力800トークン＝**$0.013346/件**）として明記。初月の `usage` 実測で置き換える対象に追加した。
  - **(3) モデル世代の矛盾**: 「`temperature 0` を受け付ける唯一の現行世代」（§5.1）と「1世代前」（§12 D-11b）が**互いに矛盾**しており、前者は事実としても誤りだった（**Opus 4.6／Sonnet 4.6 も `temperature` を受け付ける**——`claude-api` スキルで確認）。正しくは「**5系に限れば受け付けるモデルは皆無**、現行表では Haiku 4.5／Opus 4.6／Sonnet 4.6 の3つ」「Haiku 4.5 は **Haiku 系列の最新**であり1世代前ではない」。**モデル選定の結論は不変。**
  - **(4) `temperature=0` の決定性に関する注記を追加**: `temperature=0` は**ビット単位の決定性を保証しない**（バッチ構成・浮動小数点の非結合性・実装差で揺れうる）。**停止基準③の許容幅2%はこの理由で存在する**こと、**結果を見てから広げないこと**を明記した。
  - **(5) 先読み汚染プローブの開示位置と統計的表現**: ①「**API 呼び出しではなく、設計者である `claude-opus-5` エージェント自身の自己採点**」という留保が事前登録の奥にしかなく、結果表だけ読むとAPI実測に見えたため、**§9 冒頭の警告ボックスと §9.1・§9.3 の表に移した**（独立した測定ではない旨も明記）。②「プラセボが真値を上回った→記憶による漏洩でないことを**強く示す**」は過大であり、**McNemar 正確検定 両側 p=0.229（真値のみ13件 vs プラセボのみ21件）＝有意差なし**に訂正。③**15/200 の Wilson 95%CI [4.6%, 12.0%] は閾値10%を含む**＝真の漏洩率10%を棄却できていないことを追記。**主判定の結論（NOT_CONFIRMED・C-8 を取り下げない・判定(B)を覆さない）は不変。** 事前登録 §12 に **D-11e（APIキー供与後の再実施。n と閾値の設計をやり直すこと）** を追加。
  - **(6) テキスト抽出の適用範囲を整理（司令塔判断により運用を記述に合わせた）**: 事前登録 §4 の「ユニバース外のテキストは失われない」が**実装（`--universe-file` によるユニバース外スキップ）と矛盾**していた。⇒ **日次実行から `--universe-file` を外し全銘柄対象に復帰**（LLM を呼ばず**費用¥0**）。**271銘柄限定は LLM 特徴量抽出（有償APIコール）にのみ適用**と整理。**バックフィル結果の記録も訂正**——「560件成功」ではなく**ユニバース該当561件中 ok 560件・empty 1件**（2026-09-01・95080 九州電力のスキャン画像PDF。OCR はスコープ外のため恒久的に「テキストなし」扱い）。ユニバース外の残骸2件（2026-08-11・67580 ソニーグループ2,577字／48890 Ｇ－レナサイエンス1,064字）は全銘柄対象への復帰により不整合ではなくなった。
  - **更新した文書**: `research/_snapshots/llm_features/00-preregistration.md`（§2・§3 ST-B・§4・§5.1・§5.4・§7・§9・§10.5・§12・§13）／`research/_snapshots/llm_features/01-universe-271.md`（選定理由・適用範囲・変更履歴）／本ファイル（0-18・0-22・履歴）。
