# Claude Code Instructions

このプロジェクトは日本株スイングトレードAI自動売買エンジン「Smart Wave Rider」開発のプロジェクトです。
姉妹プロジェクト [trading-app-v2](../trading-app-v2/CLAUDE.md)（暗号資産版）の基本ルール・検証プロトコル・ドキュメント体系を踏襲しつつ、日本株向けに新規構築します。

## プロジェクトの優先事項

- トレード判定ロジックを信頼性の高いものとし本番運用できるようにすること
- そのためのバックテスト、フォワードテスト、検証機能の精度をあげること
- 日本株固有の制約（単元株・値幅制限・取引時間・信用規制）を検証段階から正しく模擬すること

## 開発の方針

- 日本語で回答する。途中で英語になることがあるため気を付ける
- セキュリティを優先する
- APIキーや `.env.local` は読まない
- 提案 → 計画 → 実装調査 → 実装 → 再チェック
- このリポジトリ内のリソースへのアクセスはすべて許可します
- 検証・分析 ⇒ 修正 ⇒ テスト ⇒ デバッグ のサイクルを開発件名をもとに回す
- **セッション開始時に `research/ACTIVE.md` と `research/STRATEGY-BRIEF.md` を読む**。次に `obs/smart_wave_rider/引き継ぎ/01未完了/` の引き継ぎ書を確認する

## 運営体制（マルチエージェント）

- 本プロジェクトの開発・分析はマルチエージェント体制で進める。正式な定義・フロー・役割境界は `obs/smart_wave_rider/00プロジェクト方針/PJ000002-マルチエージェント運営計画書.md` を正とする（**STEP0 で作成予定**。本項は索引＋要点）。
- **構成**: 司令塔（ユーザー）＋ 実験ライン5体（設計チーム→実装チーム→品質チーム→デプロイチーム→進行チーム）＋ その上位に立つ戦略チーム。各エージェント定義は `.claude/agents/*.md`。
  - S 戦略チーム(chief-strategist): 日本株市場の目利き・全体監視・司令塔への具申
  - A 設計チーム(strategy-architect): 実験仕様(spec)確定。成功基準を**回す前に数値で**固定
  - B 実装チーム(quant-researcher): spec に忠実に実装。予測単位＋パイプライン両方の**生データのみ**出力（解釈しない）
  - C 品質チーム(adversarial-reviewer): 好結果を壊しにいく偽陽性の最終防波堤。採用/不採用を宣告
  - D デプロイチーム(integration-deploy): 採用＋司令塔GOのものだけ本番反映
  - E 進行チーム(archivist-pm): OBS移動・リンク修正・引き継ぎ・ACTIVE更新
- **利用モデル**: 全サブエージェント（S・A・B・C・D）=`sonnet` ／ E=`haiku`（エイリアス指定で常に最新世代へ自動追従）。**2026-09-15変更**: 当初はS・C=`opus`だったが、opusの利用制限（セッション/週次）に繰り返し当たり作業が中断したため、司令塔判断により全サブエージェントを`sonnet`に統一した。
- **作業場**: `research/`（`EXP-OBSxxxxx/` ＝実験1件=OBS1件のバケツリレー。`ACTIVE.md`=進行信号機、`STRATEGY-BRIEF.md`=作戦ブリーフ、`portfolio-ledger.md`=採用/不採用台帳、`_templates/`=雛形）。`research/` は揮発、`obs/` が記録の正。
- **鉄則**:
  - **成功基準は「回す前」に数値で確定する**（HARKing防止）。結果を見てから基準を緩めない
  - **予測単位とパイプライン統合の両方を測定する**（片方だけは禁止）
  - **選定（探索）データと確認（未見）データを分離する**
  - B(実装)と C(判定)は必ず別エージェントとし、自分の実装を甘く採点させない
  - 採用ゲートは人間。C の「採用可」は推薦であり、本番反映(D の起動)は**司令塔の最終GO**を必須とする
  - 不採用も負の結果として `research/portfolio-ledger.md` と PJ000001 の変更履歴に残す（偽陽性回避の資産）
  - リレーは全自動ではない。オーケストレーター（Claude）が各サブエージェントを順に起動する運用
- **司令塔の必須操作は2点のみ**: ① S にテーマを渡す ② C の宣告に対し本番反映の GO を出す。状況把握は `research/ACTIVE.md` と `research/STRATEGY-BRIEF.md` の2枚で完結させる。

## trading-app-v2 から輸入した負の資産（必読）

trading-app-v2 は8連敗（OBS000018/022/025/027/028/029/030/031）で「**価格由来の特徴量から方向を予測する**」エッジ族の枯渇を確定させた。
本プロジェクトの既存4戦略（A: MACD+RSI / B: ボリンジャーバンド+CCI / C: 移動平均クロス+ADX / D: ATR+Stochastic）は**この族に完全に含まれる**。

一覧と本PJへの含意は `research/portfolio-ledger.md` の「姉妹プロジェクトから輸入した負の資産」を参照。
**同型のアイデアを起票する場合は、なぜ日本株では結論が変わりうるのかを prescreen で明示すること。**

## コード修正方針

- 修正前に影響範囲を確認する
- あいまいな要求は、不明点を確認してからコード変更を実施
- 不具合があれば他にも同様の不備がないかチェックする
- 要求事項がわかりにくい場合は仕様提示して認識誤りがないことを確認
- 今後の保守メンテナンスを考慮したロジックとする
- 必要に応じてリファクタリングした方がよいタイミングがあれば自ら提案
- スマホで指示することが多いため、ユーザのオペレーションは極小とする
- ユーザにコンソール確認を求めるのではなく、アプリ固有のログ機能の拡張によりデバッグを進める方式とする

### プロジェクト進捗サマリの定期更新

- `obs/smart_wave_rider/00プロジェクト方針/PJ000003-プロジェクト進捗サマリ.md` を**週次または重要判定の直後**に更新する（**STEP0 で作成予定**）。
- 更新内容: ゴールまでの距離（KPI現在地）・直近マイルストーン・進行中実験の状態・重要課題・次のアクション。
- 更新のトリガー: 週次の定例／C品質チームの採用・不採用宣告／フォワード較正の中間チェックポイント（30日/60日/90日）／新しい実験（EXP-OBSxxxxx）の起票または完了／司令塔からの依頼。

## ドキュメント体系

### 基本方針

- 本アプリに関するドキュメントは `docs/` 配下に保管する
- 基本は md 形式とするが、ユーザが求められた場合はその形式で作成する
- 改訂する場合は、変更履歴を入れる
- ドキュメントの更新タイミングは1週間に1回、あるいはユーザから依頼があったとき

### 作成単位

- ドキュメントが巨大にならないよう、章立て単位に1つの MD ファイルとする
- 大きくは以下の4つ
  - A-利用者導入編
  - B-トレーダ向けアプリ活用編
  - C-トレーディング仕様解説編
  - D-開発者向けシステム仕様編
- 上記の単位でフォルダを作成し、目次ファイル（例 `A000-目次.md`）を作成し各種リンクできるようにする
- ドキュメントは `A001-ファイル名.md` とする

## 開発件名管理

### 管理体系

- `obs/smart_wave_rider/00プロジェクト方針` は、個別の開発件名とは別に、プロジェクト全体の方向性を定める方針ドキュメントを格納する。通常の開発件名ライフサイクル（アイデア→検討中→対応待ち→実装中→解決済）には乗せず、**ゴールが達成されるまでクローズしない**。内容の見直しは変更履歴に追記して継続更新する。
- `obs/smart_wave_rider/01開発アイデア` は、アイデアや提案事項のメモ
- `obs/smart_wave_rider/02対応検討中` は、対応方法検討中のメモ
- `obs/smart_wave_rider/70対応待ち` は、現在対応している内容が完了したら実施予定のメモ（ToDo）
- `obs/smart_wave_rider/80実装中` は、実装中のもの
- `obs/smart_wave_rider/90解決済` は、実装完了して解決したもの
- `obs/smart_wave_rider/85レビュー` は、外部（第三者AI・レビュアー等）から受け取ったレビュー・改善提案の原本や、プロジェクト全体の現状レビューを保管する。開発件名ライフサイクルとは別の参考資料置き場。正式採用する場合は通常の OBS 件名として別途起票する。

### 開発件名ファイルについて

- `OBS999999-開発件名.md` とする
- 999999 は 000001 からの SEQ。OBS は固定。ファイル名にこの SEQ が無い場合は付与する
- 方針ドキュメントは `PJ999999-件名.md`（同じく 000001 からの SEQ）

### 開発件名のメモの作成ルール

- ヘッダーに変更履歴を付与する
- MD ファイルから派生して作成された MD ファイルはリンクする。派生課題・関連課題など分類してリンクの紐づけを行う
- ファイル名のリネーム、フォルダ移動によりリンクが切れるため再度リンクを張り直す

### 開発件名のメモの自動整理

- 件名のコード修正を開始したら、実装中フォルダに MD ファイルを移動する
- 課題や提案があれば、件名ファイルを新規作成し開発アイデアに保存する
- 解決したら解決済みフォルダに MD ファイルを移動する
- それぞれ作成・ステータス変更を行った日付のフォルダに入れていく
- 移動元のファイルは削除されてOK。移動元の日付フォルダも MD ファイルがなくなれば削除
- 件名ファイルにて調査結果・対応案・解決方法などは常に件名ファイルをアップデート

## セッション間引き継ぎ（`obs/smart_wave_rider/引き継ぎ`）

### フォルダ構成

- `引き継ぎ/01未完了` … 現在アクティブな引き継ぎ書。**次セッションで最初に読むべき「今どこにいて、次に何をするか」**を書く。原則1件（最新のもの）を置く
- `引き継ぎ/02完了済み` … 対応が完了・陳腐化した過去の引き継ぎ書の保管先

### ファイル命名

- `YYYYMMDD-引き継ぎ.md` とする（1日に複数あれば `YYYYMMDD-用途-引き継ぎ.md`）

### 運用ルール

- **セッション開始時**: まず `01未完了` の引き継ぎ書を読み、現状と次アクションをユーザに確認してから作業を始める
- **セッション終了時 / 区切りの良いところ**: `01未完了` の引き継ぎ書を最新状態に更新する（到達点・次アクション・環境メモ）。古くなった引き継ぎ書は `02完了済み` へ移動する
- 引き継ぎ書には関連する OBS 件名へのリンクを張り、詳細は各 OBS 側に残す（引き継ぎ書は索引＋要約に徹して肥大化を防ぐ）

## TODO 運用

- **全てのプロジェクトルール適用**、および**引き継ぎ資料**・**外部レビュー結果**への対応は、着手前にまず件名単位で TODO に分割する
- 各 TODO は完了の都度クローズする（まとめて後追いクローズしない）

## セキュリティ

- **APIキーを絶対に読まない** → 必ず `.env.local` で管理
- 本番環境の認証情報はコード内に埋め込まない
- Git commit 前に必ず `.env.local` が `.gitignore` に登録されていることを確認

## ディレクトリ構成

```
SmartWaveRider/
├─ CLAUDE.md                  ← 本ファイル（開発ルール）
├─ README.md                  ← プロジェクト概要
├─ docs/                      ← 利用者向けドキュメント（A〜D の4分類）
├─ obs/smart_wave_rider/      ← 開発件名管理（Obsidian Vault）
│  ├─ 00プロジェクト方針/     ← PJ999999-*.md（クローズしない方針文書）
│  ├─ 01開発アイデア/ 〜 90解決済/
│  └─ 引き継ぎ/01未完了, 02完了済み/
├─ research/                  ← 実験の作業場（揮発領域）
│  ├─ ACTIVE.md / STRATEGY-BRIEF.md / portfolio-ledger.md
│  ├─ _templates/
│  ├─ config/parameter-set.json
│  └─ EXP-OBSxxxxx/
├─ src/                       ← 7層アーキテクチャ
│  ├─ data_layer/             ① データ層
│  ├─ signal_layer/           ② シグナル生成・配分層
│  ├─ llm_layer/              ③ LLM特徴量抽出層
│  ├─ decision_layer/         ④ 統合判断層
│  ├─ risk_layer/             ⑤ リスク管理・サイジング層
│  ├─ execution_layer/        ⑥ 執行層
│  ├─ evaluation_layer/       ⑦ 評価・チューニング層
│  ├─ types/ / shared/ / utils/
├─ scripts/                   ← 検証・運用スクリプト（lib/, templates/）
├─ tests/
├─ config/
├─ data/{raw,processed,cache}/
└─ logs/{backtest,forward_test,live_trading}/
```

## Claude API の利用方針

- **ニュース・適時開示の分類/抽出**（大量・低コスト）: `claude-haiku-4-5`
- **シグナル統合・少数の高難度判断**: `claude-opus-5`
- プロンプトキャッシュを効かせるため、銘柄リスト・分析指示など**安定部分を前置**し、日付・当日ニュースなど変動部分を後置する
- LLM の判断根拠は全てログ化し、事後に妥当性検証できるようにする

## ビルド、リソース管理

- github で管理。一人開発のため、main ブランチにて直接コミット
- コミットメッセージには STEP 情報または OBS 番号を含める（例: `STEP2: Implement PEAD sleeve prediction unit (OBS000003)`）

## 関連ドキュメント

- [PJ000001-日本株スイングトレードAI自動売買エンジン](obs/smart_wave_rider/00プロジェクト方針/PJ000001-日本株スイングトレードAI自動売買エンジン.md) ← **プロジェクト計画書**
- [docs/000-目次.md](docs/000-目次.md)
- [research/README.md](research/README.md)

---

**作成日**: 2026-09-13
**最終更新**: 2026-09-13
